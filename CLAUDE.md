# CLAUDE.md

이 저장소에서 작업할 때 참고할 내용.

## 프로젝트

유튜브 영상의 특정 구간만 반복 재생하는 받아쓰기 연습용 웹 페이지.
주 사용 환경은 **아이폰 사파리**다.

## 파일

| 파일 | 역할 |
| --- | --- |
| `index.html` | 앱 전체 (HTML + CSS + JS 한 파일) |
| `SPEC.md` | 동작 명세. 기능 판단의 기준 |
| `README.md` | 저장소 소개 |

## 실행

빌드 과정이 없다. `index.html`을 브라우저로 열면 끝이다.
유튜브 IFrame API를 쓰므로 인터넷 연결이 필요하다.

`file://`로 열어도 대체로 동작하지만, 재생이 막히면 로컬 서버로 확인한다.

```
python3 -m http.server 8000
```

## 규칙

**한 파일 유지.** 번들러, npm, 프레임워크를 도입하지 않는다. HTML/CSS/JS를
`index.html` 안에 그대로 둔다. 파일을 쪼개거나 의존성을 추가하기 전에 먼저 물어본다.

**바닐라 JS, ES5 문법.** 구형 iOS 사파리에서도 돌아가야 한다. `var`와 함수 선언을
쓰고 있으므로 그 결을 따른다. 트랜스파일 단계가 없다는 점을 기억한다.

**SPEC.md가 기준.** 동작을 바꾸면 `SPEC.md`도 같이 고친다. 명세와 코드가 어긋나면
그건 버그다. 코드 주석이 `SPEC 6번`처럼 명세 항목을 가리키는 곳이 있으니
번호를 바꿀 때는 주석도 확인한다.

**UI 문구는 한국어.** 버튼, 안내, 오류 메시지 모두 한국어로 쓴다.
사용자에게 영상 ID나 초 단위 이상의 기술 용어를 노출하지 않는다.

**조용히 실패하지 않는다.** 어떤 실패든 화면 아래 상태 줄(`#status`)에 한국어로
남긴다. `console.log`만 찍고 끝내지 않는다.

## 자주 건드리는 값

`index.html` 상단 IIFE에 상수로 모여 있다.

- `LEAD_IN` (0.3) — 시작점 여유. SPEC 6번
- `TICK_MS` (40) — 끝 지점 감시 주기
- `STOP_MARGIN` (0.04) — 감시 주기로 인한 지연 보정

## 모바일에서 깨지기 쉬운 것

고치기 전에 이유를 알고 고친다. 아래는 의도적으로 그렇게 되어 있다.

- 입력창 `font-size:16px` — 더 작으면 iOS가 포커스 시 화면을 확대한다
- `playsinline:1` — 없으면 아이폰에서 전체화면으로 튄다
- 버튼 `min-height:52px`, `touch-action:manipulation` — 탭 영역과 더블탭 확대 방지
- `env(safe-area-inset-bottom)` — 홈 인디케이터에 버튼이 가리지 않게

## 확인 방법

자동 테스트가 없다. 바꾼 뒤에는 브라우저에서 직접 해 본다.

1. 기본값(10초~15초)으로 재생 → 약 9.7초에서 시작해 15초에서 멈추는지
2. `다시 듣기` 연타 → 감시 타이머가 겹치지 않는지
3. 재생 중 `0.75배속` 토글 → 즉시 반영되는지
4. 영상 ID를 다른 값으로 바꾸고 재생 → 새 영상을 불러온 뒤 구간이 맞는지
5. 잘못된 입력(끝 ≤ 시작, 이상한 ID) → 안내 문구가 뜨고 재생되지 않는지
6. 아이폰 세로 화면 → 확대되지 않고 버튼이 손에 닿는지
