# CLAUDE.md

가족용 영어 받아쓰기(dictation) 연습 웹앱. 이 저장소에서 작업할 때 참고할 내용.

## 먼저 읽을 것

**`SPEC.md`가 기준 문서다.** 앞으로 무엇을 만들지는 **`ROADMAP.md`** 에 있다 —
비용 계산과 "왜 그렇게 정했는지"가 거기 남아 있으니, 대화 연습이나 AI 목소리 얘기가 나오면 먼저 읽는다.

 목표, 아키텍처 결정, 데이터 구조, 화면 흐름, Phase 구분이
전부 거기 있다. 작업을 시작하기 전에 읽고, 스펙과 어긋나는 구현은 버그로 취급한다.

동작을 바꾸면 `SPEC.md`도 같이 고친다. 스펙에 없는 기능을 임의로 추가하지 않는다.

## 현재 구현 상태

Phase 1 진행 중.

**`tools/add_video.py` — 영상 등록 스크립트 (SPEC 8). 동작 확인됨.**
올리기 전에 `git pull --rebase` 로 최신 상태를 맞춘다. PR 머지가 계속 일어나므로
운영자의 PC 저장소는 대개 뒤처져 있고, 그대로 push 하면 거절당한다.
시작할 때 `check_repo()` 로 저장소가 멈춰 있지 않은지 본다 — 운영자는 git 을 모르므로
파이썬 traceback 을 보여 주면 안 된다. 되돌리는 명령을 화면에 적어 준다.
자막을 받아 문장으로 나눠 `data/` 에 JSON을 만들고 git push까지 한다.
PC에서만 돌아간다 — 유튜브가 클라우드 IP를 막기 때문에 이 저장소를 다루는
원격 환경에서는 유튜브에 닿지 않는다. 로직 확인은 `tools/test_add_video.py` 로 한다
(자막 문자열만 쓰므로 네트워크가 필요 없다).

**연습에 못 쓰는 줄은 감춘다 (SPEC 8).** 낱말 두 개 이하(`Hi.` `Yeah.` `Me too.`)나
감탄사뿐인 줄(`No, no, no.`)은 받아쓰기가 안 되는데 자료의 네 개 중 하나였다.
등록할 때 `add_video.py` 가 아예 빼고, 이미 등록한 영상은 `app.js` 의 `usableLine` 이 감춘다.
**지우지 않고 감추는 이유** — 문장카드와 진도가 자리 번호를 가리켜서, 번호가 밀리면
예전 카드가 엉뚱한 문장을 가리킨다. 두 곳에 같은 규칙이 있으니 한쪽을 고치면 다른 쪽도 고친다.
검사는 `node tools/test_lines.js`, `python3 tools/test_add_video.py`.

**앱 — 프로필 선택 / 홈 / 연습 세 화면.**
`index.html` + `style.css` + `app.js` + `store.js`. `data/` 를 XHR로 읽는다.
연습 화면은 듣기 → 타이핑 → 채점 → 힌트까지다. SPEC 6번 1·2·3단계.
정답은 채점하거나 정답 보기를 누르기 전까지 감춘다.

**홈 — 무엇을 할지 고르는 화면 (SPEC 5).** 프로필을 고르면 여기가 나온다.
**첫 화면에 영상 목록을 두지 않는다 (운영자 결정).** 예전에는 곧바로 영상 목록이 나왔고,
받아쓰기 말고도 갈래가 셋이 되면서 목록 하나가 화면을 다 먹고 나머지는 머리말의
작은 단추 다섯 개로 밀려나 있었다. 영상 목록은 `Dictation` 안(`#/{프로필}/videos`)으로 들어갔다.
칸마다 **오늘 어디까지 했는지**를 한 줄 적는다 — 그 값은 화면을 그린 **뒤에** 채운다.
자료를 기다렸다가 그리면 단추가 늦게 뜬다. 자리(`.meta` 의 높이)는 미리 잡아 둔다.
**못 하는 갈래도 감추지 않는다** — 열쇠가 없으면 `Talk` 칸에 그 사실을 적는다.
갈래가 늘면 이 화면에 칸을 하나 더하는 것으로 끝나야 한다. 머리말에 단추를 더하지 않는다.

**`store.js` — 개인 기록 (SPEC 4-2).** IndexedDB 에 진도·오답 낱말·문장카드를 남긴다.
모든 열쇠에 프로필 ID 가 들어간다. 저장을 못 쓰는 브라우저(사파리 사생활 보호 모드 등)에서는
기록만 포기하고 연습은 그대로 되게 하되, 그 사실을 화면에 남긴다.

**오답 리포트 / 통계 화면 (SPEC 4-3).** 홈의 `Report` 로 들어간다.
연속 학습 일수, 최근 14일 학습량, 자주 틀리는 낱말. 낱말을 누르면 그 문장으로 간다.

**주간 판정표 (SPEC 4-3-b).** 리포트 화면 안. 2단계로 넘어갈지 운영자가 주마다 보는 숫자다 —
연 날 / 건너뛴 비율 / 담은 카드를 최근 네 주치로. **셀 수 있는 셋만 센다.**
주는 **달력 주가 아니라 오늘부터 거꾸로 7일씩** 끊는다 — 달력으로 끊으면 화요일에 늘 미달로 보인다.
**첫 기록보다 앞선 주는 판정에서 뺀다.** 안 그러면 시작한 첫 주에 "멈춰라"가 뜬다.
셈은 `node tools/test_gate.js`.

**`record.js` — 따라 말하기 (SPEC 6의 4·5).** 녹음은 메모리에만 두고 문장이 넘어가면 버린다.
음성인식이 되는 기기에서는 받아적어 정답과 견준다. 못 하는 기기에서는 그 칸을 통째로 감춘다.
**멈추기는 무슨 일이 있어도 한 번은 답을 준다** — 알림·녹음기 상태·시계 세 가지로 받는다.
아이폰에서는 녹음기가 저 혼자 멈추고도 알려 주지 않아 단추가 `Stop` 인 채로 굳은 적이 있다.
화면 쪽도 답을 기다리지 말고 **단추를 먼저 되돌린다.**
**아이폰은 마이크를 한 곳에만 준다** — 음성인식이 가져가면 녹음이 끊긴다. 기능 감지로는 못 잡는다.
마이크가 끊기는 것을 보고 알아채서 그 뒤로는 음성인식을 걸지 않는다. 둘 중에는 **녹음이 먼저다.**
검사는 `node tools/test_record.js` (브라우저 없이 기기가 어긋나는 경우를 흉내낸다).

**문장카드 복습 (SPEC 7).** 홈의 `Cards` 로 들어간다. 네 모드 — 한→영, 듣고 말하기,
빈칸 채우기(틀렸던 낱말만 가림), 받아쓰기 재시험.
카드는 **손으로만 담는다** — 연습 화면의 `Save card`, 카드 화면의 `Remove card`.
틀렸다고 자동으로 담지 않는다 (운영자 결정). 틀린 낱말 기록은 오답 리포트용으로 그대로 남는다.

**하루 다섯 문장 (SPEC 6-2, ROADMAP 1단계).** 홈의 `Daily`.
한국어를 보고 영어로 옮겨 말한 뒤 정답과 스스로 견준다. 녹음·문장카드는 받아쓰기 것을 그대로 쓴다.
**채점은 하지 않는다** — 정답이 하나가 아니라서 맞힐 수가 없다 (운영자 결정). 점수도 오답 표시도 없다.
문장은 **일상 대화체**여야 한다. 격식체나 비즈니스 영어가 나오면 실패다.
**영어와 한국어를 한 호출에서 같이 만들지 않는다 (운영자 결정).** 같이 만들면 모델이
한국어도 내놔야 하는 것을 알고 **옮기기 좋은 영어**를 쓴다. 글로 "영어 먼저" 라고 적는 것으로는
안 막힌다 — 실제로 갈라 놓아야 한다. 만드는 단계는 `{ "texts": [...] }` 만 받는다.
한국어는 **영어가 확정된 뒤 제일 마지막에** 옮긴다. 거기서는 영어를 고칠 수 없다.
**한국에만 있는 것을 영어 낱말로 옮기지 않는다.** 회식의 1차·2차를 `first round`/`second round`
로 옮겨 내보낸 적이 있다 — 영어의 round 는 술을 한 차례 돌리는 것이지 자리를 옮기는 것이 아니다.
순서를 지켜도 **상황 자체가 한국 것이면** 영어에 새어 나온다. 그래서 `config.situations` 에는
영어권에도 있는 상황만 둔다(회식·수능·전세 같은 것은 넣지 않는다).
가늠하는 법은 하나다 — **한국을 모르는 사람이 그 영어만 읽어도 통하는가.**
**돈은 달러로 쓴다.** 한국어의 숫자도 같이 달러로 적는다 — 한쪽만 바꾸면 안 된다.
**자연스러움이 다른 모든 조건보다 먼저다.** 12~28 단어는 넘지 말라는 울타리지
채워야 하는 양이 아니다 — **한 문장으로 끝나도 된다.** 하한을 20으로 두었더니
할 말이 끝난 뒤에 한 마디를 더 붙이게 됐고, 그 접착제가 `and that ~` 이었다
(`and that helps nobody`, `and that was that`). 쉼표로 이으면 글이 되는 것은 그대로라
쉼표 검사는 남긴다. 난이도는 길이가 아니라 **내용**에서 만든다.
**다섯 개의 길이를 서로 다르게 한다** — 둘은 20 단어 이하여야 한다(`shortCount`/`shortWords`).
상한만 두었더니 45개가 길이도 비슷하고 한 문장으로 끝난 것이 하나도 없었다.
길이는 셀 수 있으므로 이건 기계가 본다.
`that`/`it` 은 무엇을 가리키는지 낱말 하나로 짚을 수 있어야 한다 — 앞 문장 전체를 받지 않는다.
구체적인 물건·시각·요일을 넣는다.
**자연스러운지는 글자 모양으로 걸러내지 않는다.** `and that was that` 은 제자리에 쓰면 멀쩡한
관용구고, `and that helps nobody` 가 나쁜 것은 시제가 안 맞아서지 그 글자 때문이 아니다.
기계는 그 둘을 구별하지 못한다 — 규칙을 두면 모델이 문제가 아니라 그 글자를 피하게 된다.
기계는 셀 수 있는 것만 본다(단어 수, 중복, 강조한 표현이 문장에 실제로 있는지).
말버릇(honestly, like)을 장식으로 넣으면 안 된다 — 뜻이 맞을 때만.
**워크플로를 연달아 돌리면 안 된다.** 줄 서 있던 실행은 대기를 시작한 시점을 체크아웃해서
앞 실행이 이미 올린 묶음 번호를 또 만든다. 그래서 만들기 전에 최신 상태로 맞추고,
그래도 부딪히면 되감아 새 번호로 다시 붙인다. **되감기를 빼면 남은 재시도가 전부 헛돈다** —
실제로 겪었다(s015~s017 이 두 번 만들어져 다섯 번의 재시도가 다 같은 오류로 실패했다).

문장은 `.github/workflows/daily-sentences.yml` 이 **매일 새벽 5시 23분(한국시간)에 저절로** 돈다.
운영자가 눌러 줄 필요가 없다. 정각을 피한 것은 깃허브가 정각에 예약이 몰려 거르기 때문이다.
급하면 Actions 화면에서 버튼으로 바로 돌릴 수도 있다. Claude Code CLI 를 돌려 만든다 —
**운영자 구독 한도로 도므로 비용이 들지 않는다.** 유료 API 를 끌어들이면 안 된다.
만드는 쪽 로직은 `tools/daily.js`, 검사는 `node tools/test_daily.js`.
화면 위 날짜 줄은 최근 5개만 단추로 두고 나머지는 목록에서 고른다 —
**달력은 쓰지 않는다.** 공부한 날은 드문드문 있어서 달력을 열면 대부분이 빈 칸이다.

**최근에 쓴 상황과 문장은 `sets/` 에서 읽는다.** 옛 날짜 파일만 보던 때가 있었는데,
그러면 한 번에 만드는 세 묶음이 서로를 못 봐서 같은 상황이 두 번 나온다 — 실제로 겪었다.
`avoidRecentSets`(3) 개만큼 거슬러 본다. **최근 영어는 더 멀리 본다**(`avoidRecentTexts`, 8) —
상황은 며칠이면 한 바퀴 도는데 내용까지 같이 돌아오면 안 된다. 실제로 "가방 싸 놓음 + 비 + 무릎"
운동 핑계가 세 번 나왔다. 상황 창을 넓히면 쓸 상황이 없어지므로 **내용 창만 넓힌다.**

**`sets.json` 은 앱이 나눠 줄 목록이고, `sets/` 는 만들어 둔 전부다.** 둘은 다르다.
옛 묶음을 그만 내보내고 싶으면 **목록에서만 빼고 파일은 남긴다** — 담아 둔 문장카드와
지난 공부 날짜는 파일을 번호로 직접 읽으므로 그대로 산다. 앱은 안 고쳐도 된다.
**번호 매기기와 겹침 피하기는 목록이 아니라 파일을 본다** (`allSetIds`) — 목록을 줄였다고
번호를 다시 쓰거나(파일을 덮어쓴다) 피할 것을 잊으면 안 된다.

**문장 묶음은 날짜에 묶여 있지 않다.** `data/daily/{프로필}/sets/{번호}.json` 에 쌓아 두고,
앱이 공부하러 온 날에 하나씩 꺼내 배정한다(IndexedDB `plan`). 며칠 안 해도 안 버려지고,
만든 날이 아니라 **실제로 공부한 날**이 남는다. **묶음 번호는 절대 바뀌면 안 된다** —
문장카드가 그 번호를 가리킨다. 예전 날짜 파일도 계속 읽히게 둔다.
**영어 만들고 → 소리내어 두 번 읽고 → 말투 바꾸고 → 한국어 붙이고 → 검수한다. 여섯 번 부른다.**
앞의 네 단계는 **영어만** 다룬다 — 한국어가 화면에 아예 없다.
**문장을 좋게 만드는 것은 리뷰지 규칙이 아니다** (운영자 결정). 규칙은 울타리로만 쓴다.
**영어는 두 번 읽힌다.** 한 번만 부르면 "걸리는 데 없음" 으로 넘어가는 일이 잦았다
(세 묶음 중 두 번). 두 번째는 첫 번째가 봤다는 것을 모르는 새 눈이라
"아까 괜찮다고 했으니" 가 생기지 않는다. 회차마다 무엇을 고쳤는지 실행 기록에 남는다.
**소리내어 읽는 자리에는 영어만 보여 준다** (`tools/daily_aloud_*`) — 한국어와 상황을 같이 주면
"번역이 맞느냐"를 보게 되어 어색한 영어가 "잘 옮겼으니 괜찮다"로 넘어간다. 견줄 것을 없애야
남는 질문이 하나가 된다: **사람이 실제로 이렇게 말하나.** 걸릴 데가 없으면 그대로 두게 한다 —
고칠 것이 없는데 고치면 더 나빠진다. 개수나 순서가 어긋나면 안 끼우고 초안을 그대로 쓴다.
검수는 그 다음이고, 거기서는 **영어를 다시 손대지 않는다** — 한국어·alts·note 를 영어에 맞춘다.
**어떤 모델로 돌릴지는 `data/daily/config.json` 의 `model` 에 못 박는다.**
실제로 답한 모델은 실행 기록에 남는다 (`tools/daily_model.js`). 정해 두지 않으면
어느 날 문장이 달라졌을 때 모델 탓인지 지시문 탓인지 갈라 볼 수가 없다.
한 번에 뽑으면 틀린 관용구가 그대로 나간다 (`hit the spot`, `call it a day` 를 엉뚱한 자리에
쓴 채로 나간 적이 있다). 캐주얼은 **관용구를 넣는 게 아니라 더 짧고 축약된 구어**다.
`videoId` 가 `daily-` 로 시작하면 앱은 `data/daily/` 에서 문장을 찾는다 — 카드도 같은 규칙을 탄다.

**되풀이 (SPEC 6-1).** 받아쓰기 화면과 대사 장면 둘 다 `Repeat` 를 켜면 멈출 때까지 되풀이한다.
**멈췄다 다시 트는 게 아니라 자리만 되돌린다** — 멈추면 유튜브가 영상 위에 공유 단추를 띄우고
가림막이 깜빡인다. 되돌리는 동안의 멈춤을 사람이 멈춘 것으로 보지 않게 시작 시각도 같이 되돌린다.

**화면에 순서를 적는다 (SPEC 6-2).** Daily 화면의 각 칸에 1·2·3 번호를 붙인다.
따로 도움말 상자를 두지 않는다 — 매일 여는 화면에서 접었다 폈다 하게 만들면 안 보게 된다.
마이크가 없는 기기에서는 말하기 칸이 빠지므로 번호는 그때그때 다시 매긴다.

**드라마 대사 갈래 (SPEC 6-2).** 홈의 `Shows` (Daily 화면 안의 `Shows` 탭과 같은 자리다).
**여기서는 장면을 먼저 본다** — 그 대사가 나온 자리를 화면 안에서 바로 튼다
(앞 한 마디 + 연습하는 대사 + 뒤 한 마디). **저절로 재생하지 않는다** —
한국어만 하고 싶은 날에는 지나칠 수 있어야 한다. 재생기는 연습 화면 것과 따로 만든다
(iframe 은 화면을 옮기면 다시 읽힌다). 같은 문장을 다시 그릴 때는 재생기를 건드리지 않는다 —
정답을 펴는 순간 보던 장면이 멈추면 안 된다.
**대사는 정답이 하나라 여기서만 채점한다** — 영어로 쳐 보는 칸이 있고 틀린 낱말은 오답 리포트로 간다.
지어낸 문장(`Daily`) 쪽은 그대로 채점하지 않는다.
**여기서는 한국어를 감춰 둔다** (운영자 요청) — 누를 때까지 안 보이고 문장이 넘어가면 도로 감긴다.
`Daily` 쪽에서 한국어는 **문제**라 감추면 할 일이 없어지지만, 대사 쪽에서는 **막혔을 때 보는 힌트**다.
펴 놓으면 눈이 먼저 가서 듣기 전에 뜻을 알아 버린다.
**여기서는 영어를 지어내지 않는다** — 자막에서 실제로 한 말을 그대로 쓰고 AI 는 한국어만 붙인다.
영어를 한 글자라도 바꾸면 검사에서 걸린다.
`tools/add_subs.py` 가 PC 에서 자막을 읽어 300줄쯤을 `data/shows/{프로필}/pool.json` 에 올린다 —
**자막 파일 전체를 저장소에 넣지 않는다. 그건 재배포다.** 뽑은 줄만 올린다.
고르기는 두 단계다: 규칙으로 후보 30개 → AI 가 뜻을 보고 5개만. 비속어와 맥락 없는 대사는 뺀다.
**문장부호가 없는 자막은 대사 연습에 못 쓴다.** 유튜브 자동 자막이 문장부호를 붙일 때도
있고 안 붙일 때도 있다 — **"자동이냐 아니냐"가 아니라 "문장부호가 있느냐"가 기준이다.**
등록할 때 `add_video.py` 가 쓸 수 있는지 없는지를 화면에 적어 준다.
받아쓰기에는 써도 여기서는 후보가 0줄이 된다. 어느 영상이 몇 줄을 내놓는지는 워크플로 로그에 남는다.
강연·뉴스처럼 대사가 아닌 영상은 `config.showsExclude` 에 영상 ID 를 적어 뺀다.
검사는 `python3 tools/test_add_subs.py`, `node tools/test_daily_shows.js`.

**대화 연습의 열쇠 넣는 화면 (SPEC 10-b).** 홈의 `Settings`. ROADMAP 2단계의 첫 조각이다.
`talk.js` 가 회사에 직접 말을 건다 — **서버는 없다.**
**열쇠는 기기에 하나이고 프로필별이 아니다** — `store.js` 의 `settings` 저장소만 프로필 ID 를 안 쓴다.
가족이 각자 발급받게 하면 감당이 안 되어서다. **백업에는 안 넣는다.**
**저장하기 전에 실제로 한 번 불러 보고, 안 되면 저장하지 않는다.**
회사를 더할 때는 `talk.js` 의 `COMPANIES` 에 어댑터를 하나 더 쓴다 —
설정값 하나로 갈아 끼우려 하면 안 된다. **만드는 것은 0원, 돈은 열쇠를 넣는 순간부터다.**

**대화 연습 화면 (SPEC 10-c).** 홈의 `Talk`. 화면은 `talkui.js` 가 그린다 —
`app.js` 는 이미 커서 다리만 놓았다.
**답의 모양을 강제하는 것이 이 기능의 전부다** — 칸 넷을 필수로 두면 교정을 까먹는 것이
구조적으로 불가능해진다. 그 약속은 `node tools/test_talk.js` 가 지킨다.
**단추로 굴리지 않는다** — 말하면 듣고, 멎으면 보내고, 답을 읽고, 다시 듣는다.
`Say`/`Send` 를 눌러 굴렸더니 대화가 아니라 서식 채우기가 됐다 (운영자 지적).
**여기서는 녹음기를 안 켠다** — 소리를 저장하지 않으므로 음성인식만 쓰면 되고,
그래야 아이폰 마이크 다툼이 아예 없다. **읽는 동안에는 마이크를 닫는다.**
다 읽었다는 알림이 안 오는 기기가 있어 **시계로 한 번 더 받는다** — 이걸 빼면 거기서 멈춘다.
**고칠 것이 없으면 크게 그리지 않는다.** 할 말 없는데 큰 상자가 뜨면 기계처럼 느껴진다.
**한 턴의 차례는 내 말 → 답 → 고친 것이다.** 사이에 교정을 끼웠더니 내가 한 말과 그 답이
멀어져 어느 것이 짝인지 알기 어려웠다 (운영자 지적). 교정은 대화가 아니라 나중에 볼 공부거리다.
**얼마나 기다렸다 보낼지를 하나로 두면 안 된다** — 1.5초로 뒀더니 생각하는 사이에 잘라 보냈다.
말끝이 관사·전치사·접속사면 아직 하는 중이니 더 기다린다(`It's kind of a` 에서 잘린 적이 있다).
**헷갈리면 기다리는 쪽이다** — 답답한 것보다 잘리는 것이 훨씬 나쁘다. 셈은 `node tools/test_talkwait.js`.
**멈춰도 하던 말은 안 없어진다.** 지우는 자리는 보내고 난 뒤 한 곳뿐이다 —
여러 군데서 지우면 어느 하나가 하던 말을 삼킨다.
**받아적힌 상자가 곧 고치는 칸이다.** 낱말 하나 잘못 알아들었다고 전부 다시 말할 수 없다.
처음에는 `Fix a word` 를 눌러 딴 칸을 열게 했는데 운영자가 되물었다 —
"그냥 바로 수정하면 안 되고 그 과정을 겪어야 되냐". **보이는 글과 고치는 글이 같은 자리여야 한다.**
**칸은 하나뿐이다** (마이크 없는 기기도 같은 칸).
**고치는 동안에도 마이크는 켜 둔다** (운영자 지적) — 고치고 말하고 고치고 말하고 할 수 있어야 한다.
커서가 튀는 것은 마이크가 아니라 그리는 쪽에서 막는다 — 받아적히는 중인 조각은 손대는 동안
안 넣고(마디가 끝나면 뒤에 한 번에 붙는다), 커서 자리는 되돌려 놓는다.
고치는 중에는 저절로 안 보낸다 — **손댈 때 시계를 멈춘다**(말이 들어왔을 때만 재면 이미 돌던 것이 터진다).
**손댈 때·뗄 때 화면을 통째로 다시 그리면 안 된다** — 큰 단추를 누르면 손이 떨어지는데
그때 다시 그리면 누르려던 단추가 사라져 누름이 허공에 떨어진다. 실제로 겪었다.
친 것은 곧바로 `saidFinal` 로 간다 — 칸 안에만 두면 화면을 다시 그릴 때 사라진다.
글자는 **16px 아래로 내려가면 안 된다** — 누를 때 iOS 가 화면을 확대한다.
**되풀이된 낱말을 저절로 지우지 않는다** (운영자 물음, 안 하기로 결정) —
`I had had enough`, `that that guy`, `no no no` 는 맞는 영어고 기계는 더듬은 것과 구별을 못 한다.
게다가 **지우는 것은 조용한 실패다.** AI 쪽은 이미 더듬는 것을 알고 있고,
거슬리면 `Fix a word` 로 사람이 지운다.
**이건 글쓰기가 아니라 말하기다.** 받아적힌 글은 더듬고 되풀이하고 문장부호가 없는데,
그게 **잘못 들은 것인지 더듬은 것인지 진짜 실수인지 가릴 수가 없다.**
**원인을 추측하게 하지 말고 결과를 판단하게 한다** — "사람 입에서 그렇게 나오면 이상한가".
**낱말 목록(`wanna` 는 고치지 마라 …)을 다시 넣으면 안 된다** — 예외를 적기 시작하면
끝이 없고, 모델이 문제가 아니라 그 글자를 피하게 된다 (하루 문장에서 겪은 것과 같다).
**틀은 규칙으로, 무엇이 좋은 영어인가는 상황으로.** 양쪽 다 `node tools/test_talk.js` 가 지킨다.
**마이크와 스피커는 브라우저에서 못 고른다** (SPEC 10-c 의 표). 음성인식에는 마이크를 고르는
길이 아예 없고, 스피커를 고르는 것(`setSinkId`)은 아이폰 사파리에 없다.
**아이폰이 마이크를 켜고 끌 때 내는 소리도 못 없애고 못 바꾼다** — 그건 폰이 내는 소리다.
**줄일 수 있는 것은 횟수뿐이다** — 사파리가 저 혼자 끝낼 때마다 다시 켜면 그 소리가
되풀이되므로, 마지막 말로부터 1분 조용하면 다시 켜지 않는다(`keepListening`).
짧게 잡으면 생각하는 사이에 꺼지니 **여기서도 헷갈리면 기다리는 쪽이다.**
**이미 저 혼자 끝난 것을 또 끄지 않는다**(`stopHearing(true)`) — 끄는 동작이 소리를 낸다.
켤 때와 끌 때 중 **어느 쪽이 들리는지는 우리가 정하지 못한다.** 왜 한쪽만 들리는지 모르면
모른다고 한다 — 원인을 지어내지 않는다.
**고를 수 있는 것은 답을 읽어 줄 목소리다** — 폰에 든 영어 목소리 중에서 고른다(SPEC 10-d).
**얼마 들었는지 화면에 적는다** — 요약에는 이번 대화, 리포트에는 이번 달과 전부.
**달러로 적는다** (운영자 결정) — 콘솔이 달러라서 같은 단위여야 대조가 된다.
캐시에 넣은 것·다시 읽은 것은 배수가 달라 따로 세고 겹쳐 세면 안 된다.
단가는 `talk.js` 회사표에 있고, 셈은 `node tools/test_talkcost.js`.
**지시문은 대화 밖에 둔다.** 거기에 오답 기록과 최근 카드를 실어 보내는데,
그건 덤이 아니라 **캐싱을 걸리게 하는 장치**다 (길수록 싸다).
**같은 재료면 글자까지 같아야 한다** — 조금만 바뀌어도 캐싱이 처음부터 다시 걸린다.
**대화는 기기 안에만 둔다.** 백업에는 요약만 들어간다. 대화 번호는 절대 바뀌면 안 된다 —
문장카드가 그 번호를 가리키고, 그 카드는 저장소가 아니라 **기기에서 찾는다**.

**사람마다 고르는 것 (SPEC 10-d).** `Settings` 맨 위. 색 / 글자 크기 / 차례 넘기기 /
기다리는 시간. **프로필별이다** — `store.js` 의 `prefs` 저장소. 열쇠(`settings`)와 헷갈리면 안 된다.
**기본값과 고를 목록은 `prefs.js` 한 곳에만 둔다.** 화면은 그것을 보고 그린다.
**색은 `var(--…)`, 글자 크기는 `var(--f-…)` 로만 쓴다** — 직접 적으면 그 칸만 안 바뀐다.
**차례 넘기기는 두 갈래를 다 둔다.** 저절로 보내는 쪽을 먼저 만들었는데 생각하는 사이에 잘려서
**누르는 쪽이 기본**이 됐다. 한쪽을 지우면 다른 쪽이 맞는 사람이 못 쓴다.
**누를 것은 하나여야 한다** — 큰 단추 하나에 그때 할 일만, 나머지는 작은 글씨.
셈은 `node tools/test_prefs.js`.

**백업 (SPEC 10).** 리포트 화면 아래. 파일로 저장 / 클립보드 복사 두 길을 다 둔다 —
파일 저장이 막히는 기기가 있다. 불러올 때는 지금 고른 프로필로 옮겨 담는다.

**편집 화면 (SPEC 8).** 영상 목록의 영상마다 `Edit`. 합치기 / 낱말 앞에서 자르기,
자막(SRT·VTT) 붙여넣기로 갈아 끼우기, JSON 내려받기. 내려받은 파일은 사람이 저장소에 넣어야 한다.
**자르기 시각은 추정값이다** — 자막은 줄 단위로만 시각을 주므로 글자 길이로 나눈다.

Phase 1 의 화면은 모두 있다. 남은 것은 실제 기기에서 쓰며 고치는 일.

확인된 것:

- 유튜브 IFrame API로 구간 재생 / 끝 지점 자동 정지가 실제로 동작한다
- 시작점 `-0.3초` 여유를 두면 첫 단어가 잘리지 않는다 (SPEC 4-1)
- 0.75배속 전환이 재생 중에도 즉시 먹는다
- 아이폰에서 `playsinline`으로 전체화면 튐 없이 재생된다
- `file://` 로 열면 유튜브가 Error 153으로 막는다. https에서만 확인할 수 있다

## 사용자에 대해

운영자(사용자)는 **코드를 읽거나 쓰지 않는다.** 안내는 "설치 → 실행 → 확인" 수준으로 쓴다.
코드 조각, 파일 경로, 기술 용어를 설명 없이 들이밀지 않는다.

이미 동작하는 코드를 고칠 때는 **한 번에 한 가지만** 바꾸고, 확인한 뒤 다음으로 넘어간다.

## 작업 흐름

작업은 브랜치에서 하고 PR로 올린다. **PR 은 만든 뒤 물어보지 말고 바로 머지한다** — 운영자가
그렇게 하기로 정했다. 머지 방식은 squash.

다만 아래는 손대기 전에 먼저 묻는다.

- 지금 잘 동작하는 것을 다른 방식으로 바꾸는 경우
- 스펙에 없는 기능을 새로 넣는 경우
- 빌드 도구·프레임워크 도입처럼 제약을 바꾸는 경우

PR 본문에는 **무엇을 왜 고쳤는지, 확인한 것과 확인하지 못한 것**을 적는다.
유튜브 재생은 이 환경에서 확인할 수 없으므로 그 사실을 매번 분명히 남긴다.

## 지켜야 할 제약

**빌드 단계 없음.** GitHub Pages에 정적 파일로 그대로 올라간다. 번들러, npm 빌드,
프레임워크 도입은 스펙 변경 사안이므로 먼저 물어본다. (파일을 여러 개로 나누는 것은 괜찮다 —
금지되는 건 "빌드해야 돌아가는 상태"다.)

**서버 없음.** 백엔드, 유료 API, 유료 서비스를 끌어들이지 않는다. 비용 0원이 전제다.
브라우저에서 하지 못하는 일(자막 수집)은 PC 로컬 스크립트가 맡는다 — SPEC 8번.

> **예외 하나 — 대화 연습(ROADMAP 2단계).** 여기서는 **유료 API 를 쓰기로 이미 정했다.**
> 비용은 운영자 계정에서 나가고, 열쇠는 기기 안에만 둔다. 값과 근거는 `ROADMAP.md` 에 있다.
> 그 작업을 할 때 위 줄을 근거로 되묻거나 거부하지 않는다. **"서버 없음" 은 그대로 지킨다.**
> 다만 회사마다 브라우저에서 직접 부를 수 있는지가 달라서, 그 부분은 ROADMAP 의
> "브라우저에서 직접 부를 수 있나" 표를 먼저 본다 — 확인된 것만 쓴다.

**바닐라 JS, 보수적인 문법.** 트랜스파일이 없으므로 구형 iOS 사파리에서 그대로 돌아가야 한다.
현재 `index.html`은 `var`와 함수 선언 위주의 ES5로 쓰여 있다.

**개인 기록은 IndexedDB.** localStorage 아니다(용량). 키에 프로필 ID를 반드시 포함한다 —
같은 기기를 가족 여러 명이 쓴다.
**예외는 `settings` 저장소 하나뿐이다** (AI 열쇠). 그건 기기에 하나이고 온 가족이 같이 쓴다 —
이유는 SPEC 10-b 에 있다. 다른 저장소에 이 예외를 옮기면 안 된다.

**녹음은 저장하지 않는다.** 메모리에만 두고 문장이 넘어가면 버린다.

**UI 문구는 영어다 (SPEC 9).** 영어 학습 앱이라 화면 자체가 영어인 편이 자연스럽다.
PC 등록 스크립트가 뿌리는 말과 설치 안내는 한국어로 둔다 — 그건 운영자용이다.
영상 ID나 초 단위 이상의 기술 용어를 사용자에게 노출하지 않는다.

**조용히 실패하지 않는다.** 실패는 화면에 한국어로 남긴다. `console.log`만 찍고 끝내지 않는다.
기기가 기능을 지원하지 않을 때(예: 아이폰 Web Speech API)는 오류 화면 대신
**기능 감지로 해당 버튼만 숨긴다** — SPEC 9번.

## 모바일에서 깨지기 쉬운 것

`index.html`에 의도적으로 그렇게 되어 있는 것들이다. 이유를 알고 고친다.

- 입력창 `font-size:16px` 이상 — 더 작으면 iOS가 포커스 시 화면을 확대한다
- `playsinline:1` — 없으면 아이폰에서 전체화면으로 튄다
- 버튼 `min-height:52px`, `touch-action:manipulation` — 탭 영역과 더블탭 확대 방지
- `env(safe-area-inset-bottom)` — 홈 인디케이터에 버튼이 가리지 않게
- **재생 순서**: `playVideo()` 를 먼저 부르고 그 다음 `seekTo()`. 반대로 하면 모바일 사파리가
  재생 명령을 무시해서 "자리는 옮겨졌는데 소리가 안 나는" 상태가 된다. 실제로 겪었다
- 자리를 옮기는 순간 유튜브가 잠깐 PAUSED 를 알린다. 이걸 사용자가 멈춘 것으로 보면
  구간 감시가 꺼져 끝에서 안 멈춘다. 재생 시작 800ms 안의 PAUSED 는 무시한다
- 문장 목록은 접어 둔다. 300개를 펼쳐 두면 뒤쪽 문장을 고를수록 영상과 버튼이 화면 밖으로 밀린다
- 유튜브는 **멈춰 있을 때** 공유 버튼·로고·관련영상을 영상 위에 띄운다. `controls:0` 으로도 안 없어진다.
  멈춘 동안에는 우리 가림막(`#cover`)으로 덮는다. 재생이 시작되면 걷는다
- **재생기는 한 번만 만들고 영상은 `cueVideoById` 로 갈아 끼운다.** 다시 만들려 하면 안 되고,
  갈아 끼우는 것을 빼먹으면 다른 영상을 골라도 앞 영상이 그대로 남는다 — 실제로 겪었다
- 유튜브 자막은 `cc_load_policy` 만으로 안 꺼진다. 준비될 때·재생이 시작될 때·영상을 갈아 낄 때
  `unloadModule('captions')` 한다. **`setOption('captions', ...)` 은 부르면 안 된다** —
  그 함수는 모듈을 먼저 올려야 동작해서, 방금 내린 자막을 도로 올린다. 한국어 자막이 그렇게 붙었다

**서비스 워커(`sw.js`)는 network-first 다. 절대 cache-first 로 바꾸지 않는다** —
고친 코드가 기기에 안 내려가는 덫이 된다. 그리고 워커 안에서 `fetch(req)` 를 그냥 쓰면
브라우저 HTTP 캐시를 타서 옛 파일이 나온다. `cache: 'no-cache'` 를 빼면 안 된다. 실제로 겪었다.

아이폰은 7일 미접속 시 사이트 데이터를 지운다. "홈 화면에 추가"는 선택이 아니라
설치 안내의 필수 단계다.

## 파일을 고쳤으면 버전을 올린다

GitHub Pages 는 정적 파일에 캐시를 건다. `app.js` / `style.css` / `store.js` / `record.js` 중
하나라도 고쳤으면 **`index.html` 의 `?v=` 값을 바꾼다.** 안 그러면 운영자 기기에서
새 화면과 옛 코드가 섞여 "버튼은 있는데 눌러도 아무 일 없는" 상태가 된다. 실제로 겪었다.

## 확인 방법

등록 스크립트는 테스트가 있다. 자막 해석과 문장 분할을 고쳤으면 반드시 돌린다.

```
python3 tools/test_add_video.py
```

하루 문장을 만드는 쪽에도 테스트가 있다. 네트워크가 필요 없다.

```
node tools/test_daily.js
```

채점 규칙에도 테스트가 있다. `app.js` 의 채점 부분만 떼어 확인한다 (앱은 node 가 필요 없다).

```
node tools/test_grading.js
```

주간 판정표의 셈에도 테스트가 있다. 주 경계와 "안 한 주"를 잘 틀리는 자리라 꼭 돌린다.

```
node tools/test_gate.js
```

대화 연습의 "답의 모양" 에도 테스트가 있다. 교정 칸이 필수가 아니게 되면 이 기능은
존재 이유가 없어지므로, 그 약속이 깨지면 바로 걸리게 해 뒀다.

```
node tools/test_talk.js
```

녹음에도 테스트가 있다. 브라우저 없이 **기기가 어긋나는 경우**를 흉내낸다 —
멈췄다고 알려 주지 않는 기기, 저 혼자 멈춘 기기, 음성인식이 마이크를 가져가는 기기.
`record.js` 를 고쳤으면 반드시 돌린다.

```
node tools/test_record.js
```

앱 쪽은 자동 테스트가 없다. 바꾼 뒤에는 브라우저에서 직접 해 본다.
마이크는 크로미움에 가짜 장치를 물려 확인할 수 있다
(`--use-fake-ui-for-media-stream --use-fake-device-for-media-stream`).
원격 환경에서는 유튜브가 막혀 재생만 확인이 안 되고, 화면과 자료 읽기는
로컬 서버 + Playwright(크로미움이 `/opt/pw-browsers/chromium` 에 있다)로 확인할 수 있다.

빌드가 없으므로 `index.html`을 그냥 열면 되지만, 재생이 막히면 로컬 서버로 확인한다.

```
python3 -m http.server 8000
```

마이크를 쓰는 기능은 `https` 또는 `localhost`에서만 동작한다. `file://`로는 확인할 수 없다.
유튜브 재생도 마찬가지다 — `file://` 에서는 Error 153이 난다.
실제 확인은 https://bh2513.github.io/dictation/ 에서 한다 (main에 올리면 1~2분 뒤 반영).

구현 후 사용자가 직접 확인할 항목은 SPEC 11번 체크리스트에 있다. 기능을 완성했다고
말하기 전에 해당 항목을 실제로 통과하는지 본다.

## 자주 건드리는 값

`app.js` 맨 위에 상수로 모여 있다.

- `LEADS` / `leadAt` — 시작점 여유. SPEC 4-1. 듣기 화면 "앞 여유" 버튼으로 바꾼다. 기본 0.6초
- `TAIL` (0.3) — 끝 여유. 마지막 단어가 잘리지 않게
- `TICK_MS` (40) — 끝 지점 감시 주기
- `STOP_MARGIN` (0.04) — 감시 주기로 인한 정지 지연 보정
