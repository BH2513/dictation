# 장기 계획 — 말하기 연습 기능 (Phase 2)

Phase 1(받아쓰기)이 끝난 뒤 무엇을 왜 만들지 정리한 문서다.
결정 근거와 비용 계산을 남겨 둔다 — 나중에 "그때 왜 이렇게 정했지"를 다시 계산하지 않기 위해서다.

기준 문서는 여전히 `SPEC.md` 다. 여기서 정한 것이 실제로 만들어지면 SPEC 에 옮긴다.

작성: 2026-08-22. 가격은 그때 조사한 값이다 — **1년쯤 지났으면 다시 확인할 것.**

---

## 왜 이 기능을 만드나

운영자가 Langua 라는 앱으로 스피킹 연습을 해 봤고(1년 159달러, 하루 30분), 그중 네 가지가
특히 좋았다고 했다. 그대로 옮겨 오는 것이 목표다.

1. 내가 말한 문장을 **더 자연스럽게 고쳐서 화면에 따로 띄워 주는 것**
2. 그 문장을 **카드로 저장해서 복습**하는 것
3. 이상하게 말하면 **내 문장을 먼저 고치고 나서** 자기 대답을 하고 대화를 잇는 것
4. 연습이 끝나면 **전체 대화를 요약**해서 잘한 것·못한 것·문화적인 부분까지 짚어 주는 것

2번은 이미 이 앱에 있다. 문장카드 시스템(SPEC 7)을 그대로 쓴다.

또 하나, 대화가 하기 싫은 날을 위해:

5. **하루에 문장 다섯 개**를 받아서 혼자 영어로 옮겨 보고, 말해 보고, 정답과 맞춰 보는 연습

---

## 세 단계로 나눈다

| 단계 | 무엇 | 비용 | 상태 |
|---|---|---|---|
| **1** | 하루 다섯 문장 (위 5번) | **0원** | 만들었다. 쓰면서 고치는 중 |
| **2** | 대화 연습 (위 1·2·3·4번) | **연 2.5만 원** | 1단계 판정 기준을 통과하면 |
| ~~3~~ | ~~AI 목소리~~ | — | **안 하기로 했다** (2026-08-23) |

> **두 가지를 뺐고, 그래서 남은 것이 아주 단순해졌다.**
>
> - **받아적기를 AI 로 하는 안(연 8.5만 원)** — 확인해 보니 **서버 없이는 안 된다.**
>   폰 내장 기능이나 브라우저 안에서 도는 것을 쓴다. **0원**
> - **AI 목소리(연 4.8만 원)** — **운영자가 안 하기로 정했다.** 아래 3단계 절 참고. **0원**
>
> 남은 것은 **글을 주고받는 것뿐이다.** 그래서:
> **회사 한 곳, 열쇠 하나, 연 2.5만 원.** 소리를 사고파는 일이 없으니
> 브라우저에서 막힐 일도, 앱이 필요할 일도 없다.

**1단계를 먼저 하는 이유가 있다.** 돈이 안 들고, 제약(서버 없음·비용 0원)을 안 깨고,
이미 만들어 둔 채점기·녹음·문장카드를 그대로 재사용한다. 무엇보다
**돈을 쓰기 전에 "이 방식이 나한테 맞나"를 먼저 확인할 수 있다.**

---

## 1단계 — 하루 다섯 문장 (비용 0원)

### 핵심 아이디어

이 기능은 **실시간이 아니다.** 아침에 문장이 준비돼 있기만 하면 된다.
그러면 미리 만들어 두면 되고, 미리 만드는 것은 **운영자의 기존 구독 한도**로 할 수 있다.
유료 API 가 필요 없다.

### 어떻게 도나

```
매일 새벽 (GitHub 가 알아서)
   → Claude 를 한 번 돌려서 문장 5개 생성
   → data/daily/{프로필}/sets/{번호}.json 으로 저장소에 커밋 (날짜에 묶지 않는다)
   → GitHub Pages 가 배포
   → 아침에 앱을 열면 새 문장 5개가 있다
```

- **비용 0원** — 운영자 구독 한도에서 나간다. 하루 5문장은 한도에 티도 안 난다
- **서버 0원** — 앱이 이미 올라가 있는 GitHub 가 그대로 해 준다
- **제약 안 깨짐** — SPEC 2의 "서버 없음 / 비용 0원" 그대로다
- 운영자가 할 일 없음

### 문장 난이도 — 이게 제일 중요하다

운영자는 듀오링고가 안 맞는 수준이다. **쉬우면 아무 도움이 안 된다.** 그래서 만들 때 못 박는다.

- **12~28 단어** — 넘지 말라는 울타리지 채워야 하는 양이 아니다 (아래 참조)
- **다섯 개의 길이를 서로 다르게** — 둘은 20 단어 이하 (상한만 두면 모델이 거기 붙는다)
- **난이도는 길이가 아니라 내용에서** — 가정 · 조건 · 이유 · 마음이 반쯤 바뀐 상태
- 상황을 매일 무작위로 바꾼다 — 회의에서 반대 의견 내기, 병원에서 증상 설명,
  서비스에 항의하기, 조건 걸고 협상하기, 뉴스 논평, 애매하게 거절하기 …
- **저장소에 이미 있는 영상 자막에서 어휘를 끌어온다** — 받아쓰기로 듣고 있는 어휘와 겹치게
- 최근 며칠에 쓴 상황은 피한다 (반복 방지)

### 채점은 하지 않는다 (2026-08-23 운영자 결정)

처음에는 받아쓰기 채점기를 그대로 붙였는데, 써 보니 **맞힐 수도 없고 맞힐 이유도 없었다.**
옮겨 말하기는 정답이 하나가 아니다. 허용 답안을 여러 개 두어도 마찬가지였다.

그래서 점수와 오답 표시를 뺐다. 지금은 이렇게 돈다.

- 말하기 → 지금 `record.js` 가 쓰는 것 그대로. 음성인식이 되면 받아적은 것을 보여 준다
- `Show answer` → 정답 · 다르게 말하는 법 · 설명을 보고 **스스로 견준다**
- 반복 연습 → **문장카드에 그대로 꽂는다.** 한국어가 있으므로 한→영 모드가 바로 된다
- 진도는 "봤다"만 남긴다. 연속 학습 일수와 학습량 그래프는 그대로 된다

`alts` 는 채점용이 아니라 **다르게 말하는 법을 보여 주는 용도**로 남았다.

### 만든 것을 한 번 더 읽힌다 (2026-08-23)

첫 문장 묶음에서 관용구 오용이 두 개 나갔다 — `hit the spot`(음식에 쓰는 말)을 휴식에,
`call it a day`(일을 끝낼 때 쓰는 말)를 배달 얘기에. 게다가 그 둘을 `note` 에
배울 표현이라고 강조까지 해 둬서, **틀린 영어를 가르치고 있었다.**

그래서 만드는 호출 다음에 **검수 호출을 한 번 더** 붙였다. 지적만 시키지 않고
고친 최종본을 받아서 내보낸다. 구독 한도로 도므로 값은 그대로 0원이다.

기계로 잡을 수 있는 것은 기계가 먼저 잡는다 — 강조한 표현이 문장에 실제로 나오는지,
캐주얼이 정답과 앞부분이 겹치지 않는지. AI 판단에만 기대지 않는다.

### 번역체를 잡는다 (2026-08-23 운영자 리뷰)

운영자가 첫 묶음을 통째로 리뷰했다. 지적이 여럿이었는데 **원인이 하나로 모였다** —
한국어를 먼저 쓰고 영어로 옮기고 있었다.

드러난 자국들:

- 한국어가 애매해서(`그거`, `그런 것`) 영어가 `it`/`that` 투성이가 됐다
- 20~35 단어를 **한 문장**으로 쓰라고 해서 쉼표가 쌓였다. 영어는 짧게 끊어 말한다
- 내가 지시문에 `honestly, kind of, like 같은 말버릇이 들어가면 좋습니다` 라고 써 놓아서,
  뜻과 상관없이 장식으로 박혔다. `이상하게`가 `weirdly` 가 아니라 `honestly` 로 나간 것,
  `like actually nothing` 이 어색했던 것이 전부 여기서 나왔다
- `reorder` 처럼 뜻은 맞지만 그 자리에서 원어민이 안 쓰는 낱말
- `caved on day three` 처럼 두 가지로 읽히는 대목

**왜 casual 쪽이 더 나았는가**도 같은 이유로 설명된다. alts 는 한국어에서 옮긴 것이 아니라
**이미 있는 영어를 말투만 바꿔 쓴 것**이라 한국어에 끌려다닐 일이 없었다. 게다가 더 짧았다.
이게 영어를 먼저 쓰는 쪽이 맞다는 증거다.

### 길이 하한을 없앤다 (2026-08-23 운영자 리뷰 2차)

영어를 먼저 쓰게 고친 뒤로 casual · formal 은 9/10 이 됐는데 **정답만 8/10 에 머물렀다.**
운영자가 짚은 자국은 `that` 이었다 — 무엇을 가리키는지 이상하고 문장 연결이 어색하다.

**모델 탓이 아니다.** 정답과 alts 는 **같은 호출에서 같은 모델이 한꺼번에** 만든다.
모델이 문제라면 alts 도 같이 나빠야 한다. 남은 차이는 **거는 조건**뿐이었다.

만들어 둔 8묶음 40문장을 세어 보니 이렇게 갈렸다.

| | 정답 `text` | `casual` |
|---|---|---|
| 길이 하한 | **20 단어 (강제)** | 없음 |
| 평균 길이 | 25.4 단어 | 20.1 단어 |
| 제일 짧은 것 | 20 단어 | **11 단어** |
| 20 단어 미만 | 0% (규칙이 막음) | **48%** |
| 한 문장으로 끝남 | **0 / 40** (규칙이 막음) | 17 / 40 |
| it · this · that | 문장당 1.1번 | 0.8번 |

**운영자가 9점을 준 문장의 절반은 정답에는 허용되지 않는 길이와 모양이었다.**
위에 이미 "게다가 더 짧았다" 라고 적어 놓고도 그 절반을 안 고쳤던 것이다.

`that` 이 이상해진 경로도 여기서 나온다. 20 단어를 채우면서 2문장 이상으로 나누라니,
두 번째 문장을 붙일 접착제가 필요해지고 그게 앞 문장 전체를 통째로 받는 `that` 이 됐다.
`and that helps nobody` (앞이 가정법인데 뒤가 현재형이라 시제까지 어긋났다),
`and that was that` 이 그렇게 나갔다.

**끊어 쓰기를 없앤 것이 아니다.** 그건 쉼표가 쌓이던 것을 고치려고 넣은 규칙이라
없애면 그게 돌아온다. 순서가 거꾸로였다 — 20 단어를 못 줄이니까 쪼갠 것이지
쪼개서 나빠진 게 아니다. 그래서 **하한을 12로 낮추고** "반드시 2문장 이상" 만 뺐다.
쉼표 검사는 그대로 둔다. 짧아진 몫은 **내용 난이도**를 지시문에 따로 못 박아 메운다.

곁들여: 어떤 모델로 돌릴지 `config.json` 에 못 박고, 실제로 답한 모델을 실행 기록에 남긴다.
지금까지는 정해 놓지 않아 그때그때 기본값으로 돌았고 기록에도 안 남았다.

### 규칙을 더 얹는 것으로는 안 된다 (2026-08-23 운영자 리뷰 3차)

위 리뷰에서 두 가지를 같이 넣었다가 운영자가 둘 다 물렸다. **둘 다 맞는 지적이었다.**

**1. `and that + 동사` 를 기계로 걸러내던 것.** "언어가 그런 게 아니잖아."
`and that was that` 은 제자리에 쓰면 멀쩡한 관용구고, `and that helps nobody` 가 나쁜 것은
**시제가 안 맞아서**지 그 글자 때문이 아니다. 기계는 그 둘을 구별하지 못한다.
게다가 그런 규칙을 두면 모델이 **문제를 피하는 게 아니라 그 글자를 피하게** 된다.
`기계로 잡을 수 있는 것은 기계가 먼저 잡는다` 는 그대로지만, **자연스러움은 그 목록에 없다.**
기계가 보는 것은 셀 수 있는 것뿐이다 — 단어 수, 중복, 쉼표 수, 강조한 표현이 문장에 있는지.

**2. 정답을 casual 과 나란히 놓고 읽게 하던 것.** "그 느낌 아닌 것 같은데."
정답과 casual 은 애초에 **다른 말투**다. "casual 이 더 사람 말 같으면 정답을 고쳐라" 라고 하면
정답이 casual 쪽으로 끌려가고, 셋으로 나눠 둔 의미가 없어진다. 진단도 틀렸다 —
정답이 나빴던 것은 덜 캐주얼해서가 아니라 **늘려 썼기** 때문이다.

12 단어도 그 자체가 답은 아니다. **20 단어 하한이 억지로 늘리게 만들던 압력을 없앤 것**이
핵심이고, 12 는 "너무 짧으면 연습이 안 된다" 는 울타리일 뿐이다.

### 소리내어 읽는 자리를 따로 둔다 (운영자 제안)

> "answer 문장을 만들고 나서 그 문장을 한번 더 모델이 보고 어색한 거 있는지 볼까?"

검수 호출은 이미 있었지만 볼 것이 열 가지쯤 된다 — 관용구, 한국어, alts, note, 단어 수, 줄표.
그 목록 안에서 "사람이 실제로 이렇게 말하나" 는 아홉 가지 중 하나가 되어 묻혔다.
**그것만 묻는 자리**는 다른 물건이다. 그래서 호출을 하나 더 둔다.

    만들기 → 소리내어 읽기 → 검수 → 저장

**읽는 자리에는 영어만 보여 준다.** 이게 이 단계의 전부다.
한국어와 상황을 같이 주면 "번역이 맞느냐" 를 보게 되고, 그러면 어색한 영어도
"한국어를 잘 옮겼으니 괜찮다" 로 넘어간다. 견줄 것을 아예 없애야 남는 질문이 하나가 된다.

- 문법도 뜻도 묻지 않는다. **입으로 읽었을 때 걸리는 데**만 묻는다
- **걸릴 데가 없으면 그대로 둔다.** 고칠 것이 없는데 고치면 더 나빠진다
- 개수나 순서가 어긋나면 안 끼우고 초안을 그대로 쓴다
- 검수는 그 다음이고, 거기서는 **영어를 다시 손대지 않는다** — 나머지를 영어에 맞춘다

### 첫 실행 결과 — 상황이 한국 것이면 영어에 새어 나온다 (2026-08-23 운영자 리뷰 4차)

고친 지시문으로 세 묶음을 뽑아 운영자와 같이 읽었다. 접착제 `that` 은 15문장에 하나도
없었고 구체적인 것이 확 늘었다. 다만 세 가지가 걸렸고 전부 고쳤다.

**1. 회식의 1차·2차가 `first round`/`second round` 로 나갔다.**
영어의 round 는 술을 한 차례 돌리는 것이지 자리를 옮기는 것이 아니다.
**원인은 만드는 순서가 아니라 상황 목록이었다** — `회식 자리에서 빠져나올 구실` 은
영어권에 없는 자리다. 순서를 아무리 지켜도 그 상황을 주면 영어에 새어 나온다.
그래서 (가) 상황 목록에서 한국 전용 항목을 갈아 끼우고, (나) 지시문과 소리내어 읽는 자리에
"한국에만 있는 것을 영어 낱말로 옮기지 마라" 를 넣었다. 가늠하는 법은 하나로 못 박았다 —
**한국을 모르는 사람이 그 영어만 읽어도 통하는가.**
한국어도 이제 **그 영어를 옮긴 것**이라고 못 박았다(직역은 아니다).

**2. 돈 단위가 묶음마다 달랐다.** 한 묶음은 12,000원을 `nine bucks` 로 바꿨고
다른 묶음은 `6,000 won` 을 그대로 뒀다. **달러로 통일한다**(운영자 결정).
한국어의 숫자도 같이 달러로 적는다 — 한쪽만 바꾸면 학습자가 옮길 수가 없다.

**3. 같은 상황이 세 묶음에 두 번씩 나왔다 — 버그였다.**
`recentSituations` 가 **옛 날짜 파일만 읽고 `sets/` 를 아예 안 보고 있었다.**
묶음으로 옮길 때(#40) 따라오지 않은 것이다. 그래서 한 번에 만드는 세 묶음이 서로를 못 봤고,
"팔이 안 올라가서 머리를 못 감았다" 가 두 묶음에 그대로 나왔다.
`sets/` 를 읽게 고치고, 옛 날짜 파일은 **날짜로 실제로 걸러지게** 했다 (앞에서 몇 개를 세기만
하면 그 파일이 영영 남는다). `knownTexts` 는 만들어만 놓고 아무도 안 쓰고 있어서,
최근 영어를 지시문에 넣어 내용이 겹치지 않게 하는 데 쓴다.

이미 저장된 세 문장은 손으로 고쳤다 (묶음 번호는 그대로 — 문장카드가 그걸 가리킨다).

**길이는 안 줄었다.** 평균 25.4 → 27.1 로 오히려 늘었고 한 문장짜리는 15개 중 0개다.
하한을 12로 내렸는데 모델이 그 여유를 안 썼다. 품질이 오른 것은 맞지만
**오른 이유가 길이 압력 제거는 아니다** — 지시문의 "늘리지 마라 / 난이도는 내용에서" 쪽이
일한 것으로 보인다. 소리내어 읽는 단계는 세 묶음 중 두 묶음에서 "걸리는 데 없음" 으로 넘어갔다.
안전망으로 잘 있는 것인지 몸을 사리는 것인지는 더 돌려 봐야 안다.

돌아간 모델은 `claude-opus-5` 였다 (이제 실행 기록에 남는다).

### 두 번째 실행 — 남은 둘을 고친다 (2026-08-23 운영자 리뷰 5차)

고친 뒤 세 묶음을 더 뽑았다. 세 가지는 다 잡혔다 — 한국 개념 0건(회식 자리가
`I said I had an early train and left around nine` 이 됐다), 영어에 `won` 0개 ·
한국어에 "원" 0개로 달러가 맞았고, 세 묶음 안에서 상황이 하나도 안 겹쳤다.
마무리 한 줄도 좋아졌다 (`There was no train.`, `The bag's still by the door.`).

남은 둘을 고친다.

**1. 상황이 돌아올 때 내용까지 같이 돌아왔다.**
운동 핑계가 세 번 나왔는데 셋 다 "가방 싸 놓음 + 비 + 무릎" 이었다.
원인은 **상황 창과 내용 창이 같이 묶여 있던 것**이다(둘 다 3묶음).
상황 창은 넓힐 수 없다 — 상황이 38개뿐이라 넓히면 쓸 것이 없어진다.
그래서 **내용 창만 8묶음으로 넓혔다**(`avoidRecentTexts`). 지시문이 보는 최근 문장이
15개에서 40개로 늘어, 상황이 돌아와도 같은 이야기를 다시 만들지 않는다.

**2. 길이가 세 번 내리 늘었다.** 평균 25.4 → 27.1 → 31.7, 한 문장으로 끝난 답은
45개 중 0개. **하한을 12로 내린 것은 세 번 돌려서 아무 효과가 없다는 것이 확인됐다** —
모델은 하한이 아니라 상한에 붙는다. 그래서

- 상한을 35 → **28** 로 내렸다
- **다섯 개의 길이를 서로 다르게** 하고 그중 둘은 20 단어 이하로 못 박았다
  (`shortCount` / `shortWords`). 길이는 셀 수 있으므로 이건 기계가 본다 —
  자연스러움과 달리 글자 모양을 보는 것이 아니다

상한만 두면 모델이 거기 붙어 다섯 개가 다 비슷해진다. 사람은 늘 같은 길이로 말하지 않는다.

### 리뷰가 뼈대다 — 영어를 두 번 읽힌다 (2026-08-23 운영자 결정)

> "AI 가 문장을 2번 리뷰 하면 더 좋아지는 느낌이었거든. 길이고 뭐고 다른 거보다,
> 2번 리뷰 하는 게 제일 베이스로 하는 게 좋을 거 같애요."

**규칙이 아니라 리뷰가 문장을 좋게 만든다.** 길이·단어 수 같은 것은 울타리로만 쓴다.

실행 기록을 보면 두 리뷰가 하는 일이 갈렸다.

- **검수(리뷰 ②)는 매번 실제로 고쳤다** — `make a thermos`(보온병을 만든다는 말이 된다),
  `attempted`(해 보려다 만 느낌), casual 이 기간을 바꿔 버린 것
- **소리내어 읽기(리뷰 ①)는 세 묶음 중 두 번을 "걸리는 데 없음" 으로 넘겼다.**
  "고칠 것이 없으면 그대로 두세요" 를 세게 못 박아 둔 탓으로 보인다

그래서 **영어를 두 번 읽힌다.**

    만들기 → 읽기 ① → 읽기 ② → 검수 → 저장

같은 지시문으로 한 번 더 부른다. 두 번째 호출은 첫 번째가 이미 봤다는 것을 모르므로
**새 눈**이고, "아까 괜찮다고 했으니 넘어가자" 가 생기지 않는다.
회차마다 무엇을 고쳤는지 실행 기록에 남겨 두 번째가 값을 하는지 볼 수 있게 했다.
값은 그대로 0원이다 (구독 한도).

### 영어를 만드는 자리에 한국어를 두지 않는다 (2026-08-23 운영자 결정)

> "계속 한국어가 껴 있어서 그게 영어 문장에 영향을 줄 거 같은데. 한국어는 신경 쓰지 말고
> 영어만 신경 쓰자 했잖아. 한국어를 계속 영어랑 비교하면서 하면 이상해져.
> 영어가 제일 자연스러운 문장을 만들고, 제일 마지막에 한국어로 번역해서 보여 주면 돼."

**맞는 지적이었고 실제로 그렇게 안 되어 있었다.** 지시문에 "1단계 영어, 3단계 한국어" 라고
적어 두긴 했지만, **한 호출에서 영어 · 한국어 · 말투 · 설명이 한꺼번에 나왔다.**
모델은 한국어도 내놔야 하는 것을 알고 있으니 옮기기 좋은 영어를 쓰게 된다.
검수도 한국어를 나란히 놓고 봤다. **글로 적는 것으로는 안 막힌다 — 갈라 놓아야 한다.**

    영어 만들기 → 읽기 ① → 읽기 ② → 말투 바꾸기 → 한국어 붙이기 → 검수

앞의 **네 단계에는 한국어가 화면에 아예 없다.**

- 만드는 단계는 `{ "texts": [...] }` 만 받는다. 지시문에서 한국어 문장 · alts · note 대목을
  통째로 뺐고, "옮기기 좋은 영어를 쓰려고 하지 마세요" 를 못 박았다
- 말투 바꾸기도 영어만 본다. 한국어를 옆에 두면 말투가 아니라 번역을 보게 된다
- 한국어는 **제일 마지막**이다. 거기서 영어는 고칠 수 없다 — 옮기기만 한다
- 상황 목록은 지시문을 만들 때 파일로 남겨 두었다가 영어와 순서대로 짝짓는다

곁들여 고친 것: `daily.js` 에 `extractJSON` · `unwrap` · `buildSchema` 가 **두 번씩 정의돼
있었다.** 뒤엣것이 이겨서 동작은 같았지만, `buildSchema` 를 고쳐도 뒤 정의에 덮이는 덫이었다.
중복 블록을 지웠다.

### 올리다가 부딪혀 실패한 것 (2026-08-23)

운영자에게 실패 메일이 갔다. **문장 품질 문제가 아니라 저장소에 올리는 자리 문제였다.**

워크플로를 연달아 두 번 돌린 것이 발단이다. 뒤엣것은 `concurrency` 로 줄을 서는데,
**대기를 시작한 시점의 상태를 체크아웃한다.** 그래서 앞 실행이 이미 올린 `s015~s017` 을
또 만들었고, 올릴 때 `add/add` 충돌이 났다.

더 나쁜 것은 재시도였다. 충돌한 rebase 를 되감지 않아서 **남은 네 번이 전부
"Pulling is not possible because you have unmerged files" 로 헛돌았다.**

- 만들기 전에 `git fetch` + `git reset --hard` 로 최신 상태에 맞춘다 (원인 제거)
- 올릴 때 부딪히면 `git rebase --abort` 로 되감고, 최신 상태에서 **새 번호로 다시 붙인다.**
  아직 올라가지 않은 묶음이라 번호를 바꿔도 문장카드가 가리키는 것이 없다
- 그래서 만든 묶음을 `/tmp/saved/` 에 남겨 둔다. 다시 붙일 재료가 있어야 한다
- 끝까지 못 올리면 한국어로 "한 번 더 돌려 주세요" 라고 적는다

### 길이 규칙이 실제로 먹었다 (s015~s017)

| | 평균 | 범위 | 20 단어 이하 |
|---|---|---|---|
| s009~011 | 26.9 | 19~34 | 1 / 15 |
| s012~014 | 31.7 | 28~34 | 0 / 15 |
| **s015~017** | **22.0** | **16~27** | **6 / 15** |

`My haircut went from $25 to $40 in a year and it's the same guy, same chair.`
`Monday I had to go down the stairs sideways, holding the rail with both hands.`
`I like it on the hanger more than on you.`

달러도 지켜졌고(`won` 0건) 한국 개념도 새지 않았다. 다만 **이건 아직 영어와 한국어를
한 호출에서 만들던 판**이다. 갈라 놓은 뒤의 결과는 다음 실행에서 본다.

### 옛 묶음을 그만 내보낸다 (2026-08-24 운영자 결정)

한국어를 떼어낸 첫 결과(s018~s020)를 보고 운영자가 **그 전 것은 다 그만 쓰기로** 했다.

| | 평균 | 범위 | 20 단어 이하 |
|---|---|---|---|
| s001~008 | 25.4 | 20~34 | 4 / 40 |
| s009~011 | 26.9 | 19~34 | 1 / 15 |
| s012~014 | 31.7 | 28~34 | 0 / 15 |
| s015~017 (길이 규칙) | 22.0 | 16~27 | 6 / 15 |
| **s018~020 (한국어 분리)** | **22.3** | **17~27** | **6 / 15** |

`It's midnight and now I know how tunnels get dug.`
`I could have waited. I drove home.`
`Now my boss wants to know if everyone was okay.`
`I'm the one who used to laugh at coupons.`

**지우지 않는다. 목록에서만 뺐다.** 앱은 `sets.json` 에서 안 쓴 것 중 제일 오래된 것을
꺼내 주지만, 문장카드와 지난 공부 날짜는 **파일을 번호로 직접** 읽는다. 그래서
목록에서만 빼면 새 것만 나오면서 담아 둔 것은 그대로 산다. 앱은 고칠 필요가 없었다.

그런데 그대로 두면 **만드는 쪽까지 같이 줄어든다** — 번호를 다시 매겨 파일을 덮어쓰거나,
겹침 방지 창이 40문장에서 15문장으로 쪼그라든다. 그래서 만드는 쪽은 목록이 아니라
**파일 목록(`allSetIds`)** 을 보게 했다. 확인: 목록 3개 / 파일 20개인 상태에서
다음 번호는 `s021`, 겹침 방지 창은 40문장 그대로.

같은 이야기가 돌아오는 것(기타가 s010 · s011 · s020 에 나왔다)은 **그대로 두기로 했다**
(운영자 결정). 내용 창이 8묶음인데 상황은 그보다 빨리 한 바퀴 돈다.

### 자연스러운 영어를 어디서 가져올 수 있나 (조사)

운영자 질문: 지어내지 말고 **실제로 쓰인 영어**를 가져오면 정답으로 공부하는 것이니 낫지 않나.
맞는 방향이고, **이 저장소에 이미 그 갈래가 있다** — Daily 의 `From your videos`.
자막에서 실제로 한 말을 그대로 쓰고 AI 는 한국어만 붙인다. 조사한 것을 남긴다.

| 후보 | 영어의 질 | 걸리는 것 |
|---|---|---|
| **Tatoeba** (CC-BY, 한국어 있음) | 낮음 | 만든 쪽이 품질 경고문을 따로 낸다. 30만 문장이 다나카 말뭉치에서 왔고 학생이 타자한 뒤 검수가 안 됐다. 비원어민 기여분도 섞여 있다. **말이 아니라 예문이다** |
| **OpenSubtitles / OPUS** (en-ko) | 높음 | 지금 쓰는 것과 같은 자막이고 규모만 크다. 재배포가 걸리고 정렬이 어긋난 줄이 많다 |
| **Cornell Movie-Dialogs** (영화 대본 22만) | 높음 | 연구용 라이선스. 맥락 의존은 그대로 |
| **Santa Barbara Corpus** (실제 대화 녹취, 무료) | 진짜 말 | 겹쳐 말하기·다시 시작·`uh` 가 그대로 있다. 연습 문장으로 떼어 쓸 수 없다 |
| **Switchboard · BNC Spoken · COCA** | 진짜 말 | LDC 유료거나 학술 신청이 필요하다 |

**규모로는 안 풀리는 문제가 하나 있다.** 대사는 **원래 맥락에 기대어 말한 것**이다.
첫 실행 로그에서 후보 30개 중 25개가 그래서 떨어졌다 — "that 이 뭘 가리키는지 장면을 봐야 안다",
"Joey 가 누군지 알아야 한다". 지금 등록된 영상 9개에서 조건에 맞는 줄이 427개 나오는데
혼자 서는 것은 그중 15% 남짓이다. 말뭉치를 키우면 **양은 늘지만 이 비율은 그대로다.**

그리고 하나 더. 이 연습은 **상황 목록**(38개, 돌려 쓰고 최근 것은 피한다)이 뼈대다.
말뭉치는 상황이 없는 문장 더미라, 오늘 상황에 맞는 줄을 찾아 오는 일이 다시 필요하다 —
그게 지금 `daily_shows` 가 하는 일이고, 떨어지는 비율이 높은 이유다.

**결론: 말뭉치는 지어낸 갈래를 대체하지 못한다. 대사 갈래를 키우는 쪽이 맞다** —
영상을 더 등록하면 후보가 그만큼 는다. 지어낸 갈래는 소리내어 읽는 자리로 고친다.

### 그런데 진짜 영어를 우리 규칙이 막고 있었다 (2026-08-24)

운영자: "20자 이내로 뭐 이런 규칙이 영어문장을 자연스럽지 못하게 만든다.
영어가 자연스러운 게 1순위다. 길이가 뭐시고 자시고 다 필요없어."

확인할 방법이 있었다 — 저장소에 등록된 영상 11개에 **진짜 원어민 대사 6,641줄**이 있다.
거기에 우리 규칙을 그대로 들이대 봤다. 연습에 쓸 만한 길이(8단어 이상)의 1,458줄 중
**63%가 우리 검사에서 탈락**했다. 대부분 12단어 하한 하나 때문이었다(58%).

| | 진짜 대사 | 우리가 지어낸 문장 |
|---|---|---|
| 8~11 단어 | **58%** | 0% |
| 20 단어 이하 | **89%** | 22% |
| 숫자·요일·시각이 들어감 | 2% | **35%** |
| 말버릇 (like, I mean, kind of) | 28% | 9% |
| 물음표 | 11% | 5% |
| `you` — 상대에게 말 걸기 | **45%** | **14%** |

규칙은 걷어냈다 (SPEC 6-2). **남은 하나는 `you` 다** — 우리 문장은 대화가 아니라
듣는 사람 없는 독백이다. 상황이 "친구한테 ~ 얘기하기" 인데 나오는 것은 수기에 가깝다.
이건 규칙 문제가 아니라 만드는 틀의 문제라 **아직 손대지 않았다.**

그래서 순서를 뒤집었다. **영어 → 소리내어 확인 → 한국어.**
기계 검사도 셋 더했다 — 한 문장 안 쉼표 3개 초과, 영어가 한 문장뿐, 대명사 5개 초과.
넣자마자 기존 다섯 개가 전부 걸렸다.

### 말투 (2026-08-23 운영자 결정)

처음 만든 문장이 회의·협상·항의 같은 격식체라 **"문장이 너무 별로"** 라는 지적을 받았다.
난이도와 길이는 그대로 두고 **일상 대화체로 바꿨다** — 친구·가족에게 하는 말,
축약형을 쓰고, 소리내어 읽었을 때 어색하지 않게. 상황 목록도 통째로 갈았다.

캐주얼을 어떻게 잡을지도 정했다. **관용구를 넣는 것이 아니라 더 짧고 축약된 구어**다
(운영자 선택). 관용구는 뜻이 정확히 맞을 때만 쓴다 — 어설프게 쓰면 틀린 영어를 가르치게 된다.
포멀은 **뜻을 바꾸지 않고 말투만 올린다.**

### 아직 못 하는 것 (솔직하게)

- **자동 난이도 조절.** "너무 쉬우면 다음 날 올린다"는 내 채점 결과를 봐야 하는데,
  그 기록은 폰의 IndexedDB 에만 있고 저장소로 올라오지 않는다.
  지금은 난이도를 `data/daily/config.json` 에 고정해 두고, 조절이 필요하면 사람이 바꾼다.
  자동화하려면 기록을 저장소로 올리는 길이 먼저 필요하다 — 3단계 이후 과제
- **정답 판정의 미묘한 경우.** 허용 답안에 없는데 사실 맞는 표현일 때가 있다.
  2단계(대화)가 붙으면 그때 AI 판정을 얹는다

### 알려진 걸림돌

정해진 시각에 자동으로 도는 기능에서 권한 문제가 보고된 적이 있다
([issue #814](https://github.com/anthropics/claude-code-action/issues/814)).
그래서 `claude-code-action` 을 쓰지 않고 **CLI 를 직접 돌린다** — 그쪽 권한 절차를 아예 안 탄다.

그리고 **조용히 멈추면 안 된다.** 며칠 문장이 안 오면 앱 화면에 알린다. (CLAUDE.md 원칙)

---

### 드라마 대사 갈래를 더한다 (2026-08-23 운영자 결정)

번역체를 잡고 나서도 운영자가 물었다 — **미드 대사를 그대로 쓰면 어색함이 아예 없지 않겠나.**
맞는 말이다. 실제 작가가 쓰고 배우가 말한 것은 어색할 수가 없다.

문제는 자막을 어디서 구하느냐였다. 이 작업 환경에서는 자막 사이트가 전부 막혀 있고
(OpenSubtitles, IMSDb, archive.org, GitHub 검색), AI 가 "기억해서" 대사를 적으면 지어낸다.
그리고 **자막 파일을 통째로 공개 저장소에 넣는 것은 재배포**다.

그래서 이렇게 정했다.

- 운영자가 PC 에 자막 파일을 둔다. `tools/add_subs.py` 가 거기서 **300줄쯤만 뽑아** 올린다.
  원본은 PC 에 남는다 — 인용 정도다
- 두 달에 한 번쯤만 돌리면 된다. 매일 다섯 줄 뽑는 것은 저절로 돈다
- 고르기는 **규칙 → AI** 두 단계다. 규칙은 길이밖에 못 보므로 뜻은 AI 가 본다 (운영자 제안)

Daily 화면은 탭 두 개가 되었다 — `Made for you` 와 `From your videos`.

## 2단계 — 대화 연습

### 왜 챗으로 하면 안 되나 — 이 단계의 존재 이유

운영자가 겪은 문제: 챗에서 "내 문장 먼저 고치고 대답해"라고 시켜도
**한 번만 하고 그 뒤로는 안 한다.** 지적하면 사과하고 또 안 한다.

원인은 모델이 멍청해서가 아니다. **지시가 놓인 자리가 잘못돼서다.**
챗에서 그 지시는 "대화 속의 한 마디"라, 10턴만 지나면 메시지 20개 중 하나가 되고
최근 대화에 묻힌다. 사과도 그 턴에만 효과가 있다.

API 로 만들면 세 겹으로 막을 수 있다.

**1. 지시를 대화 밖으로 뺀다.** 매 요청마다 맨 앞에 자동으로 다시 붙는 "역할 설명" 칸이
따로 있다. 대화가 100턴이 돼도 매번 새로 읽는다. 묻힐 자리가 없다.

**2. 답의 모양을 강제한다 (structured output). ← 결정적**

답을 자유롭게 쓰게 두지 않고 칸을 미리 정한다.

```
내가 한 말   : I want to going to the meeting tomorrow.
고친 문장    : I want to go to the meeting tomorrow.
더 자연스럽게 : I'd like to sit in on tomorrow's meeting.
왜          : want to 뒤에는 동사원형. "sit in on"은 참관한다는 뉘앙스
AI 대답     : Sure — which meeting are you thinking of?
```

**"고친 문장" 칸을 비우면 답 자체가 형식에 안 맞아서 거부되고 다시 만든다.**
까먹는 것이 구조적으로 불가능해진다. 챗으로는 아예 할 수 없는 것이다.

**3. 화면이 한 번 더 막는다.** 교정 칸은 별도 박스로 그린다. 안 채우면 빈 박스가 보인다.

> **한계도 적어 둔다.** 보장되는 것은 "칸이 반드시 채워진다"이지
> "교정이 항상 훌륭하다"가 아니다. 내 문장이 이미 멀쩡하면 "고칠 것 없음"이 들어간다.
> 하지만 "한 번 하고 안 함"은 사라진다.

**따라서 이 문제는 모델 성능 문제가 아니다.** 제일 싼 모델로도 해결된다.

> **다만 "설정 한 줄로 회사를 갈아 끼운다"는 아니다.** 답의 모양을 강제하는 방식이
> 회사마다 다르다 — Anthropic 은 도구 정의로, Google 은 응답 스키마로, OpenAI 는
> 또 다른 이름으로 부른다. 회사를 바꾸면 **그 부분을 회사별로 따로 써야 한다.**
> 갈아 끼우기는 "회사별 어댑터 하나씩"으로 잡아야지 설정값 하나로 잡으면 안 된다.

---

### 무엇을 만들 것인가

#### 화면과 흐름

홈 화면의 `Talk` 칸이 들어갈 자리다 — 지금은 `Soon` 딱지를 달고 비어 있다 (SPEC 5).

```
홈 → Talk → 주제 고르기 → 대화 → 끝내기 → 대화 요약
```

**주제 고르기.** 오늘 무슨 얘기를 할지 고른다. 세 갈래.
- `Free talk` — 아무 얘기나
- `From today's sentences` — 오늘 Daily 에서 한 문장의 상황을 이어서
- `From a show` — 등록한 영상의 장면 상황으로 (Shows 갈래와 이어진다)

**대화 화면.** 한 턴이 이렇게 생겼다.

```
[내가 말한 것]  (음성인식이 받아적은 것, 고칠 수 있게 입력칸)
─────────────────────────────
[고친 문장]     ← 별도 박스. 안 채워지면 빈 박스가 보인다
[더 자연스럽게]
[왜]            ← 배울 표현은 ** 로 강조 (Daily 와 같은 규칙)
─────────────────────────────
[AI 대답]       ← 폰 목소리로 읽어 준다
```

- 아래에 `Say` (녹음) / `Send` (타이핑) / `Save card` / `End`
- **`Save card` 는 "고친 문장"을 담는다** — 내가 한 말이 아니라 고쳐진 쪽이다.
  담긴 카드는 기존 문장카드 네 모드에 들어간다.
  `videoId` 는 `talk-{대화번호}`, 번호는 바뀌지 않는다 (Daily 묶음과 같은 규칙)
- **다만 "그대로"는 아니다 — 읽는 길을 새로 내야 한다.** 지금 카드 화면은 문장을
  저장소의 `data/` 파일에서 찾아온다(`loadCardSentences`). 그런데 대화는 저장소에
  올리지 않기로 했으니 거기 없다. **`talk-` 로 시작하면 기기 안(IndexedDB)에서 찾는
  갈래를 따로 둔다.** 이걸 빼먹으면 카드는 담기는데 열면 빈 화면이 나온다
- 소리가 필요한 모드(듣고 말하기)는 영상이 없으므로 **폰 목소리로 읽어 준다** —
  Daily 문장이 이미 그렇게 하고 있다. 빈칸 채우기는 채점을 안 하므로 가릴 낱말이
  없다. **대화 카드는 그 모드를 빼거나, 교정에서 바뀐 낱말을 가린다**
- 대화는 **`End` 를 눌러야 끝난다.** 자동으로 안 끝낸다 — 하다 말면 요약도 반쪽이다

**조용히 실패하지 않는다 (SPEC 9).** 이 저장소 원칙을 여기에도 잇는다.

| 언제 | 화면에 뭐라고 |
|---|---|
| 인터넷이 끊김 | "인터넷이 끊겼습니다. 이어서 하려면 다시 눌러 주세요." 지금까지 대화는 **안 지운다** |
| 열쇠가 틀림 | "열쇠가 맞지 않습니다. 설정에서 다시 넣어 주세요." + 설정으로 가는 단추 |
| 한도 초과 | "오늘 쓸 수 있는 양을 다 썼습니다." (회사가 내려주는 값을 그대로 보여 준다) |
| 답이 안 옴(30초) | "답이 오지 않습니다. 다시 보낼까요?" + 다시 보내기 단추 |
| 답의 모양이 틀림 | 조용히 한 번 다시 요청한다. 두 번 틀리면 그 턴만 건너뛰고 화면에 남긴다 |

**대화 기록은 어디에.** 기기 안(IndexedDB `talks` 저장소)에만 둔다.
저장소에 올리지 않는다 — 개인 대화다.

**백업(SPEC 10)에는 요약만 넣고 대화 원문은 넣지 않는다 (2026-08-23).**
백업은 기기를 바꿀 때 기록을 옮기는 파일이고, **사람이 손으로 주고받는 물건**이라
열쇠를 뺀 것과 같은 이유가 걸린다. 그런데 다시 읽을 값어치가 있는 것은 요약 쪽이고,
원문은 부피만 크고 제일 민감하다. **요약은 옮겨지고, 대화 원문은 기기를 바꾸면 사라진다.**

#### 대화 요약 (운영자가 꼽은 네 가지 중 4번)

`End` 를 누르면 **그 대화 전체를 한 번 더 보내서** 요약을 받는다.
대화 한 판에 한 번뿐이라 좋은 모델을 써도 부담이 없다.

> **"하루 요약"이 아니라 "대화 요약"이다 (2026-08-23 운영자 결정).**
> 하루에 두 번 하면 요약도 두 개 나온다. 하루치를 합치지 않는다 —
> 대화마다 흐름이 달라서 합치면 뭉개진다.

칸을 정해 둔다.

| 칸 | 무엇 |
|---|---|
| `summary` | 오늘 무슨 얘기를 했는지 두세 줄 |
| `did_well` | 잘한 것 2~3개. **구체적으로** — "좋았어요"가 아니라 어느 문장의 무엇이 |
| `to_fix` | 고칠 것 2~3개. **되풀이된 것 위주** — 한 번 틀린 것 말고 |
| `culture` | 문화적으로 어긋난 대목. 문법은 맞는데 그 자리에서 안 쓰는 말 |
| `words` | 오늘 배운 표현 3~5개. Daily 의 `note` 와 같은 모양 (`**`로 강조) |

- **어디서 보나:** 대화가 끝나면 바로 뜨고, 리포트 화면에 날짜별로 쌓인다
- **어디에 남나:** 기기 안. Daily 의 `plan` 과 같은 방식으로 **공부한 날**에 매단다
- `words` 는 **한 번에 문장카드로 담는 단추**를 둔다

---

#### 매 요청에 무엇을 실어 보내나 — 지시문

> 이 절은 앞선 판(2026-08-22)에 "덤"이라는 이름으로 있었는데 다시 쓰면서 빠졌다.
> **되살린다.** 그냥 덤이 아니라 **캐싱을 걸리게 하는 장치**이기도 하다 (비용 절 참고).

챗에서는 매번 내 수준과 약점을 다시 설명할 수가 없다. **API 면 매 요청마다 자동으로 들어간다.**
그리고 이 저장소에는 **그 재료가 이미 쌓여 있다.**

| 실어 보낼 것 | 어디서 가져오나 |
|---|---|
| 자주 틀리는 낱말 | 오답 기록 (SPEC 4-3). 리포트 화면이 이미 쓰고 있는 것 |
| 요즘 되풀이되는 실수 갈래 | 최근 대화 요약의 `to_fix` 를 모은 것 |
| 최근 배운 표현 | 최근 대화 요약의 `words` + 최근에 담은 문장카드 |
| 요즘 듣고 있는 어휘 | 등록한 영상의 자막에서 (하루 문장이 이미 이렇게 한다) |
| 말투·난이도 기준 | `data/daily/config.json` 의 값을 같이 쓴다 |

- **대화 안에 넣지 않는다.** 대화 밖의 "역할 설명" 칸에 넣는다 — 그래야 100턴이 지나도 안 묻힌다
- **너무 자주 바꾸지 않는다.** 이 부분이 바뀌면 캐싱이 처음부터 다시 걸린다.
  **하루에 한 번만 새로 만들고 그날은 그대로 쓴다**
- 이걸 채우면 지시문이 4,096 토큰을 넘어 **첫 턴부터 캐싱이 걸린다.**
  즉 **크게 만드는 쪽이 오히려 싸다** — 산식은 비용 절에 있다

---

### 열쇠(API key)를 어떻게 다루나

운영자는 코드를 모른다. "폰마다 한 번씩 넣어야 한다"만으로는 실행이 안 된다.

**어디에 넣나.** 홈 → `Settings` → `AI key`. 대화 화면에서 열쇠가 없으면
그 화면으로 보낸다.

**어디에 저장하나.** IndexedDB `settings` 저장소. **프로필별이 아니라 기기별로 하나.**
같은 기기를 가족이 나눠 쓰는 것이 이 앱의 전제인데(SPEC 3),
열쇠를 프로필마다 따로 두면 사람마다 발급받아야 해서 감당이 안 된다.
**기기에 하나를 넣고 온 가족이 같이 쓴다.** 대신 화면에 그 사실을 적는다 —
"이 기기의 모든 프로필이 같은 열쇠를 씁니다."

**넣을 때 확인한다.** 저장하기 전에 짧은 요청을 한 번 보내 본다.
안 되면 저장하지 않고 한국어로 이유를 말한다 (열쇠가 틀림 / 인터넷 / 한도).

**절대 안 하는 것.** 저장소에 올리지 않는다. 백업(SPEC 10)에도 **넣지 않는다** —
백업 파일은 사람이 주고받는 물건이다.

**발급 방법 안내.** `tools/DAILY_SETUP.md` 와 같은 모양으로 `tools/TALK_SETUP.md` 를 쓴다.
화면 보고 따라가는 수준으로. 열쇠가 만료됐을 때 무엇을 다시 하는지도 적는다.

---

### 브라우저에서 직접 부를 수 있나 — 확인한 값 (2026-08-23)

"서버 없음"을 지키려면 **폰이 회사에 직접 말을 걸 수 있어야** 한다.
이게 이 단계 구조의 유일한 근거라 먼저 확인했다.

| 쓰려는 것 | 브라우저에서 직접 | 근거 |
|---|---|---|
| **Anthropic (대화·요약)** | **된다** | `anthropic-dangerous-direct-browser-access: true` 헤더를 붙이면 CORS 가 열린다. 열쇠를 기기에 두는 방식(BYOK)을 위해 만든 기능이다 |
| **Deepgram (받아적기)** | **안 된다** | REST 는 CORS 로 막혀 있고, 실시간 연결은 **30초짜리 임시 표**가 필요한데 그 표를 만들려면 진짜 열쇠를 든 서버가 있어야 한다 |
| **Google Gemini (대화·요약·목소리)** | **된다** | 공식 JS SDK 가 **브라우저에서 그대로 돈다** ("브라우저에서도 초기화 코드가 같다"). 구글은 "열쇠를 클라이언트에 노출하지 말라"고 경고하지만, 그건 **개발자가 자기 열쇠 하나를 모든 사용자에게 뿌리는 경우**를 말한다. 우리는 **각자 자기 열쇠를 자기 기기에 넣는다**(BYOK) — 겨냥한 상황이 다르다. 다만 **실시간(Live) 쪽은** Deepgram 과 같은 임시표 방식이라 **안 된다** |
| **OpenAI (대화·요약)** | **된다** | 공식 SDK 의 `dangerouslyAllowBrowser` 옵션. **"사용자가 자기 열쇠를 직접 넣는 경우"를 용도로 명시**하고 있다 — 우리 방식과 정확히 맞는다 |
| **브라우저 안에서 도는 받아적기** | **된다** | Whisper 를 브라우저 안에서(WebAssembly) 돌릴 수 있다. **회사에 말을 걸지 않으므로 CORS 자체가 없다.** 아래 참고 |

> **일반화해서 적어 둔다: 받아적기 REST API 는 대부분 브라우저를 안 열어 준다.**
> Deepgram 만의 사정이 아니다. 회사들이 **일부러** 안 연다 — 열쇠가 새는 것을 막으려는 것이다.
> **Anthropic 과 Google 이 예외**고, 그건 둘 다 "열쇠를 기기에 두는 방식"을 염두에 뒀기 때문이다.
> 회사를 새로 볼 때마다 이 칸부터 확인한다.

> **이게 B안을 깬다.** 앞서 "받아적기를 AI 로(연 8.5만 원)"를 권했는데,
> **그건 서버 없이는 안 된다.** 서버를 두면 이 앱의 제일 큰 전제가 무너진다.
>
> 그래서 2단계는 **A안(대화만 AI, 받아적기·목소리는 폰 내장)** 으로 시작한다.
> 대화는 확인된 방식으로 되고, 받아적기는 지금 `record.js` 가 쓰는 폰 기능이 이미 있다.
> 폰 받아적기가 정말 못 쓸 수준이면 그때 다시 생각한다 —
> 그때는 **"작은 서버 하나" · "앱으로 포장" · "정확도 포기"** 셋 중에 고르는 문제가 된다.
> **앱으로 내면 이 막힘이 통째로 없어진다** (CORS 는 브라우저에만 있는 규칙이라서다).
> 그 갈래는 3단계 절의 "브라우저 말고 다른 길" 에 적어 뒀다.

---

### 비용 (2026-08-23 조사)

> **단위는 100만 **토큰**당이다. 글자가 아니다.** 영어는 대략 글자 4개가 토큰 1개다.
> 1년 뒤 다시 계산하려면 이 단위를 맞춰야 한다.

| 역할 | 모델 | 입력 / 출력 (100만 토큰당) |
|---|---|---|
| 대화 | Claude Haiku 4.5 | $1 / $5 |
| 대화 요약 | 아래 표에서 고른다 | — |

#### 캐싱이 뭔가 (2026-08-23 운영자 질문)

**AI 는 기억을 못 한다.** 그래서 말을 걸 때마다 **지금까지의 대화 전체를 처음부터 다시 보낸다.**

30번째 턴에서 "그건 별로던데" 라고 하면 AI 는 '그건' 이 뭔지 모른다. 앞 29턴을 같이
보내야 안다. 그래서 **1번째 턴은 조금, 45번째 턴은 앞 44턴 전부**를 보낸다.
값은 보낸 양만큼 나가니 **뒤로 갈수록 비싸진다.**
45턴짜리 대화 한 판이면 다 합쳐 **소설책 두 권 분량**을 보내게 된다.
실제로 새로 한 말은 얼마 안 되는데도 그렇다.

그런데 **그 앞부분은 매번 똑같다.** 그래서 AI 회사가 "그건 저희가 갖고 있을게요" 를 해 준다.
맡겨 둔 부분은 **값이 1/10** 이다. 책 두 권이 3분의 1권이 된다.
**이것이 "캐싱이 걸렸다" 는 말이다.**

**캐싱.** 정확한 배수는 **다시 읽을 때 0.1배**, 처음 넣어 둘 때 1.25배(5분 유지)다.
대화 턴이 5분보다 촘촘하면 계속 살아 있다 —
30분에 45턴이면 40초에 한 번꼴이라 이 조건은 걱정 없다.

> **안 걸려도 오류가 나지 않는다. 이게 제일 무섭다.**
> 화면에서는 멀쩡하게 잘 돌아가고 **청구서만 몇 배로 나온다.**
> 판정표에 "그 주 청구서가 산식의 2배 이내인가" 를 넣은 이유가 이것이다.

> **캐싱에는 최소 크기가 있다. 이걸 놓치면 계산이 통째로 틀린다 (2026-08-23).**
> 앞부분이 일정 크기를 넘어야만 캐싱이 걸린다. 못 넘으면 **오류도 없이 그냥 안 걸린다.**
> 기준은 모델마다 다르고 **Claude Haiku 4.5 는 4,096 토큰 — 영어로 3천 단어쯤**이다.
> 우리 지시문은 800 토큰이라 그 5분의 1밖에 안 된다. 대화가 어느 정도 쌓여야 기준을 넘으므로
> **20번째 턴쯤 가서야 캐싱이 시작되고, 앞 19턴은 제값을 다 낸다.**
>
> **그래서 지시문을 크게 만드는 쪽이 오히려 싸진다.** 위 "매 요청에 무엇을 실어 보내나"
> 절에서 내 약점과 최근 배운 표현을 넣기로 했는데, 그걸 넣으면 지시문이 4,096 을 넘어
> **첫 턴부터 캐싱이 걸린다.** 그 기능은 덤이 아니라 **비용을 맞추는 장치**다.
> 모델을 갈아 끼울 때는 **그 모델의 최소 크기를 먼저 보고 지시문 길이를 맞춘다.**

#### Gemini 캐싱을 확인했다 — 그리고 값이 뒤집혔다 (2026-08-23)

앞 판에서 "Gemini 캐싱은 확인 못 했다" 고 적어 둔 것을 확인했다. **조건만 보면 Gemini 가 낫다.**

| | Claude | Gemini |
|---|---|---|
| 다시 읽을 때 | 0.1배 | **0.1배** (같다) |
| 넣어 둘 때 | 1.25배 | **자동 캐싱은 0배** (보관료 없음) |
| 최소 크기 | Haiku 4.5 = 4,096 토큰 | 3.x = 4,096 토큰 (같다) |

**그런데 우리가 쓰려는 방식에서는 안 걸릴 공산이 크다. ← 이게 확인의 결론이다**

이 단계의 존재 이유는 **답의 모양을 강제하는 것**이다. 그런데 그것과 캐싱이 부딪힌다.

- **직접 만드는 캐시:** 구글 API 가 **명확히 막는다.** 오류 문구까지 있다 —
  *"cached content 를 쓸 때 tools·system instruction 을 요청에 같이 넣으면 안 된다."*
  넣으려면 **캐시 안에** 넣어야 한다. 그러면 **매 턴 늘어나는 대화 부분은 캐시에 못 넣는다** —
  캐시는 고정된 앞부분이라서다. **절감이 지시문 부분에만 걸린다**
- **자동 캐시:** 도구를 정의하면 안 걸린다는 사용자 보고가 있다 (`vercel/ai` #11513).
  **구글이 확인해 준 것은 아니고 그 이슈는 "계획 없음" 으로 닫혔다.** 확정으로 적지 않는다
- **Anthropic 은 반대다.** 도구가 **캐싱 앞부분에 정식으로 포함된다** —
  문서에 순서(tools → system → messages)까지 적혀 있다. 같이 쓰라고 만든 조합이다

**그래서 값이 이렇게 된다** (전부 켰을 때, 1인 하루 30분).

| | 1년 |
|---|---|
| Gemini — 캐싱이 다 걸린다면 | 11.8만 원 |
| **Gemini — 지시문만 캐시될 때 (현실적으로 기대할 수 있는 것)** | **17.4만 원** |
| **Claude (Haiku 4.5 + Sonnet 5)** | **14.6만 원** |

> **단가는 Gemini 가 싼데 총액은 Claude 가 싸다.** 캐싱이 우리 구조에서 걸리느냐가
> 단가 차이보다 크기 때문이다.
>
> **이 문서가 걱정하던 편향 문제도 여기서 정리된다.** "Claude 가 쓴 문서라 Claude 를 민다"
> 는 의심이 있었는데, 이번 것은 취향이 아니라 **확인된 값**이다. 그래도 결론은 그대로다 —
> **갈아 끼울 수 있게 만들고 일주일씩 써 본다.** 다만 Gemini 로 바꿀 때는
> **청구서를 반드시 확인한다.** 캐싱은 안 걸려도 오류가 안 난다.

#### 산식 (검산할 수 있게)

전제: 하루 30분, 주고받기 45번. 내가 한 번에 30단어(≈40토큰),
AI 가 60단어(≈95토큰) + 교정 칸까지 해서 ≈140토큰.

```
캐싱 없이 (입력)
  턴 i 의 입력 = 지시문 + 앞 대화 (i-1)×180 + 내 말 40

가) 지시문이 800 토큰일 때  ← 캐싱이 20번째 턴부터 걸린다
  1~19턴  제값                                    ≈ 46,700 토큰
  20~45턴 넣어 두기 8,700×1.25 + 다시 읽기 160,500×0.1 ≈ 27,000 토큰
  합계                                            ≈ 74,000 토큰

나) 지시문을 4,200 토큰으로 채울 때  ← 첫 턴부터 캐싱이 걸린다
  넣어 두기 = 마지막 프리픽스 12,100 × 1.25       ≈ 15,100 토큰
  다시 읽기 = (369,000 − 12,100) × 0.1            ≈ 35,700 토큰
  합계                                            ≈ 51,000 토큰   ← 가) 보다 싸다

대화 하루치 (나 기준)
  입력  51,000 × $1/1M           ≈ $0.051
  출력  45 × 140 = 6,300 × $5/1M ≈ $0.032
                                   합 ≈ $0.083

대화 요약 (Sonnet 5, 대화 한 판에 한 번)
  입력  대화 전체 ≈ 9,000 × $3/1M  ≈ $0.027
  출력  ≈ 1,300 × $15/1M           ≈ $0.020
                                   합 ≈ $0.047

하루 합계 ≈ $0.13  →  한 달 ≈ $3.9  →  1년 ≈ $47  ≈ 6.6만 원
     (지시문을 안 채우면 ≈ $0.15 → 1년 ≈ $56 ≈ 7.8만 원)
```

#### 요약에 어느 모델을 쓸까 (2026-08-23 조사)

요약은 **대화 한 판에 한 번**이라 모델을 바꿔도 폭이 크지 않다. 깊이가 값어치를 하는 자리다.

| 모델 | 입력 / 출력 (100만 토큰당) | 하루 | **1인 1년** |
|---|---|---|---|
| Gemini 3 Flash Preview | $0.25 / $1.50 | $0.004 | 약 0.2만 원 |
| Gemini 3.7 Flash (도입가) | $0.75 / $3.75 | $0.012 | 약 0.6만 원 |
| Gemini 3.7 Flash (2027-01-01 정가) | $1.50 / $7.50 | $0.023 | 약 1.2만 원 |
| Claude Haiku 4.5 | $1 / $5 | $0.016 | 약 0.8만 원 |
| **Claude Sonnet 5** ← 지금 추천 | $3 / $15 | $0.047 | **약 2.4만 원** |
| Claude Opus 5 | $5 / $25 | $0.078 | 약 4.0만 원 |

- **제일 비싼 것과 제일 싼 것의 차이가 1년에 3.8만 원이다.** 대화 쪽(연 5만 원 안팎)에
  비하면 작다. 여기서 아끼려 애쓸 자리가 아니다
- **Gemini 3.7 Flash 는 2027년 1월 1일에 값이 2배로 오른다고 이미 예고돼 있다.**
  도입가로 계산해 두면 새해에 틀어진다. 위 표에 두 값을 다 적어 둔 이유다
- **Preview 딱지가 붙은 값은 언제든 바뀌거나 없어진다.** 그걸 기준으로 정하지 않는다
- 실제로 어느 것이 나은지는 **써 봐야 안다.** 요약 모델도 설정에서 고를 수 있게 만든다

> **왜 헤드리스(구독)로 공짜로 못 하나 (2026-08-23 운영자 질문).**
> 하루 문장처럼 GitHub 이 새벽에 도는 방식이면 구독 한도로 돌아 0원이다. 그런데
> **그러려면 대화가 저장소에 올라와 있어야 하고, 이 저장소는 공개다.** 방금 위에서
> "개인 대화라 안 올린다"고 정한 것과 정면으로 부딪힌다. (비공개 저장소로 옮기면
> 지금의 무료 배포 조건이 바뀐다 — **확인 필요.**)
> 그리고 타이밍이 안 맞는다. 원하는 것은 "대화 끝나면 바로 뜨는 요약"인데
> 헤드리스는 몇 분 걸리고 다음에 앱을 열 때나 보인다. **요약은 API 로 한다.**

#### 이 값이 무엇을 포함하나 (2026-08-23 운영자 질문)

운영자가 물었다 — **"저 1년 비용이 AI 를 쓰는 모든 것을 다 합친 값이냐."**
아니다. **대화 연습 하나에만 붙는 값이다.** 헷갈리기 쉬우니 적어 둔다.

| 기능 | 값 | 왜 |
|---|---|---|
| 받아쓰기 (Phase 1) | **0원** | 유튜브 자막 + 브라우저. AI 를 안 쓴다 |
| 하루 다섯 문장 | **0원** | GitHub Actions + 운영자 구독 한도 |
| 드라마 대사 갈래 | **0원** | 위와 같은 방식 |
| 문장카드 · 오답 리포트 · 통계 · 백업 | **0원** | 전부 기기 안에서 |
| 영상 등록 | **0원** | PC 로컬 스크립트 |
| 내 말 받아적기 | **0원** | 폰 내장 (AI 로 하려면 서버가 필요해서 뺐다) |
| AI 대답 읽어 주기 | **0원** | 폰 내장 목소리 |
| **대화 연습 (2단계)** | **연 6.6만 원** | ← **여기만 돈이 나간다. 대화와 요약을 합친 값이다** |
| ~~AI 목소리~~ | **0원** | **안 하기로 했다** — 폰 내장을 쓴다 (2026-08-23) |

#### 실제로는 얼마나 나오나

위 6.6만 원은 **한 사람이 하루 30분, 365일 하루도 안 빠졌을 때**다. 실제로는 그렇게 안 된다.

| 얼마나 하나 | 1인 1년 | 가족 셋이 다 |
|---|---|---|
| 매일 30분 (산식 기준) | 6.6만 원 | 20만 원 |
| **주 3회** | **2.8만 원** | 8.5만 원 |
| 주 1회 | 약 1만 원 | 2.8만 원 |
| 한 달 쉬면 그 달 | **0원** | **0원** |

**쓴 만큼만 낸다** — 이게 구독과 다른 점이다.
(Langua 는 1인 연 159달러 ≈ 22만 원이 **안 해도 나간다.** 셋이면 66만 원.)

> 값은 바뀐다. Sonnet 5 는 도입가로 더 싸게 파는 기간이 있었고,
> Gemini 3.7 Flash 는 2027년 1월에 2배가 된다.
> **1년 뒤에는 위 산식에 그때 값을 다시 넣어 계산한다.**

---

### 무엇을 보고 넘어가나 — 주마다 본다 (2026-08-23 운영자 결정)

운영자가 **일주일 단위로 리뷰**한다. 그래서 판정도 주 단위로 쌓는다.
**한 주가 좋았다고 넘어가지 않는다** — 새 기능은 첫 주가 제일 재밌다.
Langua 를 1년 결제해 놓고 안 쓰게 되는 것이 제일 흔한 실패고, 한 주짜리 관찰로는 그걸 못 거른다.

#### 매주 보는 것 — 1단계 (하루 다섯 문장)

| 보는 것 | 통과 |
|---|---|
| 그 주에 Daily 를 연 날 | **4일 이상** |
| 건너뛴 문장 비율 | **3할 미만** (높으면 문장 품질 문제가 먼저다) |
| 그 주에 담은 문장카드 | **3장 이상** (담을 만한 것이 나온다는 뜻) |
| 영상 대사 갈래에서 맥락 없이는 못 쓸 대사 | **5개 중 1개 이하** |
| 운영자가 "챗으로 하는 것보다 낫다"고 느끼나 | **예** ← 제일 중요하다 |

**넘어가는 조건:** 위가 **4주 연속** 통과.
**멈추는 조건: 4주 안에 두 주 이상 절반을 못 채우면 2단계를 만들지 않는다.**
돈을 쓰기 전에 맞는지 확인한다는 것이 이 문서의 논리인데,
그걸 실제로 강제하는 문장은 이것뿐이다. 넘어가는 조건만 있으면 그냥 넘어가게 된다.

**앞의 셋은 앱과 저장소에서 셀 수 있다. 리포트 화면의 주간 판정표가 그 숫자를 띄운다** (만들었다).
넷째(대사가 맥락 없이 쓸 만한가)는 **셀 수 없다** — 뜻을 사람이 봐야 한다.
다섯째와 함께 화면에 물음만 적어 둔다. 자세한 것은 SPEC 4-3-b.

#### 매주 보는 것 — 2단계 (대화 연습)

| 보는 것 | 통과 |
|---|---|
| 대화가 이어진 턴 수 | **10턴 이상** (금방 끊기면 재미가 없다는 뜻) |
| 말하고 답이 올 때까지 | **3초 안** — 모델을 고른 근거가 이것인데 판정에 없었다 |
| 교정이 헛나간 횟수 | **하루 한 번 이하** (아래 설명) |
| 대화 요약을 **다음 날 다시 열어 보나** | **예** — 한 번 읽는 것과 다시 찾는 것은 다르다 |
| 그 주 청구서가 산식 범위 안인가 | **2배 이내** — 넘으면 계산이 틀린 것이고 결정을 다시 해야 한다 |
| 폰 내장 목소리가 **알아듣기 어려워** 연습이 안 되나 | **아니오** — 예가 나오면 그때만 AI 목소리를 다시 본다 |

> **"교정 칸이 채워지나"는 판정 기준이 아니라 버그 조건이다.** 그건 안 되면
> 만들다 만 것이지 성공·실패를 가르는 값이 아니다. 게다가 내 문장이 이미 멀쩡하면
> **"고칠 것 없음"이 들어간다** — 그것도 채워진 것이다.
> 판정해야 할 것은 **교정이 쓸모 있나**다. 운영자가 "이건 안 고쳐도 되는데 고쳤다"
> 또는 "이건 이상한데 그냥 넘어갔다"고 느끼는 횟수를 센다.

## ~~3단계 — AI 목소리~~ — 안 하기로 했다 (2026-08-23 운영자 결정)

**폰 내장 목소리를 그대로 쓴다.** AI 목소리는 사지 않는다.

운영자의 근거는 둘이었고, 둘 다 맞다.

1. **어차피 화면을 보면서 한다.** 대답도 교정도 글로 떠 있다.
   소리는 곁들이는 것이지 이 화면의 주인공이 아니다
2. **리스닝 연습에 크게 도움이 안 된다.** 이건 특히 맞는 말이다 —
   **리스닝은 이미 1단계(받아쓰기)가 하고 있고, 거기서는 진짜 사람이 말한 것을 듣는다.**
   영상과 드라마 대사다. **합성한 목소리를 더 듣는 것이 그보다 나을 이유가 없다.**

**빼면서 같이 사라진 것들** — 이게 이 결정의 값어치다.

- 연 5.0만 원
- **거래처 한 곳** (목소리만 다른 회사였다). 이제 **회사 하나, 열쇠 하나**다.
  운영자가 폰마다 넣어야 하는 것도 하나뿐이다
- **답이 1~2초 늦어지는 문제.** 글을 만들고 → 소리로 바꾸고 → 재생하는 단계가 없어진다.
  판정 기준의 "3초 안에 오나" 를 지키기가 그만큼 쉬워진다
- **"Gemini 목소리를 브라우저에서 부를 수 있나" 를 확인할 일**도 없어졌다

> **폰 목소리를 아예 안 쓴다는 뜻은 아니다.** 지금도 문장카드의 "듣고 말하기" 가 쓰고 있고,
> 대화 답을 읽어 주는 것도 폰 목소리로 하면 된다 — **값이 0원일 뿐이다.**
> 안 사기로 한 것은 **AI 가 만든 목소리**다.

**나중에 뒤집는다면** 근거는 하나뿐이다 — 폰 목소리가 **알아듣기 어려워서** 연습이 안 될 때.
"밋밋하다" 는 근거가 되지 않는다. 위 2번이 그것을 이미 답했다.

### 브라우저 말고 다른 길 — 앱으로 내면 제약이 풀린다 (2026-08-23 운영자 질문)

운영자가 물었다 — **웹으로 만드는 이상 결국 브라우저인데, 다른 길은 없나.**
그리고 **나중에 재미로 앱처럼 출시하고 싶다**고 했다. 둘은 사실 같은 얘기다.

**막고 있는 규칙(CORS)은 브라우저에만 있다.** 진짜 앱은 그 규칙을 안 탄다.
즉 **앱으로 내면 받아적기도 목소리도 직접 부를 수 있다.**

| 길 | 받아적기·목소리 | 드는 돈 | 깨지는 제약 |
|---|---|---|---|
| **지금** (웹 + 홈 화면에 추가) | 폰 내장만 | 0원 | 없음 |
| **웹 코드를 앱으로 포장** (Capacitor 같은 것) | **직접 부를 수 있다** | 애플 연 $99 (약 14만 원), 구글 1회 $25 | **빌드 단계 없음**이 깨진다 |
| **작은 서버 하나** | 직접 부를 수 있다 | 서버비 + 계속 관리 | **서버 없음**이 깨진다 |

- 앱으로 포장하는 쪽은 **지금 코드를 다시 쓰지 않는다.** 있는 파일을 그대로 감싼다
- 대신 **빌드 단계가 생긴다.** SPEC 2 를 바꾸는 일이라 그때 다시 정한다
- **고친 것이 바로 안 나간다.** 지금은 올리면 1~2분인데, 앱은 심사를 기다린다
- 애플 쪽은 **매년** 나간다. 안 내면 앱이 내려간다 — 구독과 같은 성격이라
  "안 하면 0원"이 여기서는 안 통한다
- **운영자가 "재미로 내고 싶다"고 했으므로 이건 제약을 푸는 덤이 아니라 그 자체가 목적인 갈래다.**
  3단계와 나란히 놓고 본다. 다만 **2단계를 웹으로 먼저 써 본 뒤**다 —
  앱으로 감싸는 것은 나중에 해도 늦지 않고, 웹으로 안 쓰게 되면 앱으로도 안 쓴다

---

## 비용 계산 (2026-08 조사, 1달러 1,400원)

### 전제

한 사람, 하루 30분, 매일. 주고받는 횟수 45번.
내가 한 번에 30단어쯤 말하고, AI 가 60단어쯤 답한다.

**대화는 앞 내용을 계속 다시 보내야 해서 뒤로 갈수록 비싸진다.**
앞부분을 재활용하면(prompt caching) 크게 준다 — **산식은 2단계 절에 적어 뒀다.**

### 역할별 단가

> **AI 모델은 100만 **토큰**당이다. 글자가 아니다** (영어는 대략 글자 4개가 토큰 1개).
> 목소리(TTS)만 글자 단위다. 헷갈리면 1년 뒤 재계산이 안 된다.

| 역할 | 쓰는 것 | 단가 | 단위 |
|---|---|---|---|
| 대화 | Claude Haiku 4.5 | 입력 $1 / 출력 $5 | 100만 **토큰** |
| 대화 요약 | Claude Sonnet 5 | 입력 $3 / 출력 $15 | 100만 **토큰** — 다른 후보는 2단계 절의 비교표 |
| AI 목소리 | Google Gemini Flash TTS | $6 | 100만 **글자** |
| ~~받아적기~~ | ~~Deepgram Nova-3~~ | ~~1분당 $0.0048~~ | **서버가 필요해 뺐다** |

목소리는 회사별로 **10배 차이**가 난다. ElevenLabs 는 100만 글자당 $60~120 이라 제외.

### 1년 합계 (한 명 기준)

| 구성 | 대화 | 받아적기 | 목소리 | **1년** |
|---|---|---|---|---|
| **A. 대화만 AI** ← **이걸로 간다** | AI | 폰 내장 | 폰 내장 | **약 2.5만~6.6만 원** (회사에 따라) |
| ~~B. + 받아적기 AI~~ | AI | AI | 폰 내장 | ~~8.5만 원~~ **서버가 필요해서 뺐다** |
| ~~C. + 목소리까지~~ | AI | 폰 내장 | AI | ~~약 10만 원~~ **안 하기로 했다** |
| ~~E. 최고급 목소리(ElevenLabs)~~ | AI | 폰 내장 | AI | ~~30만 원 이상~~ **제외** |
| **Langua** | — | — | — | **약 22만 원** |

### 전부 켰을 때는 얼마인가 (2026-08-23 운영자 질문)

운영자가 물었다 — **받아적기·목소리·교정·리포트를 다 AI 로 했을 때 하루 30분 기준 얼마냐.**
아래는 **Gemini 3.7 Flash 도입가($0.75 / $3.75)** 로 계산한 값이다. 한 사람 기준.

| 무엇 | 하루 | **1년** |
|---|---|---|
| 대화 + 내 문장 교정 + 제안 버전 | $0.062 | 3.2만 원 |
| ~~내 말 받아적기~~ (AI 로 하려면 서버가 필요하다) | — | **뺐다** |
| ~~AI 목소리로 대답 읽어 주기~~ | — | **안 하기로 했다** |
| 리포트 (대화 요약) | $0.012 | 0.6만 원 |
| **합계** | **$0.23** | **약 11.8만 원** |

> **이 표는 "전부 AI 로 하면 얼마인가" 를 물어서 계산해 본 것이고, 실제로는 이렇게 안 간다.**
> 받아적기는 서버가 필요해서 빠졌고, 목소리는 운영자가 안 하기로 정했다.
> **실제로 갈 구성과 값은 바로 아래 "앱 없이도 될 것 같다" 에 있다 — 연 2.5만 원.**

> **이 11.8만 원은 캐싱이 다 걸린다고 가정한 값이다. 나중에 확인해 보니 우리 구조에서는
> 잘 안 걸린다** — 2단계 절의 "Gemini 캐싱을 확인했다" 를 볼 것.
> **현실적으로는 Gemini 17.4만 원, Claude 14.6만 원이다.**

**교정은 따로 값이 붙지 않는다.** 내 문장을 고쳐 주는 것과 제안 버전은 **대화 답 안에
같이 담겨 온다** — 별도 요청이 아니라 답의 칸이 늘어나는 것이라, 출력이 조금 길어지는
값만 들고 그건 위 대화 줄에 이미 들어 있다.

#### 여기서 알아 둘 것 세 가지

**1. 받아적기 + 목소리가 전체의 68% 다** (8.0만 원 대 3.8만 원).
**모델을 아무리 싼 것으로 바꿔도 아끼는 폭이 작다.** 값을 줄이려면 모델이 아니라
"목소리를 폰 내장으로 둘까" 를 봐야 한다. 그것만 빼면 6.8만 원으로 내려간다.

**2. 캐싱이 걸리냐 안 걸리냐가 제일 큰 변수다. — 확인 안 된 값이다.**
위 값은 캐싱이 Claude 처럼 걸린다고 **가정**한 것이다.
**안 걸리면 대화값만 5배로 뛰어 총 23.9만 원**이 되고, 그러면 Langua 보다 비싸다.
Claude 는 4,096 토큰을 넘어야 걸린다는 것을 확인했지만 **Gemini 는 규칙도 할인율도 다르고
아직 확인하지 못했다.** 붙이기 전에 반드시 확인해서 이 자리에 적는다.

**3. 2027-01-01 에 값이 2배가 된다.** 도입가 기준이라서다.
그때 대화와 요약이 두 배가 되면 약 14.6만 원이 된다.

#### 앱이 필요한가 — 이제 아니다

**여기까지 오면서 앱이 필요한 이유가 하나씩 사라졌다.**

- 받아적기를 AI 로 하려면 앱이나 서버가 필요했다 → **폰 내장·브라우저 안에서 쓰기로 했다**
- AI 목소리도 마찬가지였다 → **운영자가 안 사기로 정했다 (2026-08-23)**
- 남은 것은 **글을 주고받는 것뿐이고, 그건 세 회사 다 브라우저에서 직접 된다**

**그래서 앱은 기능 때문에 낼 일이 아니다.** 아래 표는 "그래도 앱으로 냈다면" 의 값이라
기록으로만 남긴다. 앱으로 내면 **애플 연 $99 ≈ 14만 원**이 붙고 쓰든 안 쓰든 나간다 —
다만 **다른 앱 때문에 계정이 어차피 생긴다면 이 앱 몫은 0원**이다 (아래 참고).

| | AI 값 | **+ 앱 등록비** | Langua |
|---|---|---|---|
| 1인, 매일 30분 | 11.8만 | **25.8만** | 22만 |
| 1인, 주 3회 | 5.0만 | **19.0만** | 22만 |
| **가족 셋, 매일** | 35.4만 | **49.4만** | **66만** |

> **혼자 쓰면 앱 등록비 때문에 Langua 와 비슷하거나 더 비싸다. 가족이 셋이면 확실히 싸다.**
> 앱 등록비는 한 계정에 한 번이고 Langua 는 사람 수만큼 곱해지기 때문이다.
> **"쓴 만큼만 낸다"는 장점이 고정 등록비에 먹힌다** — 적게 쓸수록 손해다.

#### 그런데 앱 없이도 될 것 같다 (2026-08-23 확인)

위 표는 "받아적기와 목소리는 브라우저에서 못 부른다" 를 전제로 한 것인데,
확인해 보니 **둘 다 우회로가 있다.**

- **목소리:** Gemini 는 브라우저에서 직접 부를 수 있다는 것을 확인했다(확인표 참고).
  **실시간(Live) 쪽이 아니라 한 번에 주고받는 TTS 를 쓰면 된다.** 대답 한 줄을 통째로
  소리로 바꿔 받아 재생하면 되므로 실시간일 필요가 없다
- **받아적기:** Whisper 를 **브라우저 안에서(WebAssembly)** 돌릴 수 있다.
  **회사에 말을 걸지 않으므로 CORS 도 열쇠도 필요 없고 값도 0원이다.**
  폰 내장보다 한국식 발음에 강할 가능성이 있다

그러면 이런 구성이 된다 — **앱도 서버도 없이, 웹 그대로.**

| 무엇 | 어디 | 1년 |
|---|---|---|
| 대화 + 교정 | **OpenAI gpt-5-mini** (또는 Claude Haiku 4.5) | 1.3만 (Claude 면 4.2만) |
| 리포트 | **OpenAI gpt-5** (또는 Claude Sonnet 5) | 1.2만 (Claude 면 2.4만) |
| 목소리 | **폰 내장** (운영자 결정) | **0원** |
| 받아적기 | 브라우저 안 Whisper, 또는 폰 내장 | **0원** |
| **합계** | | **약 2.5만 원** (Claude 조합이면 6.6만) |

**주고받는 것이 글뿐이라 회사 한 곳, 열쇠 하나로 끝난다.**

**앱 없이, 서버 없이, 웹 그대로 된다.** 심사도 등록비도 없고
고친 것이 1~2분이면 나가는 지금의 장점도 그대로다.

> **확인해야 할 것은 이제 하나뿐이다.** (목소리를 안 사기로 하면서 하나가 없어졌다.)
> **브라우저 Whisper 가 폰에서 쓸 만한가.** 모델 파일을 한 번 내려받아야 하고
> (수십 MB), 폰에서 느릴 수 있다. **아이폰에서 되는지가 특히 중요하다** —
> 이 앱이 제일 자주 깨지는 자리다.
>
> **이게 안 돼도 앱 없이 간다.** 그때는 지금 `record.js` 가 쓰는 **폰 내장 받아적기**를
> 그대로 쓰면 되고, 값은 어차피 둘 다 0원이다. 브라우저 Whisper 는
> **폰 내장이 한국식 발음을 못 알아들을 때 꺼내는 카드**지 전제가 아니다.

#### 앱은 언제 내나 (2026-08-23 운영자 결정)

운영자가 **다른 앱도 재미로 앱스토어에 낼 생각**이라고 했다. **지금 당장은 아니다.**
그러면 계산이 달라진다.

- **애플 개발자 계정이 다른 앱 때문에 어차피 생긴다면, 이 앱에 붙는 추가 비용은 0원이다.**
  앞에서 "앱은 연 14만 원이 더 든다" 고 적은 전제가 사라진다
- 그러면 앱 갈래에 남는 부담은 **빌드 단계가 생기는 것**과
  **고친 것이 심사를 기다리는 것** 둘뿐이다
- 그리고 앱으로 내면 **받아적기 회사를 아무 데나 고를 수 있다** — CORS 가 상관없어진다

**그래도 서두를 이유는 없다.** 웹으로도 전부 되는 것이 이번에 확인됐기 때문이다.

> **순서를 이렇게 둔다.**
> **웹으로 2단계를 만들어 써 본다 → 앱은 다른 앱을 낼 때 같이 낸다.**
> 앱은 **제약을 풀려고 내는 것이 아니라 재미로 내는 것**이다. 그 구분을 지킨다 —
> 필요해서 내는 것으로 착각하면 웹에서 할 수 있는 일을 앱을 기다리며 미루게 된다.

### 숫자를 볼 때 감안할 것

1. **가족 수만큼 곱한다.** 셋이 매일 하면 A 는 17만 원. (Langua 는 셋이면 66만 원)
2. **매일 30분 빠짐없이 하는 기준이다.** 실제로는 그렇게 안 된다.
   **쓴 만큼만 낸다는 게 구독과 다른 점이다** — 두 달 안 하면 그 두 달은 0원
3. **거래처.** A 는 회사 한 곳(Anthropic), C 는 두 곳

## 모델 선택 기준

역할마다 중요한 게 다르다.

| 역할 | 고르는 기준 | 지금 추천 |
|---|---|---|
| 대화 상대 | 지시 준수 · **속도** · 값 | Claude Haiku 4.5 — 대화는 3초 넘으면 흐름이 끊긴다. 똑똑함보다 빠름 |
| 대화 요약 | 깊이 · 뉘앙스 | Claude Sonnet 5 — 대화 한 판에 한 번뿐이라 좋은 걸 써도 부담 없다 |
| 내 말 받아적기 | 서버가 필요한가 | **폰 내장** — AI 로 하려면 서버가 있어야 한다 (2단계 절 참고) |
| AI 목소리 | — | **안 산다.** 폰 내장을 쓴다 (2026-08-23 운영자 결정) |
| 하루 5문장 | 값 0원 | 기존 구독 (1단계) |

**편향을 밝혀 둔다.** 이 문서를 쓴 것이 Claude 라서 대화 부분에 Claude 를 미는 것이
편향으로 보일 수 있다. 게다가 운영자에게는 **반대 증거**가 있다 —
챗에서 Claude 가 지시를 안 지켰고 Gemini 가 조금 나았다.

그래서 **2단계를 만들 때 대화 모델을 갈아 끼울 수 있게 짠다.** 설정에서 회사를 고른다.
일주일씩 써 보고 운영자가 정한다. 위 추천은 참고값이다.

(다만 "지시를 안 지킨다" 문제 자체는 위에서 적었듯 **모델이 아니라 구조**의 문제라,
어느 회사를 골라도 똑같이 해결된다.)

> **갈아 끼우기는 설정 한 줄이 아니다.** 답의 모양을 강제하는 방식이 회사마다 다르고
> (Anthropic 은 도구 정의, Google 은 응답 스키마), 브라우저에서 직접 부를 수 있는지도
> 회사마다 다르다. **회사별 어댑터를 하나씩 쓰는 일**로 잡아야 한다.
> 회사를 더할 때마다 "브라우저에서 직접 부를 수 있나" 를 먼저 확인하고 표에 적는다.

### 세 회사를 나란히 놓고 본다 — 대화·요약 (2026-08-23)

운영자가 ChatGPT 도 넣어 보라고 해서 셋을 같이 봤다.

| | Claude Haiku 4.5 | Gemini 3.7 Flash | **OpenAI gpt-5-mini** |
|---|---|---|---|
| 단가 (입력 / 출력, 100만 토큰당) | $1 / $5 | $0.75 / $3.75 | **$0.25 / $2** |
| 캐싱 할인 | 90% | 90% | 50~90% (세대마다 엇갈린다) |
| 캐싱 최소 크기 | 4,096 | 4,096 | **1,024** — 제일 낮다 |
| **답의 모양 강제와 캐싱이 같이 되나** | **된다** (문서에 명시) | **문제 있다** (위 참고) | **된다** — 스키마가 캐시 앞부분에 포함된다 |
| 브라우저에서 직접 | 된다 | 된다 | 된다 |

**대화 + 요약만, 1인 매일 30분, 1년**

| | 1년 |
|---|---|
| **OpenAI** (gpt-5-mini 대화 + gpt-5 요약) | **2.5만 ~ 4.3만 원** |
| Claude (Haiku 4.5 + Sonnet 5) | 6.6만 원 |
| Gemini 3.7 Flash (캐싱 문제를 반영한 값) | 9.4만 원 |

- **OpenAI 가 제일 싸다.** 단가가 낮고, 캐싱 최소 크기가 1,024 라 **첫 턴부터 걸리고**,
  Gemini 가 걸린 함정(답의 모양 강제와 캐싱이 부딪히는 것)에 안 걸린다
- 폭이 넓은 것은 **캐싱 할인율이 모델 세대마다 50% 인지 90% 인지 엇갈려서**다.
  붙일 때 청구서로 확인한다
- **모델이 똑똑할 필요가 없다는 것이 이 단계의 전제다** (답의 모양을 강제하니까).
  그러니 제일 싼 칸을 고르는 것이 이치에 맞는다
- 다만 운영자에게는 **Gemini 가 챗에서 더 나았다**는 경험이 있다.
  OpenAI 쪽 경험은 없다. **일주일씩 써 보고 정한다** — 값은 참고값이다

### 목소리는 어느 회사인가 — Claude 는 안 된다 (2026-08-23 운영자 질문)

**Anthropic 에는 목소리(TTS) 제품이 아예 없다.** Claude 는 글·그림·문서를 받아
**글을 내놓는 것까지**다. 그래서 목소리는 반드시 다른 회사다.

**그리고 목소리는 캐싱과 아무 관련이 없다.** 캐싱은 "대화를 매번 처음부터 다시 보내는 것"
을 아끼는 장치인데, 목소리는 **완성된 답 한 줄을 소리로 바꾸는 별개 요청**이라
반복되는 앞부분이 없다. 아낄 것이 없다.

| 목소리 | 1년 (하루 16,200 글자) |
|---|---|
| 폰 내장 | **0원** ← **운영자가 이걸로 정했다 (2026-08-23)** |
| Gemini Flash TTS | 5.0만 원 (AI 중에는 제일 싸다) |
| OpenAI tts-1 | 12.4만 원 |
| OpenAI gpt-4o-mini-tts | 13.8만 원 |
| ElevenLabs | 50만 원 이상 |

**목소리는 폰 내장으로 간다.** 근거는 3단계 절에 있다.
**나중에 AI 목소리를 다시 본다면 Gemini 다** — 값이 두 배 넘게 차이 난다.

### 다른 방식은 어떤가 — 알아본 결과 (2026-08-23)

운영자가 물어서 알아봤다. **또 물어볼 질문이라 답을 여기 남긴다.**

| 방식 | 값 | 판단 |
|---|---|---|
| **Gemini 3 Flash Preview** | $0.25 / $1.50 | 제일 싸다. 다만 **Preview 딱지**가 붙은 값은 언제든 바뀌거나 없어진다 |
| **Gemini 로 갈아 끼우기** | — | **브라우저에서 직접 되는 것은 확인했다.** 다만 **캐싱이 우리 구조에서 잘 안 걸려** 총액은 Claude 가 싸다 (2단계 절 참고) |
| **Gemini 3.7 Flash** | $0.75 / $3.75 (도입가) | 싸다. **2027-01-01 에 2배**로 오른다고 이미 예고돼 있다 |
| Claude Haiku 4.5 (지금 추천) | $1 / $5 | 기준값 |
| **오픈 모델을 클라우드 GPU 에 올리기** | GPU 한 장 **월 160만~290만 원** | **비교가 안 된다.** 아래 참고 |
| 오픈 모델을 토큰당 파는 곳 | 100만 토큰당 $0.28~0.88 | 그냥 더 싼 API 다. "클라우드 비용만"이 아니다 |

**대화를 Gemini 로 바꾸면 1인 연 2만 원쯤 아낀다.** 값어치가 있고,
운영자에게 **"챗에서 Gemini 가 더 나았다"는 경험**까지 있으므로 꼭 비교해 볼 만하다.
다만 **브라우저에서 직접 부를 수 있는지가 아직 미확인**이다 — 받아적기가 그래서 깨졌다.
먼저 확인하고 표에 적은 뒤에 붙인다.

> **"오픈 모델을 클라우드에 올리면 클라우드 비용만 내면 되지 않나" (운영자 질문).**
> 그게 성립하려면 GPU 를 빌려 켜 둬야 하는데, **GPU 는 시간당 과금이라 쓰든 안 쓰든 나간다.**
> H100 한 장이 시간당 $2~4, **한 달에 160만~290만 원**이다.
> API 로 1년 쓸 돈(6~8만 원)을 하루 반이면 쓴다.
>
> 손익분기는 대략 **가동률 30%** 또는 **하루 500만 토큰**이다.
> 우리는 하루 30분(가동률 2%), 세 식구가 다 해도 하루 23만 토큰 — **20배 넘게 모자란다.**
> 껐다 켜면 되지 않냐 하면, 그때는 시동에 몇 분 걸린다.
> **이 저장소는 이미 같은 이유로 서버 방식을 한 번 기각한 적이 있다** (SPEC 2, Apps Script 콜드스타트).
>
> **핵심은 이것이다.** 이 앱의 제일 큰 장점은 "안 하면 0원"이다. 시간당 과금은 그 정반대다.

---

## 지금 정하지 않은 것

- ~~대화 기록을 저장소에 남길지~~ → **안 남기기로 정했다** (2026-08-23, 2단계 절).
  자동 난이도 조절은 그래서 기기 안에서 푸는 길을 따로 찾아야 한다
- 발음 점수 (Langua 에 있지만 운영자가 언급하지 않았다)
- 가족 구성원별 난이도 분리 (지금은 프로필이 하나뿐)
