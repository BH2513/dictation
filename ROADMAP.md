# 장기 계획 — 말하기 연습 기능 (Phase 2)

Phase 1(받아쓰기)이 끝난 뒤 무엇을 왜 만들지 정리한 문서다.
결정 근거와 비용 계산을 남겨 둔다 — 나중에 "그때 왜 이렇게 정했지"를 다시 계산하지 않기 위해서다.

기준 문서는 여전히 `SPEC.md` 다. 여기서 정한 것이 실제로 만들어지면 SPEC 에 옮긴다.

작성: 2026-08-22. 가격은 그때 조사한 값이다 — **1년쯤 지났으면 다시 확인할 것.**

---

## 왜 이 기능을 만드나

운영자가 Langua 라는 앱으로 스피킹 연습을 해 봤고(1년 159달러, 하루 30분), 그중 네 가지가
특히 좋았다고 했다. 그대로 옮겨 오는 것이 목표다.

1. 내가 말한 문장을 **더 자연스럽게 고쳐서 화면에 따로 띄워 주는 것**
2. 그 문장을 **카드로 저장해서 복습**하는 것
3. 이상하게 말하면 **내 문장을 먼저 고치고 나서** 자기 대답을 하고 대화를 잇는 것
4. 연습이 끝나면 **전체 대화를 요약**해서 잘한 것·못한 것·문화적인 부분까지 짚어 주는 것

2번은 이미 이 앱에 있다. 문장카드 시스템(SPEC 7)을 그대로 쓴다.

또 하나, 대화가 하기 싫은 날을 위해:

5. **하루에 문장 다섯 개**를 받아서 혼자 영어로 옮겨 보고, 말해 보고, 정답과 맞춰 보는 연습

---

## 세 단계로 나눈다

| 단계 | 무엇 | 비용 | 상태 |
|---|---|---|---|
| **1** | 하루 다섯 문장 (위 5번) | **0원** | 만들었다. 쓰면서 고치는 중 |
| **2** | 대화 연습 (위 1·2·3·4번) | 연 5.5만 원 | 1단계 판정 기준을 통과하면 |
| **3** | AI 목소리 | 연 +4.8만 원 | 2단계가 밋밋하면 |

> **받아적기를 AI 로 하는 안(연 8.5만 원)은 뺐다.** 확인해 보니 **서버 없이는 안 된다** —
> 2단계 절의 "브라우저에서 직접 부를 수 있나" 를 보라. 받아적기는 폰 내장 기능을 쓴다.

**1단계를 먼저 하는 이유가 있다.** 돈이 안 들고, 제약(서버 없음·비용 0원)을 안 깨고,
이미 만들어 둔 채점기·녹음·문장카드를 그대로 재사용한다. 무엇보다
**돈을 쓰기 전에 "이 방식이 나한테 맞나"를 먼저 확인할 수 있다.**

---

## 1단계 — 하루 다섯 문장 (비용 0원)

### 핵심 아이디어

이 기능은 **실시간이 아니다.** 아침에 문장이 준비돼 있기만 하면 된다.
그러면 미리 만들어 두면 되고, 미리 만드는 것은 **운영자의 기존 구독 한도**로 할 수 있다.
유료 API 가 필요 없다.

### 어떻게 도나

```
매일 새벽 (GitHub 가 알아서)
   → Claude 를 한 번 돌려서 문장 5개 생성
   → data/daily/{프로필}/sets/{번호}.json 으로 저장소에 커밋 (날짜에 묶지 않는다)
   → GitHub Pages 가 배포
   → 아침에 앱을 열면 새 문장 5개가 있다
```

- **비용 0원** — 운영자 구독 한도에서 나간다. 하루 5문장은 한도에 티도 안 난다
- **서버 0원** — 앱이 이미 올라가 있는 GitHub 가 그대로 해 준다
- **제약 안 깨짐** — SPEC 2의 "서버 없음 / 비용 0원" 그대로다
- 운영자가 할 일 없음

### 문장 난이도 — 이게 제일 중요하다

운영자는 듀오링고가 안 맞는 수준이다. **쉬우면 아무 도움이 안 된다.** 그래서 만들 때 못 박는다.

- **20~35 단어**, 절이 둘 이상
- 관계절 · 가정법 · 분사구문 중 최소 하나는 반드시 포함
- 상황을 매일 무작위로 바꾼다 — 회의에서 반대 의견 내기, 병원에서 증상 설명,
  서비스에 항의하기, 조건 걸고 협상하기, 뉴스 논평, 애매하게 거절하기 …
- **저장소에 이미 있는 영상 자막에서 어휘를 끌어온다** — 받아쓰기로 듣고 있는 어휘와 겹치게
- 최근 며칠에 쓴 상황은 피한다 (반복 방지)

### 채점은 하지 않는다 (2026-08-23 운영자 결정)

처음에는 받아쓰기 채점기를 그대로 붙였는데, 써 보니 **맞힐 수도 없고 맞힐 이유도 없었다.**
옮겨 말하기는 정답이 하나가 아니다. 허용 답안을 여러 개 두어도 마찬가지였다.

그래서 점수와 오답 표시를 뺐다. 지금은 이렇게 돈다.

- 말하기 → 지금 `record.js` 가 쓰는 것 그대로. 음성인식이 되면 받아적은 것을 보여 준다
- `Show answer` → 정답 · 다르게 말하는 법 · 설명을 보고 **스스로 견준다**
- 반복 연습 → **문장카드에 그대로 꽂는다.** 한국어가 있으므로 한→영 모드가 바로 된다
- 진도는 "봤다"만 남긴다. 연속 학습 일수와 학습량 그래프는 그대로 된다

`alts` 는 채점용이 아니라 **다르게 말하는 법을 보여 주는 용도**로 남았다.

### 만든 것을 한 번 더 읽힌다 (2026-08-23)

첫 문장 묶음에서 관용구 오용이 두 개 나갔다 — `hit the spot`(음식에 쓰는 말)을 휴식에,
`call it a day`(일을 끝낼 때 쓰는 말)를 배달 얘기에. 게다가 그 둘을 `note` 에
배울 표현이라고 강조까지 해 둬서, **틀린 영어를 가르치고 있었다.**

그래서 만드는 호출 다음에 **검수 호출을 한 번 더** 붙였다. 지적만 시키지 않고
고친 최종본을 받아서 내보낸다. 구독 한도로 도므로 값은 그대로 0원이다.

기계로 잡을 수 있는 것은 기계가 먼저 잡는다 — 강조한 표현이 문장에 실제로 나오는지,
캐주얼이 정답과 앞부분이 겹치지 않는지. AI 판단에만 기대지 않는다.

### 번역체를 잡는다 (2026-08-23 운영자 리뷰)

운영자가 첫 묶음을 통째로 리뷰했다. 지적이 여럿이었는데 **원인이 하나로 모였다** —
한국어를 먼저 쓰고 영어로 옮기고 있었다.

드러난 자국들:

- 한국어가 애매해서(`그거`, `그런 것`) 영어가 `it`/`that` 투성이가 됐다
- 20~35 단어를 **한 문장**으로 쓰라고 해서 쉼표가 쌓였다. 영어는 짧게 끊어 말한다
- 내가 지시문에 `honestly, kind of, like 같은 말버릇이 들어가면 좋습니다` 라고 써 놓아서,
  뜻과 상관없이 장식으로 박혔다. `이상하게`가 `weirdly` 가 아니라 `honestly` 로 나간 것,
  `like actually nothing` 이 십대 말투가 된 것이 전부 여기서 나왔다
- `reorder` 처럼 뜻은 맞지만 그 자리에서 원어민이 안 쓰는 낱말
- `caved on day three` 처럼 두 가지로 읽히는 대목

**왜 casual 쪽이 더 나았는가**도 같은 이유로 설명된다. alts 는 한국어에서 옮긴 것이 아니라
**이미 있는 영어를 말투만 바꿔 쓴 것**이라 한국어에 끌려다닐 일이 없었다. 게다가 더 짧았다.
이게 영어를 먼저 쓰는 쪽이 맞다는 증거다.

그래서 순서를 뒤집었다. **영어 → 소리내어 확인 → 한국어.**
기계 검사도 셋 더했다 — 한 문장 안 쉼표 3개 초과, 영어가 한 문장뿐, 대명사 5개 초과.
넣자마자 기존 다섯 개가 전부 걸렸다.

### 말투 (2026-08-23 운영자 결정)

처음 만든 문장이 회의·협상·항의 같은 격식체라 **"문장이 너무 별로"** 라는 지적을 받았다.
난이도와 길이는 그대로 두고 **일상 대화체로 바꿨다** — 친구·가족에게 하는 말,
축약형을 쓰고, 소리내어 읽었을 때 어색하지 않게. 상황 목록도 통째로 갈았다.

캐주얼을 어떻게 잡을지도 정했다. **관용구를 넣는 것이 아니라 더 짧고 축약된 구어**다
(운영자 선택). 관용구는 뜻이 정확히 맞을 때만 쓴다 — 어설프게 쓰면 틀린 영어를 가르치게 된다.
포멀은 **뜻을 바꾸지 않고 말투만 올린다.**

### 아직 못 하는 것 (솔직하게)

- **자동 난이도 조절.** "너무 쉬우면 다음 날 올린다"는 내 채점 결과를 봐야 하는데,
  그 기록은 폰의 IndexedDB 에만 있고 저장소로 올라오지 않는다.
  지금은 난이도를 `data/daily/config.json` 에 고정해 두고, 조절이 필요하면 사람이 바꾼다.
  자동화하려면 기록을 저장소로 올리는 길이 먼저 필요하다 — 3단계 이후 과제
- **정답 판정의 미묘한 경우.** 허용 답안에 없는데 사실 맞는 표현일 때가 있다.
  2단계(대화)가 붙으면 그때 AI 판정을 얹는다

### 알려진 걸림돌

정해진 시각에 자동으로 도는 기능에서 권한 문제가 보고된 적이 있다
([issue #814](https://github.com/anthropics/claude-code-action/issues/814)).
그래서 `claude-code-action` 을 쓰지 않고 **CLI 를 직접 돌린다** — 그쪽 권한 절차를 아예 안 탄다.

그리고 **조용히 멈추면 안 된다.** 며칠 문장이 안 오면 앱 화면에 알린다. (CLAUDE.md 원칙)

---

### 드라마 대사 갈래를 더한다 (2026-08-23 운영자 결정)

번역체를 잡고 나서도 운영자가 물었다 — **미드 대사를 그대로 쓰면 어색함이 아예 없지 않겠나.**
맞는 말이다. 실제 작가가 쓰고 배우가 말한 것은 어색할 수가 없다.

문제는 자막을 어디서 구하느냐였다. 이 작업 환경에서는 자막 사이트가 전부 막혀 있고
(OpenSubtitles, IMSDb, archive.org, GitHub 검색), AI 가 "기억해서" 대사를 적으면 지어낸다.
그리고 **자막 파일을 통째로 공개 저장소에 넣는 것은 재배포**다.

그래서 이렇게 정했다.

- 운영자가 PC 에 자막 파일을 둔다. `tools/add_subs.py` 가 거기서 **300줄쯤만 뽑아** 올린다.
  원본은 PC 에 남는다 — 인용 정도다
- 두 달에 한 번쯤만 돌리면 된다. 매일 다섯 줄 뽑는 것은 저절로 돈다
- 고르기는 **규칙 → AI** 두 단계다. 규칙은 길이밖에 못 보므로 뜻은 AI 가 본다 (운영자 제안)

Daily 화면은 탭 두 개가 되었다 — `Made for you` 와 `From your videos`.

## 2단계 — 대화 연습

### 왜 챗으로 하면 안 되나 — 이 단계의 존재 이유

운영자가 겪은 문제: 챗에서 "내 문장 먼저 고치고 대답해"라고 시켜도
**한 번만 하고 그 뒤로는 안 한다.** 지적하면 사과하고 또 안 한다.

원인은 모델이 멍청해서가 아니다. **지시가 놓인 자리가 잘못돼서다.**
챗에서 그 지시는 "대화 속의 한 마디"라, 10턴만 지나면 메시지 20개 중 하나가 되고
최근 대화에 묻힌다. 사과도 그 턴에만 효과가 있다.

API 로 만들면 세 겹으로 막을 수 있다.

**1. 지시를 대화 밖으로 뺀다.** 매 요청마다 맨 앞에 자동으로 다시 붙는 "역할 설명" 칸이
따로 있다. 대화가 100턴이 돼도 매번 새로 읽는다. 묻힐 자리가 없다.

**2. 답의 모양을 강제한다 (structured output). ← 결정적**

답을 자유롭게 쓰게 두지 않고 칸을 미리 정한다.

```
내가 한 말   : I want to going to the meeting tomorrow.
고친 문장    : I want to go to the meeting tomorrow.
더 자연스럽게 : I'd like to sit in on tomorrow's meeting.
왜          : want to 뒤에는 동사원형. "sit in on"은 참관한다는 뉘앙스
AI 대답     : Sure — which meeting are you thinking of?
```

**"고친 문장" 칸을 비우면 답 자체가 형식에 안 맞아서 거부되고 다시 만든다.**
까먹는 것이 구조적으로 불가능해진다. 챗으로는 아예 할 수 없는 것이다.

**3. 화면이 한 번 더 막는다.** 교정 칸은 별도 박스로 그린다. 안 채우면 빈 박스가 보인다.

> **한계도 적어 둔다.** 보장되는 것은 "칸이 반드시 채워진다"이지
> "교정이 항상 훌륭하다"가 아니다. 내 문장이 이미 멀쩡하면 "고칠 것 없음"이 들어간다.
> 하지만 "한 번 하고 안 함"은 사라진다.

**따라서 이 문제는 모델 성능 문제가 아니다.** 제일 싼 모델로도 해결된다.

> **다만 "설정 한 줄로 회사를 갈아 끼운다"는 아니다.** 답의 모양을 강제하는 방식이
> 회사마다 다르다 — Anthropic 은 도구 정의로, Google 은 응답 스키마로, OpenAI 는
> 또 다른 이름으로 부른다. 회사를 바꾸면 **그 부분을 회사별로 따로 써야 한다.**
> 갈아 끼우기는 "회사별 어댑터 하나씩"으로 잡아야지 설정값 하나로 잡으면 안 된다.

---

### 무엇을 만들 것인가

#### 화면과 흐름

라이브러리에 `Talk` 단추를 더한다. (Daily 와 나란히.)

```
라이브러리 → Talk → 주제 고르기 → 대화 → 끝내기 → 대화 요약
```

**주제 고르기.** 오늘 무슨 얘기를 할지 고른다. 세 갈래.
- `Free talk` — 아무 얘기나
- `From today's sentences` — 오늘 Daily 에서 한 문장의 상황을 이어서
- `From a show` — 등록한 영상의 장면 상황으로 (Shows 갈래와 이어진다)

**대화 화면.** 한 턴이 이렇게 생겼다.

```
[내가 말한 것]  (음성인식이 받아적은 것, 고칠 수 있게 입력칸)
─────────────────────────────
[고친 문장]     ← 별도 박스. 안 채워지면 빈 박스가 보인다
[더 자연스럽게]
[왜]            ← 배울 표현은 ** 로 강조 (Daily 와 같은 규칙)
─────────────────────────────
[AI 대답]       ← 폰 목소리로 읽어 준다
```

- 아래에 `Say` (녹음) / `Send` (타이핑) / `Save card` / `End`
- **`Save card` 는 "고친 문장"을 담는다** — 내가 한 말이 아니라 고쳐진 쪽이다.
  담긴 카드는 기존 문장카드 네 모드에 들어간다.
  `videoId` 는 `talk-{대화번호}`, 번호는 바뀌지 않는다 (Daily 묶음과 같은 규칙)
- **다만 "그대로"는 아니다 — 읽는 길을 새로 내야 한다.** 지금 카드 화면은 문장을
  저장소의 `data/` 파일에서 찾아온다(`loadCardSentences`). 그런데 대화는 저장소에
  올리지 않기로 했으니 거기 없다. **`talk-` 로 시작하면 기기 안(IndexedDB)에서 찾는
  갈래를 따로 둔다.** 이걸 빼먹으면 카드는 담기는데 열면 빈 화면이 나온다
- 소리가 필요한 모드(듣고 말하기)는 영상이 없으므로 **폰 목소리로 읽어 준다** —
  Daily 문장이 이미 그렇게 하고 있다. 빈칸 채우기는 채점을 안 하므로 가릴 낱말이
  없다. **대화 카드는 그 모드를 빼거나, 교정에서 바뀐 낱말을 가린다**
- 대화는 **`End` 를 눌러야 끝난다.** 자동으로 안 끝낸다 — 하다 말면 요약도 반쪽이다

**조용히 실패하지 않는다 (SPEC 9).** 이 저장소 원칙을 여기에도 잇는다.

| 언제 | 화면에 뭐라고 |
|---|---|
| 인터넷이 끊김 | "인터넷이 끊겼습니다. 이어서 하려면 다시 눌러 주세요." 지금까지 대화는 **안 지운다** |
| 열쇠가 틀림 | "열쇠가 맞지 않습니다. 설정에서 다시 넣어 주세요." + 설정으로 가는 단추 |
| 한도 초과 | "오늘 쓸 수 있는 양을 다 썼습니다." (회사가 내려주는 값을 그대로 보여 준다) |
| 답이 안 옴(30초) | "답이 오지 않습니다. 다시 보낼까요?" + 다시 보내기 단추 |
| 답의 모양이 틀림 | 조용히 한 번 다시 요청한다. 두 번 틀리면 그 턴만 건너뛰고 화면에 남긴다 |

**대화 기록은 어디에.** 기기 안(IndexedDB `talks` 저장소)에만 둔다.
저장소에 올리지 않는다 — 개인 대화다.

**백업(SPEC 10)에는 요약만 넣고 대화 원문은 넣지 않는다 (2026-08-23).**
백업은 기기를 바꿀 때 기록을 옮기는 파일이고, **사람이 손으로 주고받는 물건**이라
열쇠를 뺀 것과 같은 이유가 걸린다. 그런데 다시 읽을 값어치가 있는 것은 요약 쪽이고,
원문은 부피만 크고 제일 민감하다. **요약은 옮겨지고, 대화 원문은 기기를 바꾸면 사라진다.**

#### 대화 요약 (운영자가 꼽은 네 가지 중 4번)

`End` 를 누르면 **그 대화 전체를 한 번 더 보내서** 요약을 받는다.
대화 한 판에 한 번뿐이라 좋은 모델을 써도 부담이 없다.

> **"하루 요약"이 아니라 "대화 요약"이다 (2026-08-23 운영자 결정).**
> 하루에 두 번 하면 요약도 두 개 나온다. 하루치를 합치지 않는다 —
> 대화마다 흐름이 달라서 합치면 뭉개진다.

칸을 정해 둔다.

| 칸 | 무엇 |
|---|---|
| `summary` | 오늘 무슨 얘기를 했는지 두세 줄 |
| `did_well` | 잘한 것 2~3개. **구체적으로** — "좋았어요"가 아니라 어느 문장의 무엇이 |
| `to_fix` | 고칠 것 2~3개. **되풀이된 것 위주** — 한 번 틀린 것 말고 |
| `culture` | 문화적으로 어긋난 대목. 문법은 맞는데 그 자리에서 안 쓰는 말 |
| `words` | 오늘 배운 표현 3~5개. Daily 의 `note` 와 같은 모양 (`**`로 강조) |

- **어디서 보나:** 대화가 끝나면 바로 뜨고, 리포트 화면에 날짜별로 쌓인다
- **어디에 남나:** 기기 안. Daily 의 `plan` 과 같은 방식으로 **공부한 날**에 매단다
- `words` 는 **한 번에 문장카드로 담는 단추**를 둔다

---

#### 매 요청에 무엇을 실어 보내나 — 지시문

> 이 절은 앞선 판(2026-08-22)에 "덤"이라는 이름으로 있었는데 다시 쓰면서 빠졌다.
> **되살린다.** 그냥 덤이 아니라 **캐싱을 걸리게 하는 장치**이기도 하다 (비용 절 참고).

챗에서는 매번 내 수준과 약점을 다시 설명할 수가 없다. **API 면 매 요청마다 자동으로 들어간다.**
그리고 이 저장소에는 **그 재료가 이미 쌓여 있다.**

| 실어 보낼 것 | 어디서 가져오나 |
|---|---|
| 자주 틀리는 낱말 | 오답 기록 (SPEC 4-3). 리포트 화면이 이미 쓰고 있는 것 |
| 요즘 되풀이되는 실수 갈래 | 최근 대화 요약의 `to_fix` 를 모은 것 |
| 최근 배운 표현 | 최근 대화 요약의 `words` + 최근에 담은 문장카드 |
| 요즘 듣고 있는 어휘 | 등록한 영상의 자막에서 (하루 문장이 이미 이렇게 한다) |
| 말투·난이도 기준 | `data/daily/config.json` 의 값을 같이 쓴다 |

- **대화 안에 넣지 않는다.** 대화 밖의 "역할 설명" 칸에 넣는다 — 그래야 100턴이 지나도 안 묻힌다
- **너무 자주 바꾸지 않는다.** 이 부분이 바뀌면 캐싱이 처음부터 다시 걸린다.
  **하루에 한 번만 새로 만들고 그날은 그대로 쓴다**
- 이걸 채우면 지시문이 4,096 토큰을 넘어 **첫 턴부터 캐싱이 걸린다.**
  즉 **크게 만드는 쪽이 오히려 싸다** — 산식은 비용 절에 있다

---

### 열쇠(API key)를 어떻게 다루나

운영자는 코드를 모른다. "폰마다 한 번씩 넣어야 한다"만으로는 실행이 안 된다.

**어디에 넣나.** 라이브러리 → `Settings` → `AI key`. 대화 화면에서 열쇠가 없으면
그 화면으로 보낸다.

**어디에 저장하나.** IndexedDB `settings` 저장소. **프로필별이 아니라 기기별로 하나.**
같은 기기를 가족이 나눠 쓰는 것이 이 앱의 전제인데(SPEC 3),
열쇠를 프로필마다 따로 두면 사람마다 발급받아야 해서 감당이 안 된다.
**기기에 하나를 넣고 온 가족이 같이 쓴다.** 대신 화면에 그 사실을 적는다 —
"이 기기의 모든 프로필이 같은 열쇠를 씁니다."

**넣을 때 확인한다.** 저장하기 전에 짧은 요청을 한 번 보내 본다.
안 되면 저장하지 않고 한국어로 이유를 말한다 (열쇠가 틀림 / 인터넷 / 한도).

**절대 안 하는 것.** 저장소에 올리지 않는다. 백업(SPEC 10)에도 **넣지 않는다** —
백업 파일은 사람이 주고받는 물건이다.

**발급 방법 안내.** `tools/DAILY_SETUP.md` 와 같은 모양으로 `tools/TALK_SETUP.md` 를 쓴다.
화면 보고 따라가는 수준으로. 열쇠가 만료됐을 때 무엇을 다시 하는지도 적는다.

---

### 브라우저에서 직접 부를 수 있나 — 확인한 값 (2026-08-23)

"서버 없음"을 지키려면 **폰이 회사에 직접 말을 걸 수 있어야** 한다.
이게 이 단계 구조의 유일한 근거라 먼저 확인했다.

| 쓰려는 것 | 브라우저에서 직접 | 근거 |
|---|---|---|
| **Anthropic (대화·요약)** | **된다** | `anthropic-dangerous-direct-browser-access: true` 헤더를 붙이면 CORS 가 열린다. 열쇠를 기기에 두는 방식(BYOK)을 위해 만든 기능이다 |
| **Deepgram (받아적기)** | **안 된다** | REST 는 CORS 로 막혀 있고, 실시간 연결은 **30초짜리 임시 표**가 필요한데 그 표를 만들려면 진짜 열쇠를 든 서버가 있어야 한다 |
| **Google Gemini (목소리)** | **확인 필요** | 실시간 쪽은 Deepgram 과 같은 임시 표 방식이다. 한 번에 주고받는 쪽은 확인하지 못했다 |

> **이게 B안을 깬다.** 앞서 "받아적기를 AI 로(연 8.5만 원)"를 권했는데,
> **그건 서버 없이는 안 된다.** 서버를 두면 이 앱의 제일 큰 전제가 무너진다.
>
> 그래서 2단계는 **A안(대화만 AI, 받아적기·목소리는 폰 내장)** 으로 시작한다.
> 대화는 확인된 방식으로 되고, 받아적기는 지금 `record.js` 가 쓰는 폰 기능이 이미 있다.
> 폰 받아적기가 정말 못 쓸 수준이면 그때 다시 생각한다 —
> 그때는 **"작은 서버 하나" · "앱으로 포장" · "정확도 포기"** 셋 중에 고르는 문제가 된다.
> **앱으로 내면 이 막힘이 통째로 없어진다** (CORS 는 브라우저에만 있는 규칙이라서다).
> 그 갈래는 3단계 절의 "브라우저 말고 다른 길" 에 적어 뒀다.

---

### 비용 (2026-08-23 조사)

> **단위는 100만 **토큰**당이다. 글자가 아니다.** 영어는 대략 글자 4개가 토큰 1개다.
> 1년 뒤 다시 계산하려면 이 단위를 맞춰야 한다.

| 역할 | 모델 | 입력 / 출력 (100만 토큰당) |
|---|---|---|
| 대화 | Claude Haiku 4.5 | $1 / $5 |
| 대화 요약 | 아래 표에서 고른다 | — |

**캐싱.** 대화는 앞 내용을 계속 다시 보낸다. 같은 앞부분을 재활용하면
**다시 읽을 때 0.1배**, 처음 넣어 둘 때 1.25배(5분 유지)다.
대화 턴이 5분보다 촘촘하면 계속 살아 있다.

> **캐싱에는 최소 크기가 있다. 이걸 놓치면 계산이 통째로 틀린다 (2026-08-23).**
> 앞부분이 일정 크기를 넘어야만 캐싱이 걸린다. 못 넘으면 **오류도 없이 그냥 안 걸린다.**
> 기준은 모델마다 다르고 **Claude Haiku 4.5 는 4,096 토큰**이다.
> 지시문 800 토큰으로 시작하면 **20번째 턴쯤 가서야 캐싱이 시작되고, 앞 19턴은 제값을 다 낸다.**
>
> **그래서 지시문을 크게 만드는 쪽이 오히려 싸진다.** 위 "매 요청에 무엇을 실어 보내나"
> 절에서 내 약점과 최근 배운 표현을 넣기로 했는데, 그걸 넣으면 지시문이 4,096 을 넘어
> **첫 턴부터 캐싱이 걸린다.** 그 기능은 덤이 아니라 **비용을 맞추는 장치**다.
> 모델을 갈아 끼울 때는 **그 모델의 최소 크기를 먼저 보고 지시문 길이를 맞춘다.**

#### 산식 (검산할 수 있게)

전제: 하루 30분, 주고받기 45번. 내가 한 번에 30단어(≈40토큰),
AI 가 60단어(≈95토큰) + 교정 칸까지 해서 ≈140토큰.

```
캐싱 없이 (입력)
  턴 i 의 입력 = 지시문 + 앞 대화 (i-1)×180 + 내 말 40

가) 지시문이 800 토큰일 때  ← 캐싱이 20번째 턴부터 걸린다
  1~19턴  제값                                    ≈ 46,700 토큰
  20~45턴 넣어 두기 8,700×1.25 + 다시 읽기 160,500×0.1 ≈ 27,000 토큰
  합계                                            ≈ 74,000 토큰

나) 지시문을 4,200 토큰으로 채울 때  ← 첫 턴부터 캐싱이 걸린다
  넣어 두기 = 마지막 프리픽스 12,100 × 1.25       ≈ 15,100 토큰
  다시 읽기 = (369,000 − 12,100) × 0.1            ≈ 35,700 토큰
  합계                                            ≈ 51,000 토큰   ← 가) 보다 싸다

대화 하루치 (나 기준)
  입력  51,000 × $1/1M           ≈ $0.051
  출력  45 × 140 = 6,300 × $5/1M ≈ $0.032
                                   합 ≈ $0.083

대화 요약 (Sonnet 5, 대화 한 판에 한 번)
  입력  대화 전체 ≈ 9,000 × $3/1M  ≈ $0.027
  출력  ≈ 1,300 × $15/1M           ≈ $0.020
                                   합 ≈ $0.047

하루 합계 ≈ $0.13  →  한 달 ≈ $3.9  →  1년 ≈ $47  ≈ 6.6만 원
     (지시문을 안 채우면 ≈ $0.15 → 1년 ≈ $56 ≈ 7.8만 원)
```

#### 요약에 어느 모델을 쓸까 (2026-08-23 조사)

요약은 **대화 한 판에 한 번**이라 모델을 바꿔도 폭이 크지 않다. 깊이가 값어치를 하는 자리다.

| 모델 | 입력 / 출력 (100만 토큰당) | 하루 | **1인 1년** |
|---|---|---|---|
| Gemini 3 Flash Preview | $0.25 / $1.50 | $0.004 | 약 0.2만 원 |
| Gemini 3.7 Flash (도입가) | $0.75 / $3.75 | $0.012 | 약 0.6만 원 |
| Gemini 3.7 Flash (2027-01-01 정가) | $1.50 / $7.50 | $0.023 | 약 1.2만 원 |
| Claude Haiku 4.5 | $1 / $5 | $0.016 | 약 0.8만 원 |
| **Claude Sonnet 5** ← 지금 추천 | $3 / $15 | $0.047 | **약 2.4만 원** |
| Claude Opus 5 | $5 / $25 | $0.078 | 약 4.0만 원 |

- **제일 비싼 것과 제일 싼 것의 차이가 1년에 3.8만 원이다.** 대화 쪽(연 5만 원 안팎)에
  비하면 작다. 여기서 아끼려 애쓸 자리가 아니다
- **Gemini 3.7 Flash 는 2027년 1월 1일에 값이 2배로 오른다고 이미 예고돼 있다.**
  도입가로 계산해 두면 새해에 틀어진다. 위 표에 두 값을 다 적어 둔 이유다
- **Preview 딱지가 붙은 값은 언제든 바뀌거나 없어진다.** 그걸 기준으로 정하지 않는다
- 실제로 어느 것이 나은지는 **써 봐야 안다.** 요약 모델도 설정에서 고를 수 있게 만든다

> **왜 헤드리스(구독)로 공짜로 못 하나 (2026-08-23 운영자 질문).**
> 하루 문장처럼 GitHub 이 새벽에 도는 방식이면 구독 한도로 돌아 0원이다. 그런데
> **그러려면 대화가 저장소에 올라와 있어야 하고, 이 저장소는 공개다.** 방금 위에서
> "개인 대화라 안 올린다"고 정한 것과 정면으로 부딪힌다. (비공개 저장소로 옮기면
> 지금의 무료 배포 조건이 바뀐다 — **확인 필요.**)
> 그리고 타이밍이 안 맞는다. 원하는 것은 "대화 끝나면 바로 뜨는 요약"인데
> 헤드리스는 몇 분 걸리고 다음에 앱을 열 때나 보인다. **요약은 API 로 한다.**

#### 이 값이 무엇을 포함하나 (2026-08-23 운영자 질문)

운영자가 물었다 — **"저 1년 비용이 AI 를 쓰는 모든 것을 다 합친 값이냐."**
아니다. **대화 연습 하나에만 붙는 값이다.** 헷갈리기 쉬우니 적어 둔다.

| 기능 | 값 | 왜 |
|---|---|---|
| 받아쓰기 (Phase 1) | **0원** | 유튜브 자막 + 브라우저. AI 를 안 쓴다 |
| 하루 다섯 문장 | **0원** | GitHub Actions + 운영자 구독 한도 |
| 드라마 대사 갈래 | **0원** | 위와 같은 방식 |
| 문장카드 · 오답 리포트 · 통계 · 백업 | **0원** | 전부 기기 안에서 |
| 영상 등록 | **0원** | PC 로컬 스크립트 |
| 내 말 받아적기 | **0원** | 폰 내장 (AI 로 하려면 서버가 필요해서 뺐다) |
| AI 대답 읽어 주기 | **0원** | 폰 내장 목소리 |
| **대화 연습 (2단계)** | **연 6.6만 원** | ← **여기만 돈이 나간다. 대화와 요약을 합친 값이다** |
| AI 목소리 (3단계) | +4.8만 원 | 아직 브라우저에서 되는지 확인 전이라 확정값이 아니다 |

#### 실제로는 얼마나 나오나

위 6.6만 원은 **한 사람이 하루 30분, 365일 하루도 안 빠졌을 때**다. 실제로는 그렇게 안 된다.

| 얼마나 하나 | 1인 1년 | 가족 셋이 다 |
|---|---|---|
| 매일 30분 (산식 기준) | 6.6만 원 | 20만 원 |
| **주 3회** | **2.8만 원** | 8.5만 원 |
| 주 1회 | 약 1만 원 | 2.8만 원 |
| 한 달 쉬면 그 달 | **0원** | **0원** |

**쓴 만큼만 낸다** — 이게 구독과 다른 점이다.
(Langua 는 1인 연 159달러 ≈ 22만 원이 **안 해도 나간다.** 셋이면 66만 원.)

> 값은 바뀐다. Sonnet 5 는 도입가로 더 싸게 파는 기간이 있었고,
> Gemini 3.7 Flash 는 2027년 1월에 2배가 된다.
> **1년 뒤에는 위 산식에 그때 값을 다시 넣어 계산한다.**

---

### 무엇을 보고 넘어가나 — 주마다 본다 (2026-08-23 운영자 결정)

운영자가 **일주일 단위로 리뷰**한다. 그래서 판정도 주 단위로 쌓는다.
**한 주가 좋았다고 넘어가지 않는다** — 새 기능은 첫 주가 제일 재밌다.
Langua 를 1년 결제해 놓고 안 쓰게 되는 것이 제일 흔한 실패고, 한 주짜리 관찰로는 그걸 못 거른다.

#### 매주 보는 것 — 1단계 (하루 다섯 문장)

| 보는 것 | 통과 |
|---|---|
| 그 주에 Daily 를 연 날 | **4일 이상** |
| 건너뛴 문장 비율 | **3할 미만** (높으면 문장 품질 문제가 먼저다) |
| 그 주에 담은 문장카드 | **3장 이상** (담을 만한 것이 나온다는 뜻) |
| 영상 대사 갈래에서 맥락 없이는 못 쓸 대사 | **5개 중 1개 이하** |
| 운영자가 "챗으로 하는 것보다 낫다"고 느끼나 | **예** ← 제일 중요하다 |

**넘어가는 조건:** 위가 **4주 연속** 통과.
**멈추는 조건: 4주 안에 두 주 이상 절반을 못 채우면 2단계를 만들지 않는다.**
돈을 쓰기 전에 맞는지 확인한다는 것이 이 문서의 논리인데,
그걸 실제로 강제하는 문장은 이것뿐이다. 넘어가는 조건만 있으면 그냥 넘어가게 된다.

**앞의 넷은 앱과 저장소에서 셀 수 있다. 리포트 화면에 그 주 숫자를 띄운다.**

#### 매주 보는 것 — 2단계 (대화 연습)

| 보는 것 | 통과 |
|---|---|
| 대화가 이어진 턴 수 | **10턴 이상** (금방 끊기면 재미가 없다는 뜻) |
| 말하고 답이 올 때까지 | **3초 안** — 모델을 고른 근거가 이것인데 판정에 없었다 |
| 교정이 헛나간 횟수 | **하루 한 번 이하** (아래 설명) |
| 대화 요약을 **다음 날 다시 열어 보나** | **예** — 한 번 읽는 것과 다시 찾는 것은 다르다 |
| 그 주 청구서가 산식 범위 안인가 | **2배 이내** — 넘으면 계산이 틀린 것이고 결정을 다시 해야 한다 |
| 폰 내장 목소리가 거슬려서 못 듣겠나 | **예면 3단계로 간다** |

> **"교정 칸이 채워지나"는 판정 기준이 아니라 버그 조건이다.** 그건 안 되면
> 만들다 만 것이지 성공·실패를 가르는 값이 아니다. 게다가 내 문장이 이미 멀쩡하면
> **"고칠 것 없음"이 들어간다** — 그것도 채워진 것이다.
> 판정해야 할 것은 **교정이 쓸모 있나**다. 운영자가 "이건 안 고쳐도 되는데 고쳤다"
> 또는 "이건 이상한데 그냥 넘어갔다"고 느끼는 횟수를 센다.

## 3단계 — AI 목소리 (연 +4.8만 원)

폰 내장 목소리도 알아듣는 데 지장은 없다. 다만 밋밋하다.

**서두를 이유가 없는 이유:** AI 목소리를 넣으면 답이 1~2초 늦어진다.
글을 만들고 → 소리로 바꾸고 → 재생하는 단계가 붙기 때문이다.
대화 흐름에는 오히려 손해일 수 있다. **2단계를 써 보고 정한다.**

**아낄 구석:** 고쳐 준 문장은 화면에 글로 띄우면 되므로 **소리로 읽을 필요가 없다.**
이것만으로 목소리 비용이 40% 준다. 아래 계산은 이미 반영한 값이다.

> **먼저 확인할 것 — 2단계에서 겪은 것과 같은 함정이다.**
> 받아적기(Deepgram)가 **브라우저에서 직접 못 불러서** 통째로 빠졌다.
> AI 목소리도 실시간 쪽은 **같은 임시표 방식**이라 같은 벽에 부딪힐 수 있다.
> 위 확인표에 Gemini 목소리는 아직 **"확인 필요"** 로 남아 있다.
> **3단계를 시작하기 전에 그 칸부터 채운다.** 아래 C 안(연 10만 원)은
> "브라우저에서 직접 된다"를 전제로 한 값이라, 확인 전에는 확정값이 아니다.

### 브라우저 말고 다른 길 — 앱으로 내면 제약이 풀린다 (2026-08-23 운영자 질문)

운영자가 물었다 — **웹으로 만드는 이상 결국 브라우저인데, 다른 길은 없나.**
그리고 **나중에 재미로 앱처럼 출시하고 싶다**고 했다. 둘은 사실 같은 얘기다.

**막고 있는 규칙(CORS)은 브라우저에만 있다.** 진짜 앱은 그 규칙을 안 탄다.
즉 **앱으로 내면 받아적기도 목소리도 직접 부를 수 있다.**

| 길 | 받아적기·목소리 | 드는 돈 | 깨지는 제약 |
|---|---|---|---|
| **지금** (웹 + 홈 화면에 추가) | 폰 내장만 | 0원 | 없음 |
| **웹 코드를 앱으로 포장** (Capacitor 같은 것) | **직접 부를 수 있다** | 애플 연 $99 (약 14만 원), 구글 1회 $25 | **빌드 단계 없음**이 깨진다 |
| **작은 서버 하나** | 직접 부를 수 있다 | 서버비 + 계속 관리 | **서버 없음**이 깨진다 |

- 앱으로 포장하는 쪽은 **지금 코드를 다시 쓰지 않는다.** 있는 파일을 그대로 감싼다
- 대신 **빌드 단계가 생긴다.** SPEC 2 를 바꾸는 일이라 그때 다시 정한다
- **고친 것이 바로 안 나간다.** 지금은 올리면 1~2분인데, 앱은 심사를 기다린다
- 애플 쪽은 **매년** 나간다. 안 내면 앱이 내려간다 — 구독과 같은 성격이라
  "안 하면 0원"이 여기서는 안 통한다
- **운영자가 "재미로 내고 싶다"고 했으므로 이건 제약을 푸는 덤이 아니라 그 자체가 목적인 갈래다.**
  3단계와 나란히 놓고 본다. 다만 **2단계를 웹으로 먼저 써 본 뒤**다 —
  앱으로 감싸는 것은 나중에 해도 늦지 않고, 웹으로 안 쓰게 되면 앱으로도 안 쓴다

---

## 비용 계산 (2026-08 조사, 1달러 1,400원)

### 전제

한 사람, 하루 30분, 매일. 주고받는 횟수 45번.
내가 한 번에 30단어쯤 말하고, AI 가 60단어쯤 답한다.

**대화는 앞 내용을 계속 다시 보내야 해서 뒤로 갈수록 비싸진다.**
앞부분을 재활용하면(prompt caching) 크게 준다 — **산식은 2단계 절에 적어 뒀다.**

### 역할별 단가

> **AI 모델은 100만 **토큰**당이다. 글자가 아니다** (영어는 대략 글자 4개가 토큰 1개).
> 목소리(TTS)만 글자 단위다. 헷갈리면 1년 뒤 재계산이 안 된다.

| 역할 | 쓰는 것 | 단가 | 단위 |
|---|---|---|---|
| 대화 | Claude Haiku 4.5 | 입력 $1 / 출력 $5 | 100만 **토큰** |
| 대화 요약 | Claude Sonnet 5 | 입력 $3 / 출력 $15 | 100만 **토큰** — 다른 후보는 2단계 절의 비교표 |
| AI 목소리 | Google Gemini Flash TTS | $6 | 100만 **글자** |
| ~~받아적기~~ | ~~Deepgram Nova-3~~ | ~~1분당 $0.0048~~ | **서버가 필요해 뺐다** |

목소리는 회사별로 **10배 차이**가 난다. ElevenLabs 는 100만 글자당 $60~120 이라 제외.

### 1년 합계 (한 명 기준)

| 구성 | 대화 | 받아적기 | 목소리 | **1년** |
|---|---|---|---|---|
| **A. 대화만 AI** ← 2단계 | AI | 폰 내장 | 폰 내장 | **약 5.5만 원** |
| ~~B. + 받아적기 AI~~ | AI | AI | 폰 내장 | ~~8.5만 원~~ **서버가 필요해서 뺐다** |
| C. + 목소리까지 (싼 쪽) ← 3단계 | AI | 폰 내장 | AI (4.8만) | 약 10만 원 |
| E. 최고급 목소리(ElevenLabs) | AI | 폰 내장 | AI | 약 30만 원 이상 |
| **Langua** | — | — | — | **약 22만 원** |

### 숫자를 볼 때 감안할 것

1. **가족 수만큼 곱한다.** 셋이 매일 하면 A 는 17만 원. (Langua 는 셋이면 66만 원)
2. **매일 30분 빠짐없이 하는 기준이다.** 실제로는 그렇게 안 된다.
   **쓴 만큼만 낸다는 게 구독과 다른 점이다** — 두 달 안 하면 그 두 달은 0원
3. **거래처.** A 는 회사 한 곳(Anthropic), C 는 두 곳

## 모델 선택 기준

역할마다 중요한 게 다르다.

| 역할 | 고르는 기준 | 지금 추천 |
|---|---|---|
| 대화 상대 | 지시 준수 · **속도** · 값 | Claude Haiku 4.5 — 대화는 3초 넘으면 흐름이 끊긴다. 똑똑함보다 빠름 |
| 대화 요약 | 깊이 · 뉘앙스 | Claude Sonnet 5 — 대화 한 판에 한 번뿐이라 좋은 걸 써도 부담 없다 |
| 내 말 받아적기 | 서버가 필요한가 | **폰 내장** — AI 로 하려면 서버가 있어야 한다 (2단계 절 참고) |
| AI 목소리 | **값** (10배 차이) | Google Gemini Flash TTS |
| 하루 5문장 | 값 0원 | 기존 구독 (1단계) |

**편향을 밝혀 둔다.** 이 문서를 쓴 것이 Claude 라서 대화 부분에 Claude 를 미는 것이
편향으로 보일 수 있다. 게다가 운영자에게는 **반대 증거**가 있다 —
챗에서 Claude 가 지시를 안 지켰고 Gemini 가 조금 나았다.

그래서 **2단계를 만들 때 대화 모델을 갈아 끼울 수 있게 짠다.** 설정에서 회사를 고른다.
일주일씩 써 보고 운영자가 정한다. 위 추천은 참고값이다.

(다만 "지시를 안 지킨다" 문제 자체는 위에서 적었듯 **모델이 아니라 구조**의 문제라,
어느 회사를 골라도 똑같이 해결된다.)

> **갈아 끼우기는 설정 한 줄이 아니다.** 답의 모양을 강제하는 방식이 회사마다 다르고
> (Anthropic 은 도구 정의, Google 은 응답 스키마), 브라우저에서 직접 부를 수 있는지도
> 회사마다 다르다. **회사별 어댑터를 하나씩 쓰는 일**로 잡아야 한다.
> 회사를 더할 때마다 "브라우저에서 직접 부를 수 있나" 를 먼저 확인하고 표에 적는다.

### 다른 회사·다른 방식은 어떤가 — 알아본 결과 (2026-08-23)

운영자가 물어서 알아봤다. **또 물어볼 질문이라 답을 여기 남긴다.**

| 방식 | 값 | 판단 |
|---|---|---|
| **Gemini 3 Flash Preview** | $0.25 / $1.50 | 제일 싸다. 다만 **Preview 딱지**가 붙은 값은 언제든 바뀌거나 없어진다 |
| **Gemini 3.7 Flash** | $0.75 / $3.75 (도입가) | 싸다. **2027-01-01 에 2배**로 오른다고 이미 예고돼 있다 |
| Claude Haiku 4.5 (지금 추천) | $1 / $5 | 기준값 |
| **오픈 모델을 클라우드 GPU 에 올리기** | GPU 한 장 **월 160만~290만 원** | **비교가 안 된다.** 아래 참고 |
| 오픈 모델을 토큰당 파는 곳 | 100만 토큰당 $0.28~0.88 | 그냥 더 싼 API 다. "클라우드 비용만"이 아니다 |

**대화를 Gemini 로 바꾸면 1인 연 2만 원쯤 아낀다.** 값어치가 있고,
운영자에게 **"챗에서 Gemini 가 더 나았다"는 경험**까지 있으므로 꼭 비교해 볼 만하다.
다만 **브라우저에서 직접 부를 수 있는지가 아직 미확인**이다 — 받아적기가 그래서 깨졌다.
먼저 확인하고 표에 적은 뒤에 붙인다.

> **"오픈 모델을 클라우드에 올리면 클라우드 비용만 내면 되지 않나" (운영자 질문).**
> 그게 성립하려면 GPU 를 빌려 켜 둬야 하는데, **GPU 는 시간당 과금이라 쓰든 안 쓰든 나간다.**
> H100 한 장이 시간당 $2~4, **한 달에 160만~290만 원**이다.
> API 로 1년 쓸 돈(6~8만 원)을 하루 반이면 쓴다.
>
> 손익분기는 대략 **가동률 30%** 또는 **하루 500만 토큰**이다.
> 우리는 하루 30분(가동률 2%), 세 식구가 다 해도 하루 23만 토큰 — **20배 넘게 모자란다.**
> 껐다 켜면 되지 않냐 하면, 그때는 시동에 몇 분 걸린다.
> **이 저장소는 이미 같은 이유로 서버 방식을 한 번 기각한 적이 있다** (SPEC 2, Apps Script 콜드스타트).
>
> **핵심은 이것이다.** 이 앱의 제일 큰 장점은 "안 하면 0원"이다. 시간당 과금은 그 정반대다.

---

## 지금 정하지 않은 것

- ~~대화 기록을 저장소에 남길지~~ → **안 남기기로 정했다** (2026-08-23, 2단계 절).
  자동 난이도 조절은 그래서 기기 안에서 푸는 길을 따로 찾아야 한다
- 발음 점수 (Langua 에 있지만 운영자가 언급하지 않았다)
- 가족 구성원별 난이도 분리 (지금은 프로필이 하나뿐)
