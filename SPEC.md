# SPEC — 구간 재생 (받아쓰기 연습)

유튜브 영상의 특정 구간만 반복해서 듣는 웹 페이지. 받아쓰기(딕테이션) 연습에서
같은 문장을 여러 번 듣는 것이 핵심 동작이므로, "구간을 정확히 잘라 반복 재생하는 것"에
모든 기능을 맞춘다.

주 사용 환경은 **아이폰 사파리**다. 데스크톱은 부수적으로 동작하면 된다.

---

## 1. 영상 지정

사용자는 유튜브 영상 ID 또는 주소를 입력한다. 다음 형태를 모두 허용한다.

| 입력 | 예시 |
| --- | --- |
| 11자리 ID | `dQw4w9WgXcQ` |
| watch 주소 | `https://www.youtube.com/watch?v=dQw4w9WgXcQ` |
| 단축 주소 | `https://youtu.be/dQw4w9WgXcQ` |
| embed 주소 | `https://www.youtube.com/embed/dQw4w9WgXcQ` |
| Shorts | `https://www.youtube.com/shorts/dQw4w9WgXcQ` |
| 라이브 | `https://www.youtube.com/live/dQw4w9WgXcQ` |

어느 쪽으로도 11자리 ID를 뽑아내지 못하면 재생하지 않고 안내 문구를 띄운다.

## 2. 구간 지정

시작 초와 끝 초를 소수점 첫째 자리까지 입력한다 (`step=0.1`).

- 시작 초는 0 이상이어야 한다.
- 끝 초는 시작 초보다 커야 한다.
- 조건을 어기면 재생하지 않고 어느 값이 잘못됐는지 알려 준다.

## 3. 재생

`재생` 버튼을 누르면 지정한 구간을 처음부터 재생한다.
입력한 영상 ID가 현재 로드된 영상과 다르면 새 영상을 먼저 불러온 뒤 재생한다.
불러오는 동안에는 "영상을 불러오는 중…"을 표시하고, 로딩이 끝나면 자동으로 이어서 재생한다.
(로딩 대기는 최대 4초까지만 기다리고, 그 후에는 그대로 재생을 시도한다.)

## 4. 끝 지점 자동 정지

재생 중 현재 시각을 주기적으로 확인해서 끝 지점에 도달하면 스스로 멈춘다.

- 감시 주기는 40ms.
- 감시 주기 때문에 늦게 멈추는 것을 보정하려고 0.04초 여유를 두고 판정한다.
- 멈춘 뒤에는 재생 위치를 끝 지점에 정확히 맞춰 둔다. 다음에 어디서 멈췄는지가 흔들리지 않게 하기 위함이다.
- 사용자가 직접 일시정지하거나 영상이 끝나면 감시를 중단한다.

## 5. 다시 듣기

`다시 듣기` 버튼은 같은 구간을 처음부터 다시 재생한다. 동작은 3번과 같다.
받아쓰기 중 가장 자주 누르는 버튼이므로 항상 한 번의 탭으로 닿아야 한다.

## 6. 시작점 여유 (lead-in)

**재생은 입력한 시작 초보다 0.3초 앞에서 시작한다.**

정확히 시작 초에서 재생을 시작하면 첫 단어의 앞부분이 잘려서 들린다.
사용자가 입력하는 값은 "이 말이 시작되는 지점"이지 "재생을 걸 지점"이 아니다.
따라서 실제 seek 위치는 `max(0, 시작 초 - 0.3)`이다. 끝 지점에는 이런 여유를 두지 않는다.

이 0.3초는 화면에도 안내한다.

## 7. 배속

`0.75배속` 버튼은 켜짐/꺼짐을 오가는 토글이다.

- 켜지면 버튼이 강조되고 문구가 `0.75배속 켜짐`으로 바뀐다.
- 재생 중에 눌러도 즉시 반영된다.
- 다음 재생에도 설정이 유지된다.

## 8. 상태 표시

화면 아래에 한 줄짜리 상태 문구를 항상 둔다. 최소 한 가지는 늘 표시된다.

- 로딩 중 / 준비 완료
- 재생 중 (실제 시작 초 ~ 끝 초, 배속 켜짐 여부)
- 구간 재생 완료 (정지한 초)
- 입력값 오류 안내
- 재생 실패 안내

## 9. 오류 처리

- 플레이어 준비 전에 버튼을 누르면 "잠시 후 다시" 안내만 하고 아무 일도 하지 않는다.
- 유튜브가 재생 오류를 알리면 감시를 멈추고 영상 ID를 확인하라고 안내한다.
- 어떤 경우에도 조용히 실패하지 않는다.

## 10. 모바일 대응

- 플레이어는 `playsinline`으로 띄운다. 아이폰에서 전체화면으로 튀면 입력창이 가려져 연습이 끊긴다.
- 입력창 글꼴은 16px 이상. 그 미만이면 iOS가 포커스 시 화면을 자동 확대한다.
- 버튼 높이는 52px 이상, 세로 화면에서는 버튼을 한 줄에 하나씩 배치한다.
- 시작/끝 초 입력은 좁은 화면에서도 나란히 둔다.
- 하단 여백에 `env(safe-area-inset-bottom)`을 더한다.
- 영상 영역은 16:9 비율을 유지한다.

## 범위 밖

지금 버전에서 다루지 않는 것:

- 구간 목록 저장, 여러 구간 관리
- 자동 반복(N회 반복 재생)
- 받아쓰기 입력창과 정답 비교
- 자막/스크립트 표시
- 설정 값의 영구 저장
