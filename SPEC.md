# 가족용 영어 받아쓰기 연습 앱 — 개발 스펙 v1.0

> 이 문서는 Claude Code에 그대로 전달하기 위한 스펙이다.
> 사용자(운영자)는 코드를 읽거나 쓰지 않는다. 모든 안내는 "설치 → 실행 → 확인" 수준으로 제공할 것.
> 이미 동작하는 코드를 수정할 때는 한 번에 한 가지만 바꾸고 확인 후 다음으로 넘어갈 것.

---

## 1. 목표

dictation 방식의 문장 단위 받아쓰기 연습을, 사용자가 고른 아무 유튜브 영상으로 할 수 있는 개인·가족용 무료 웹앱.

- 비용 0원 (유료 API, 유료 서버 없음)
- 가족 구성원 여러 명이 각자 프로필로 사용
- PC(Windows, Chrome), 갤럭시(Chrome), 아이폰(Safari/chrome 엔진)에서 동작

## 2. 확정된 아키텍처 결정사항

| 항목 | 결정 | 이유 |
|---|---|---|
| 호스팅 | GitHub Pages (정적 사이트) | 무료, https(마이크 필요), 딜레이 없음 |
| 백엔드 서버 | **없음** | 필요 없음. Apps Script는 콜드스타트 딜레이로 기각됨 |
| 문장 데이터 보관 | 저장소 내 JSON 파일 (앱과 같은 repo) | CORS 원천 차단 불가, 반영 즉시 |
| 개인 진도/기록 | 브라우저 IndexedDB (프로필별 키 분리) | 기기별 독립, 서버 불필요. localStorage 아님 — 용량 문제 |
| 영상 등록 | **PC 로컬 스크립트** (yt-dlp) | 유튜브가 클라우드 서버 IP를 차단하므로 서버 방식은 불안정. 집 IP는 안정적 |
| 영상 재생 | YouTube IFrame Player API | 무료, 키 불필요. 영상 데이터는 일절 보관하지 않음 (구간 재생 명령만) |
| 인증/로그인 | **없음** | 프로필 선택만. 비밀번호 없음 |
| 녹음 | 메모리에만 유지, 저장 안 함 | 문장 넘어가면 폐기 |
| 하루 연습 문장 생성 | **GitHub Actions 가 매일 Claude Code CLI 를 돌려 저장소에 커밋** | 실시간이 아니므로 미리 만들면 된다. 운영자 구독 한도로 도므로 **비용 0원 · 유료 API 없음**이 유지된다 |

## 3. 사용자 모델

- 프로필: 이름 + 색상/이모지 정도의 단순 구조. 앱 첫 화면에서 선택 (넷플릭스 방식)
- **영상 라이브러리는 프로필별로 분리** — 각자 자기 영상만 보임 (전체 보기 옵션은 있어도 됨)
- 문장카드(복습 저장), 오답 기록, 진도, 통계 모두 프로필별 분리
- 같은 기기를 두 사람이 쓸 수 있음 → IndexedDB 키에 프로필 ID 포함

## 4. 데이터 구조

### 4-1. 저장소 내 정적 데이터 (전 기기 공유, 등록 스크립트가 생성)

```
/data/
  profiles.json          # [{id, name, color}]
  videos/
    {profileId}/
      index.json         # 그 사람의 영상 목록 [{videoId, title, addedAt, sentenceCount, hasKorean, source}]
      {videoId}.json     # 문장 데이터
```

`{videoId}.json` 형태:

```json
{
  "videoId": "abc123",
  "title": "...",
  "source": "manual_captions | auto_captions",
  "sentences": [
    {
      "i": 0,
      "start": 12.4,
      "end": 17.8,
      "text": "영어 문장.",
      "ko": "한국어 번역 (있을 때만)",
      "recording": null
    }
  ]
}
```

- `recording` 필드는 향후 확장용 자리만 확보. Phase 1에서는 항상 null.
- `index.json` 의 `source` 는 8번의 "경계가 부정확할 수 있음" 표시를 영상 목록에서 띄우기 위한 것이다. 없으면 영상 파일을 전부 열어봐야 한다.
- 구간 재생 시 시작점에 lead-in 여유를 둔다 (유튜브 탐색이 정밀하지 않고, 자동자막은 문장 시작을 늦게 잡는다). 듣기 화면 슬라이더로 0~2.0초를 0.1초 단위로 맞춘다. 기본값 0.6초. 문장 목록 위에 둔다.
- 끝점에도 `end + 0.3초` 여유를 둔다. 자동자막의 단어 시각이 정밀하지 않아 마지막 단어가 잘린다. 이 값도 설정으로 조절 가능하게.
- 유튜브 플레이어의 눈금과 시간 표시는 감춘다(`controls: 0`). 재생 위치가 보이면 듣기에 방해가 되고, 조작은 앱 버튼으로만 한다.
- 유튜브 자막(CC)은 꺼야 한다. 답이 화면에 그대로 보이기 때문이다. `cc_load_policy: 0` 만으로는 확실하지 않아 재생기가 준비될 때·재생이 시작될 때·영상을 갈아 낄 때마다 `unloadModule` 로 끈다. `setOption` 으로 끄려 하면 오히려 켜진다. 조작 막대를 감췄으므로 사용자가 손으로 끌 방법이 없다 — 앱이 책임진다.
- **화면을 덮고 소리만 듣는 선택지를 둔다.** 어떤 영상은 번역 자막이 그림 자체에 새겨져 있어 어떤 설정으로도 못 가린다. 받아쓰기에는 소리만 있으면 된다. 주 기능이 아니므로 설정 줄에 작은 토글로 둔다.
- **이전/다음 문장 버튼은 화면 아래에 고정한다.** 연습 중 가장 자주 누르는 버튼인데 화면을 움직일 때마다 찾아 헤매게 된다.

### 4-1-b. 하루 연습 문장 (ROADMAP 1단계)

`data/daily/config.json` — 난이도와 상황 목록. 사람이 고친다.
`data/daily/{프로필}/sets.json` — `[{id, count, madeAt}]`, 만든 순서.
`data/daily/{프로필}/sets/{번호}.json` — 만든 문장 묶음. **영상 파일과 같은 모양**으로 만든다.
`data/daily/{프로필}/index.json` — 영상 대사 갈래의 날짜 목록 `[{date, hasShows, showsCount}]`.
`data/daily/{프로필}/{날짜}-shows.json` — 그날 뽑은 영상 대사.
`data/daily/{프로필}/{날짜}.json` — **옛 모양.** 새로 만들지 않지만 계속 읽는다 —
그때 담아 둔 문장카드가 이걸 가리킨다.
그래야 문장카드가 영상 문장과 하루 문장을 구분 없이 담을 수 있다.

```
{ "videoId": "daily-2026-08-23", "title": "...", "date": "2026-08-23", "source": "daily",
  "sentences": [ { "i":0, "ko":"...", "text":"정답",
                   "alts":[{"style":"casual","text":"..."},{"style":"formal","text":"..."}],
                   "note":"설명. 핵심 표현은 **로 감싼다", "situation":"상황",
                   "start":null, "end":null, "recording":null } ] }
```

- `videoId` 가 `daily-s001` 이면 묶음 파일, `daily-{날짜}` 면 옛 날짜 파일,
  `shows-{날짜}` 면 영상 대사 파일을 가리킨다. 진도·오답·카드 열쇠도 이 값을 쓴다
- `alts` 는 **같은 말을 말투를 바꿔 하는 법**이다. 채점에는 쓰지 않는다.
  정확히 두 개 — `casual`(더 편한 말투) 하나와 `formal`(격식 있는 말투) 하나.
  화면에는 딱지를 붙여 보여 준다
- `note` 에서 **로 감싼 대목은 화면에서 강조된다.** 배울 표현이 눈에 띄어야 하므로
  최소 하나는 감싸야 한다 — 없으면 그날 파일을 쓰지 않는다
- **만든 것을 한 번 더 읽히고 내보낸다.** 만드는 호출 다음에 검수 호출이 한 번 더 간다 —
  관용구 오용, 캐주얼이 정답과 겹치는 것, 포멀이 뜻을 바꾼 것, 번역투 한국어를 잡아 고친다.
  고친 내용은 그날 파일의 `reviewed` 에 남는다 (앱은 안 읽는다).
  검수본이 조건에 안 맞으면 초안이라도 되는지 보고, 그것도 안 되면 다시 만든다
- 문장이 조건에 맞지 않으면 **그날 파일을 아예 쓰지 않는다.** 반쯤 망가진 것을 넣느니 건너뛴다.
  기계로 잡는 조건: 단어 수, 캐주얼/포멀 각 하나씩, `note` 의 강조가
  **실제로 문장에 나오는 표현인지**, 캐주얼이 정답과 앞 네 낱말이 겹치지 않는지
- 며칠째 새 문장이 안 들어오면 앱 화면에 알린다 (조용히 실패하지 않는다)

### 4-2. IndexedDB (기기별·프로필별)

- 진도: 영상별 마지막 문장 위치, 문장별 시도/정답 여부
- 오답 기록: 틀린 단어 목록 (단어, 문장 참조, 날짜) → 오답 리포트 재료
- 문장카드: 저장한 문장 참조 + 저장 사유(수동/자동)
- 일별 학습량: 하루에 채점한 문장 수. 연속 학습 일수는 여기서 센다

### 4-3. 오답 리포트 / 통계 화면

홈의 `Report` 로 들어간다. 한 화면에 넷을 담는다.

- **연속 학습 일수** — 오늘부터 거꾸로 이어지는 날 수. 오늘 아직 안 했으면 어제까지 인정한다
- **최근 14일 학습량** — 하루에 채점한 문장 수를 막대로
- **주간 판정표** — 아래 4-3-b
- **자주 틀리는 낱말** — 많이 틀린 순. 낱말을 누르면 그 낱말을 마지막으로 틀린 문장으로 바로 간다

목록이 길어지면 위 50개만 보여 준다. 자주 틀리는 것을 보자는 화면이므로 꼬리는 도움이 안 된다.

### 4-3-b. 주간 판정표 (ROADMAP "매주 보는 것 — 1단계")

운영자가 **주마다 리뷰**해서 2단계(대화 연습)로 넘어갈지 정한다. 그 판단에 쓸 숫자를
최근 네 주치로 보여 준다. **기록에서 셀 수 있는 셋만 센다.**

| 보는 것 | 어디서 세나 | 통과 |
|---|---|---|
| 그 주에 Daily 를 연 날 | `plan` (묶음이 배정된 날) | 4일 이상 |
| 건너뛴 문장 비율 | `progress` 의 `skip` — **그 주에 배정된 묶음만** | 3할 미만 |
| 그 주에 담은 문장카드 | `cards` 의 날짜 | 3장 이상 |

- 표 아래에 **몇 주 연속 통과인지**를 적고, 4주 연속이면 넘어가도 된다고 알린다
- 판정한 네 주 중 **두 주 이상이 세 항목 중 절반을 못 채우면** 멈추라고 알린다
- **나머지 두 항목(대사가 맥락 없이 쓸 만한가, 챗보다 나은가)은 세지 않는다.**
  사람이 보고 판단하는 것이라 화면에 물음만 적어 둔다

지켜야 할 것 넷:

- **주는 오늘부터 거꾸로 7일씩 끊는다.** 달력 주(월~일)로 끊으면 화요일에는
  "4일 이상"이 될 수가 없어서 매번 미달로 보인다
- **문장에는 날짜가 없다.** `progress` 는 문장별 시각을 남기지 않으므로
  건너뛴 비율은 **그 주에 배정된 묶음**을 통해 센다
- **아직 시작도 안 한 주를 미달로 세지 않는다.** 첫 기록보다 앞선 주는 판정에서 뺀다 —
  안 그러면 쓰기 시작한 첫 주에 바로 "멈춰라"가 뜬다
- **기록이 아예 없으면 표를 그리지 않는다.** 갓 깐 기기에서 빈 표 네 줄은 군더더기다

셈은 `node tools/test_gate.js` 로 확인한다.

## 5. 화면 흐름

```
프로필 선택 → 홈 (무엇을 할지 고른다)
                ├ Dictation → 영상 목록 → 연습 화면 → 편집 화면 (보정용, 평소엔 안 씀)
                ├ Daily     → 하루 다섯 문장 (한→영 옮겨 말하기)
                ├ Shows     → 영상에서 뽑은 실제 대사
                ├ Talk      → 대화 연습 (SPEC 10-c)
                ├ Cards     → 문장카드 복습
                ├ Report    → 통계 / 오답 / 주간 판정표 / 백업
                └ Settings  → AI 열쇠
```

**첫 화면은 영상 목록이 아니라 고르는 화면이다 (2026-08-23 운영자 결정).**
예전에는 프로필을 고르면 곧바로 영상 목록이 나왔다. 받아쓰기 말고도 갈래가 셋이 되면서
목록 하나가 첫 화면을 다 먹고, 나머지 갈래는 머리말의 작은 단추 다섯 개로 밀려나 있었다.
운영자가 "지저분하다"고 한 자리가 여기다. **갈래를 먼저 고르고, 영상 목록은
받아쓰기 안(`#/{프로필}/videos`)으로 들어간다.**

- 갈래마다 **오늘 어디까지 했는지**를 칸에 한 줄로 적는다 (`Today · 2 of 5`, `11 videos`).
  그 값은 **화면을 그린 뒤에 채운다** — 자료를 기다렸다가 그리면 단추가 늦게 뜬다.
  자리는 미리 잡아 두어야 값이 들어올 때 화면이 흔들리지 않는다
- **못 하는 갈래도 감추지 않는다.** 열쇠가 없으면 `Talk` 칸에 "Settings 에서 열쇠를 먼저 넣으세요"
  라고 적어 둔다. 단추를 없애 버리면 왜 없는지 알 길이 없다 (SPEC 9)
- 머리말 밑 한 줄에 연속 학습 일수와 오늘 한 개수를 적는다. 둘 다 없는 날에는 줄이 사라진다
- 갈래가 늘어나도 이 화면에 한 칸을 더하는 것으로 끝나야 한다. 머리말에 단추를 더하지 않는다

## 6. 연습 화면 — 핵심 루프 (문장당 5단계)

1. **리스닝** — 해당 구간만 재생. 반복 재생, 0.75배속 지원.
   `Repeat` 를 켜면 **그 문장만 계속 되풀이한다** — 끌 때까지. 귀에 붙을 때까지 듣는 연습이다.
   끄면 지금 도는 한 바퀴를 끝까지 들려주고 멈춘다. 도는 중에 뚝 끊지 않는다.
   되풀이는 **멈췄다 다시 트는 것이 아니라 자리만 되돌린다.** 멈추면 유튜브가 영상 위에
   공유 단추를 띄우고 가림막이 깜빡여서 흐름이 끊긴다. 자리를 되돌리는 동안의 멈춤을
   사용자가 멈춘 것으로 보지 않게 시작 시각도 함께 되돌린다
2. **타이핑** — 들은 것을 입력
3. **정답 확인** — 단어 단위 비교, 틀린 단어만 표시. 오답 단어는 자동으로 오답 기록에 적재.
   문장카드는 **자동으로 담지 않는다** — 담을 문장은 사람이 고른다 (운영자 결정으로 바뀐 부분)
4. **따라 말하기** — 마이크 녹음 (메모리에만). 지원 기기에서는 음성인식(Web Speech API)으로 받아적어 정답과 비교 표시
5. **녹음 듣기** — 원본 ↔ 내 녹음 번갈아 듣기, 재녹음 가능 → 다음 문장

### 채점 규칙
- 기본: 관대 모드 — 대소문자, 문장부호, 축약형(it's/it is), 붙임표(short-term/short term) 차이는 정답 처리
- 설정에서 엄격 모드 전환 가능 (설정 화면이 생기기 전까지는 연습 화면에 토글을 둔다)
- 축약형은 **애매하지 않은 것만** 같게 본다. `Bill's` 같은 소유격은 `Bill is` 로 풀지 않는다
- 낱말 비교는 자리를 하나씩 맞추지 않고 **가장 길게 겹치도록 맞춰 본다.** 낱말 하나를 빠뜨렸을 때 그 뒤가 전부 오답이 되면 안 된다
- 문장 글자는 **채점하거나 정답을 보기 전까지 감춘다.** 보이면 받아쓰기가 아니다

### 힌트 단계 (정답 보기 전)
0.75배속 재듣기 → 각 단어 첫 글자 → 각 단어 앞 절반 → 정답 공개

단어 개수만 알려 주는 단계는 두지 않는다. 첫 글자를 보여 주면 개수도 함께 보인다.

```
T–––– y–– s– m––– f–– j–––––– u– o– t––– S–––––.
Tha–– yo– s– mu–– fo– join––– u– o– th–– Sun–––.
Thank you so much for joining us on this Sunday.
```

### 단축키 (PC)
입력칸에 글을 치는 중에도 눌리는 조합으로 정했다.

| 키 | 하는 일 |
|---|---|
| `Enter` | 채점. 채점한 뒤 다시 누르면 다음 문장 |
| `Shift+Enter` | 다시 듣기 |
| `Alt+H` | 힌트 한 단계 |
| `Alt+S` | 느리게(0.75배속) 듣기 |

넓은 화면에서는 이 목록을 화면에도 적어 둔다.

## 6-2. 하루 다섯 문장 — 옮겨 말하기 (ROADMAP 1단계)

대화가 하기 싫은 날을 위한 혼자 연습. 매일 아침 새 문장 다섯 개가 준비된다.

1. 한국어 문장과 상황을 본다
2. 영어로 **말한다**. 음성인식이 되면 받아적은 것을 화면에 보여 준다
3. `Show answer` — 정답 · 다르게 말하는 법(캐주얼/포멀) · 설명이 나온다.
   `Listen` 으로 폰 목소리가 읽어 준다 (되는 기기에서만)
4. `Save card` 로 문장카드에 담는다 — 한국어가 있으므로 한→영 모드가 바로 된다
5. `Skip` — 하기 싫은 문장은 건너뛴다. 다섯 개 단위에 묶이지 않는다.
   이 묶음을 다 봤으면 안 쓴 묶음을 하나 더 그 날에 배정한다.
   건너뛴 것은 `skip` 으로 남고 리포트에서 모아 볼 수 있다 — 없어지는 게 아니라 미뤄 둔 것이다

**문장 묶음은 날짜에 묶여 있지 않다 (운영자 결정).**
만드는 쪽은 `data/daily/{프로필}/sets/{번호}.json` 에 번호만 붙여 쌓아 둔다.
앱이 사람이 공부하러 온 날에 안 쓴 묶음을 하나 꺼내 배정하고,
그 짝(공부한 날 ↔ 묶음 번호)을 IndexedDB `plan` 에 남긴다.

- 며칠 안 해도 묶음이 버려지지 않는다. 만든 날이 아니라 **실제로 한 날**이 기록된다
- 화면 위 날짜 줄에서 지난 공부한 날을 골라 그때 한 문장을 다시 볼 수 있다
- 묶음 번호는 한번 매기면 바뀌지 않는다 — **문장카드가 이 번호를 가리킨다**
- 예전 날짜 파일(`{날짜}.json`)도 그대로 읽힌다. 그때 담아 둔 카드가 어긋나면 안 된다

**채점하지 않는다 (운영자 결정).** 이건 옮겨 말하기 연습이라 정답이 하나가 아니고,
글자로 맞히는 것이 목적이 아니다. 점수도 오답 표시도 없다.
진도는 "봤다"만 남긴다 — 연속 학습 일수와 학습량 그래프의 재료다.

난이도는 `data/daily/config.json` 에서 정한다. 기본은 12~28 단어.
**난이도는 길이가 아니라 내용에서 나온다** — 가정·조건·이유처럼 어른이 실제로 겪는
복잡한 사정을 담는다. **쉬운 내용은 이 연습의 목적에 어긋난다.** 다만 길이로 난이도를
만들려 하면 안 된다. 길이 하한을 20 단어로 두었더니 할 말이 끝난 뒤에도 한 마디를
더 붙이게 되어, 그 붙인 자리가 어색해졌다 (아래 참조).

**어떤 모델로 돌릴지도 `config.json` 에서 정한다** (`model`). 못 박아 두어야
어느 날 문장이 달라졌을 때 모델 탓인지 지시문 탓인지 갈라 볼 수 있다.
실제로 답한 모델은 실행 기록에 남는다. 못 박은 모델을 못 쓰면 기본 모델로 돌되
그 사실을 기록에 적는다 — 조용히 넘어가지 않는다.

**만드는 순서가 정해져 있다 — 영어를 먼저 쓴다.** 한국어를 먼저 쓰고 옮기면 번역체가 된다.
목표는 번역을 잘하는 것이 아니라 **영어가 자연스러운 것**이고, 한국어는 그 느낌을 담는 쪽이다.
1:1로 안 맞아도 된다.

**자연스러움이 다른 모든 조건보다 먼저다.** 단어 수는 넘지 말라는 울타리지
채워야 하는 양이 아니다. 할 말이 끝났으면 거기서 끝낸다 — **한 문장으로 끝나도 된다.**
12~28 단어는 한 문장이 아니라 전체 기준이다.
**다섯 개의 길이를 서로 다르게 한다** — 둘은 20 단어 이하여야 한다. 길이는 셀 수 있으므로
이건 기계가 본다. 상한만 두었더니 모델이 늘 위쪽에 붙어 다섯 개가 다 비슷해졌다.
쉼표로 계속 이으면 말이 아니라 글이 된다 — 한 문장에 쉼표 셋을 넘으면 나눈다.

`that`/`it`/`this` 는 **무엇을 가리키는지 낱말 하나로 짚을 수 있어야 한다** —
앞 문장 전체를 `that` 하나로 받으면 무엇을 말하는지 흐려진다.

**자연스러운지는 글자 모양으로 걸러내지 않는다.** 기계가 보는 것은 셀 수 있는 것뿐이다 —
단어 수, 중복, 쉼표 수, 강조한 표현이 문장에 실제로 나오는지. 어떤 표현이 어색한지는
그 자리와 시제와 앞말에 달려 있어서 글자로는 갈라지지 않는다.

**한국에만 있는 것을 영어로 옮기지 않는다.** 상황은 무슨 이야기를 할지 정해 줄 뿐이고,
영어권에 없는 제도나 관습을 영어 낱말로 바꿔 놓으면 안 된다. 가늠하는 법은 하나다 —
**한국을 모르는 사람이 그 영어만 읽어도 통하는가.** 그래서 상황 목록에도 영어권에 있는 것만 둔다.
**돈은 달러로 쓴다.** 한국어의 숫자도 같이 달러로 적는다.

**`sets.json`(앱이 나눠 줄 목록)과 `sets/`(만들어 둔 전부)는 다르다.**
옛 묶음을 그만 내보낼 때는 목록에서만 뺀다. 파일은 남겨 두어야 담아 둔 문장카드와
지난 공부 날짜가 계속 열린다. 번호 매기기와 겹침 피하기는 파일을 본다.

**최근에 쓴 상황과 문장은 묶음(`sets/`)에서 읽어 피한다.** 하루에 여러 묶음을 만들므로
날짜가 아니라 묶음 개수로 센다. **상황(`avoidRecentSets`)보다 내용(`avoidRecentTexts`)을
더 멀리 본다** — 상황은 며칠이면 한 바퀴 돌지만 같은 이야기가 다시 나오면 안 된다.

**영어 만들고 → 소리내어 두 번 읽고 → 말투 바꾸고 → 한국어 붙이고 → 검수한다. 여섯 번 부른다.**
**앞의 네 단계는 영어만 다룬다.** 영어와 한국어를 한 호출에서 같이 만들면 모델이
옮기기 좋은 영어를 쓰게 된다 — 글로 "영어 먼저" 라고 적는 것으로는 안 막힌다.
한국어는 영어가 확정된 뒤 제일 마지막에 옮기고, 거기서 영어는 고칠 수 없다.
**문장을 좋게 만드는 것은 리뷰지 규칙이 아니다.** 규칙은 울타리로만 쓴다.
영어는 **두 번** 읽힌다 — 한 번만 부르면 그냥 통과시키는 일이 잦다.
소리내어 읽는 자리에는 **영어만 보여 준다.** 한국어와 상황을 같이 주면 "번역이 맞느냐"를
보게 되어 어색한 영어가 그대로 통과한다. 견줄 것을 없애야 남는 질문이 하나가 된다 —
사람이 실제로 이렇게 말하나. 걸릴 데가 없으면 그대로 둔다.
검수는 그 다음이고, 거기서는 영어를 다시 손대지 않고 한국어·alts·note 를 영어에 맞춘다.

**구체적으로 쓴다.** `it`/`this`/`that` 으로 얼버무리면 영어가 대명사 투성이가 된다.
한국어가 애매하면 영어도 따라서 애매해지므로 한국어부터 구체적으로 쓴다.

**말버릇을 장식으로 넣지 않는다.** 뜻이 맞을 때만 쓴다.
(`이상하게`는 `honestly` 가 아니라 `weirdly` 다.)

격식체와 비즈니스 영어는 쓰지 않는다. 축약형을 쓴다.

문장을 만들 때 저장소에 있는 영상 자막에서 어휘를 끌어온다 — 듣고 있는 낱말을 말해 보게 하려는 것이다.

**자동 난이도 조절은 아직 없다.** 채점 결과가 폰의 IndexedDB 에만 있고 저장소로 오지 않기 때문이다.
ROADMAP 참고.

### 6-2-b. 드라마·영화 대사 갈래

Daily 화면은 탭 두 개다.

- **Made for you** — AI 가 만든 문장 (위 6-2)
- **From your videos** — 자막에서 가져온 **실제 대사**

두 번째 갈래는 **영어를 만들지 않는다.** AI 가 영어를 지어내면 아무리 조여도 어색한 것이
섞이기 때문이다. 실제로 사람이 한 말을 그대로 쓰고, AI 는 한국어·설명·말투 바꾸기만 붙인다.
**영어를 한 글자라도 바꾸면 저장하지 않는다.**

재료는 두 곳에서 온다.

1. `data/shows/{프로필}/pool.json` — `tools/add_subs.py` 가 PC 의 자막 파일에서 뽑아 올린다
2. 없으면 등록한 영상 자막에서 뽑는다 (시각이 있어 그 대목을 들어 볼 수 있다)

**자막 파일 전체를 저장소에 넣지 않는다.** 그건 재배포다. 300줄쯤만 뽑아 올린다 —
교재가 문장을 인용하는 정도다. 원본은 PC 에 남는다.

고르기는 두 단계다. 규칙은 길이와 문장부호밖에 못 보므로 뜻은 AI 가 본다.

1. **규칙** — 문장부호로 끝나고 6~30 단어. 노래·소리 설명·화자 이름표를 뺀다.
   자동 생성 자막과 한국어 자막은 파일째 거른다
2. **AI** — 후보 30개 중 5개만 고른다. 이런 것은 버린다:
   맥락 없이는 뜻이 안 통하는 말 / 그 작품에서만 통하는 말 / 일상에 못 쓰는 말 /
   **비속어·성적인 내용(가족용 앱이다)** / 배울 것 없는 뻔한 말.
   **다섯 개를 못 채우면 채우지 않는다**

#### 화면 흐름 — 장면을 먼저 본다

이 갈래에만 있는 것 둘이다. 지어낸 문장 쪽(`Daily`)에는 없다.

1. **장면 재생기** — 그 대사가 나온 자리를 화면 안에서 바로 튼다. 정답을 펴기 전에 볼 수 있다.
   상황을 보고 나서 옮기는 것이 순서다 — 한국어만 덩그러니 보면 배우가 한 말이 아니라
   내 식대로 지은 문장이 나온다.
   - **앞 한 마디 + 연습하는 대사 + 뒤 한 마디**를 이어서 튼다. 그 줄만 틀면 상황을 알 수 없다.
     앞뒤가 `Yeah.` 같은 토막이면 건너뛰고 말이 되는 줄까지 간다
   - `This line only` 로 그 대사만 다시 들을 수 있다
   - `Repeat` 를 켜면 방금 튼 것(장면이든 그 대사든)을 멈출 때까지 되풀이한다
   - **저절로 재생하지 않는다.** 한국어만 보고 하고 싶은 날에는 그냥 지나칠 수 있어야 한다
   - 연습 화면 재생기와 **따로 만든다.** iframe 은 화면 사이를 옮기면 다시 읽히므로
     하나를 두 화면이 나눠 쓸 수 없다. 다만 여기서도 재생기는 한 번만 만들고
     영상은 `cueVideoById` 로 갈아 끼운다
   - 같은 문장을 다시 그릴 때(정답 펴기 등)는 재생기를 건드리지 않는다.
     보고 있던 장면이 멈춰 버리면 안 된다
2. **영어로 쳐 보는 칸** — 대사는 배우가 실제로 한 말이라 **정답이 하나다.** 그래서 채점한다.
   받아쓰기 화면과 같은 채점기를 쓰고, 틀린 낱말은 오답 리포트로 간다.
   지어낸 문장 쪽은 정답이 여럿이라 그대로 채점하지 않는다 (운영자 결정)
3. **한국어를 감춰 둔다** (2026-08-26 운영자 요청). 누를 때까지 안 보인다.

   지어낸 문장 쪽에서 한국어는 **문제**다 — 그걸 보고 영어로 옮기는 것이라
   감추면 할 일이 없어진다. 그래서 거기서는 그대로 펴 둔다.

   대사 쪽은 다르다. **장면을 보고 들은 것을 영어로 쳐 보는** 것이라, 한국어는
   문제가 아니라 **막혔을 때 보는 힌트**다. 펴 놓으면 눈이 먼저 가서
   듣기 전에 뜻을 알아 버린다.

   - 자리에는 `Show the Korean` 이 앉아 있다. **한국어 상자와 같은 자리에 같은 크기로**
     둬서, 폈을 때 화면이 튀지 않게 한다
   - 그 밑에 왜 감춰 뒀는지 한 줄 — *"장면부터 해 보고, 필요할 때만 누르세요"*
   - **문장이 넘어가면 도로 감긴다.** 앞 문장에서 폈다고 다음 문장까지 펴져 있으면
     감춘 뜻이 없다

화면 순서: 장면 → 한국어(감춰 둠) → 말해 보기 → 써 보기 → 정답.

**순서에 번호를 붙여 화면에 적는다.** 무엇부터 할지 알아야 흐름대로 하게 된다.
번호는 그 자리에 붙인다 — 따로 도움말 상자를 두지 않는다. 매일 여는 화면에서
접었다 폈다 하게 만들면 결국 안 보게 된다.

| 갈래 | 순서 |
|---|---|
| `Shows` | 1 Listen to the scene · 2 Check the meaning in Korean *(눌러야 보인다)* · 3 Say it in English · 4 Write it in English · 5 Check the answer |
| `Daily` | 1 Read the Korean · 2 Say it in English · 3 Check the answer |

마이크가 없는 기기에서는 말하기 칸이 빠지므로 **번호는 그때그때 다시 매긴다.**
빠진 자리에 번호가 비어 있으면 안 된다.

## 7. 문장카드 복습 모드

- 카드 적재: **수동 저장만.** 연습 화면의 `Save card` 로 담고, 카드 화면의 `Remove card` 로 뺀다.
  틀렸다고 자동으로 담지 않는다 — 처음에는 자동 적재도 넣었으나 운영자가 필요 없다고 정했다.
  틀린 낱말 기록은 그대로 남는다. 그건 오답 리포트의 재료다
- 복습 모드 4종:
  1. **한→영**: 한국어 보고 영어로 말하기 (`ko` 있는 문장만)
  2. **듣고 말하기**: 소리만 듣고 따라 말하기 (텍스트 숨김)
  3. **빈칸 채우기**: 틀렸던 단어만 가리고 채우기
  4. **받아쓰기 재시험**: 다시 타이핑
- 녹음/음성인식은 연습 화면과 동일하게 동작

## 8. 영상 등록 파이프라인 (PC 전용)

사용자 조작: **명령 하나** (예: `add "유튜브URL" --profile 이름`) 또는 동등하게 단순한 방식.

스크립트 동작 순서:
1. yt-dlp로 자막 다운로드 — 사람이 단 자막 우선, 없으면 자동생성 자막. 한국어 자막이 있으면 함께 수신하여 `ko` 필드에 매칭
2. 문장 분할 (아래 규칙)
3. `/data/videos/{profileId}/` 에 JSON 생성, index.json 갱신
4. git add / commit / push 까지 자동 → GitHub Pages 반영

### 문장 분할 규칙
- 줄 끝 `.` `?` `!` → 문장 종료
- 다음 줄 대문자 시작 → 경계
- 줄 간 무음 0.8초 이상 → 경계
- 한 문장이 15초 또는 25단어 초과 → 강제 분할
- 시작 시간 = 첫 조각의 start, 끝 시간 = 마지막 조각의 end
- **자동생성 자막**: JSON에 `source: "auto_captions"` 표시. 영상 목록에서는 줄 끝에 `· auto captions` 라고만 적고,
  무엇이 문제인지는 연습 화면에서 설명한다 — 영상마다 주황색 딱지가 한 줄씩 붙으면 목록 전체가 경고처럼 보인다
  - 요즘 유튜브 자동자막은 구두점과 대문자를 넣어 준다. 자막에 문장부호가 실제로 있으면 구두점 규칙을 쓴다 — 안 쓰면 무음 간격에만 기대게 되어 문장이 25단어마다 잘린다
  - 문장부호가 없으면 무음 간격 + 강제 분할만으로 처리한다
  - 대문자 규칙은 자동자막에 쓰지 않는다. 반복되는 줄 때문에 단어 단위로 다루는데, 거기에 대문자 규칙을 쓰면 사람 이름마다 문장이 끊긴다
  - 자막 한 줄의 앞부분 단어에는 시각 표시가 없다. 여기에 자막이 화면에 뜬 시각을 주면 안 된다 — 한 줄은 다 말해진 뒤에 뜨므로 그 단어들은 그보다 **먼저** 말해진 것이다. 큐 시작에서 단어당 0.28초씩 거슬러 올라가 놓는다. 이걸 어기면 문장 앞부분이 통째로 잘려 들린다
- `[applause]` `[Music]` 같은 소리 표시는 **문장 경계로 쓰고** 글자에서만 뺀다. 그냥 지우면 박수 구간이 사라져 그 앞뒤 문장이 맞붙는다. `>>` (말하는 사람 바뀜)도 같게 다룬다
- **연습에 못 쓰는 줄은 뺀다.** 낱말 두 개 이하(`Hi.` `Yeah.` `Me too.`)이거나
  줄 전체가 감탄사뿐인 줄(`No, no, no.` `Wait, wait, wait.`)은 받아쓰기가 되지 않는다.
  실제 자료에서 네 개 중 하나가 이런 토막이었다 — 목록만 채우고 뒤 문장으로 가는 길을 막는다.
  등록할 때 아예 빼고, 앱에서도 같은 규칙으로 감춘다 (아래 "이미 등록한 영상")
- 웃음소리·노래처럼 말이 아닌 구간 표시는 `[laughter]` `(laughs)` `♪ 가사 ♪` 세 모양을 모두 본다
- 자막 조각을 끝내는 것은 **완전히 빈 줄**뿐이다. 공백 한 칸짜리 줄을 끝으로 보면 그 뒤 내용을 통째로 잃고, 그 말은 다음 조각에서 주워지면서 문장 시작이 몇 초씩 늦어진다. 유튜브 자동자막은 실제로 그런 줄을 넣는다

#### 이미 등록한 영상
앱도 같은 규칙(`app.js` 의 `usableLine`)으로 못 쓰는 줄을 **감춘다.** 지우지는 않는다 —
문장카드와 진도가 **자리 번호**를 가리키고 있어서 번호가 밀리면 예전에 담아 둔 카드가
엉뚱한 문장을 가리키게 된다. 파일은 그대로 두고 목록·이동·개수에서만 뺀다.
등록 스크립트와 앱, 두 곳에 같은 규칙이 있으므로 한쪽을 고치면 다른 쪽도 고친다.
검사는 `python3 tools/test_add_video.py` 와 `node tools/test_lines.js`.

### 편집 화면 (보정용)
- 자동 분할 결과를 목록으로 표시, 각 경계에 "합치기 / 여기서 자르기" 버튼
- **자르기 시각은 추정값이다.** 자막은 줄 단위로만 시각을 주므로 앞뒤 글자 길이 비율로 나눈다. 화면에 그 사실을 적고 들어서 확인하라고 안내한다
- 수정 결과를 JSON 파일로 내려받기 → 저장소의 기존 파일 교체 (Claude Code로 반영)
- 자막 직접 붙여넣기 입력도 지원 (유튜브 스크립트 패널 / SRT / VTT / Language Reactor 붙여넣기 — 포맷 자동 인식). 등록 스크립트가 실패하는 예외 상황의 우회로

### 폰에서 발견한 영상
Phase 1에서는 도구 없이 운영으로 해결: 링크를 메신저 "나에게 보내기" 등으로 모아뒀다가 PC에서 일괄 등록. (앱 내 대기열 기능은 Phase 2 후보)

## 9. 기기별 대응

### 공통
- 반응형 — 폰 세로 화면 기준으로 연습 화면 최적화
- 마이크 권한은 최초 1회 요청

### 앱 화면 언어

**앱 UI 문구는 영어로 쓴다.** 영어 학습 앱이므로 화면 자체가 영어인 편이 자연스럽다.
PC 등록 스크립트가 뿌리는 말과 설치 안내(`tools/README.md`)는 **한국어로 둔다** — 그건 운영자용이다.

### 앱으로 설치 (PWA)

`manifest.json` + `sw.js` 로 PC 바탕화면·폰 홈 화면에 앱처럼 설치할 수 있다.
설치 절차는 `INSTALL.md` 에 있다 (운영자용).

**서비스 워커는 반드시 network-first 로 둔다.** 캐시를 먼저 주는 방식으로 짜면
고친 코드가 기기에 영영 안 내려가는 덫이 된다. 게다가 워커 안에서 그냥 `fetch(req)` 를
쓰면 브라우저 HTTP 캐시를 타서 옛 파일이 나온다 — `cache: 'no-cache'` 로 서버에
꼭 물어보게 한다. 확인해 보고 알았다.

캐시는 인터넷이 안 될 때만 쓴다. 오프라인에서도 화면은 뜨지만 유튜브 재생은 안 된다.

### 아이폰 (크롬 포함 — 엔진은 Safari)
- **7일 미접속 시 사이트 데이터 삭제 규칙** 존재 → 설치 안내에 "홈 화면에 추가" 필수 단계로 포함. 홈 화면 추가 시 이 규칙에서 제외됨
- Web Speech API(음성인식 채점) 동작 불확실 → **기능 감지(feature detection)로 처리**: 지원되면 버튼 표시, 미지원이면 해당 버튼만 숨기고 녹음 듣기는 유지. 오류 화면 금지
- **마이크는 한 곳에만 준다.** 음성인식이 마이크를 가져가면 녹음이 그 자리에서 끊긴다.
  기능 감지로는 못 잡는다 — 있다고 나오는데 같이 못 쓰는 것이다.
  녹음 중 마이크가 끊기는 것을 보고 알아채고, **그 뒤로는 음성인식을 걸지 않는다.**
  둘 중 하나를 골라야 하면 **녹음이 먼저다** — 내 목소리를 들어 보는 것이 SPEC 6 의 4·5단계다
- **멈추기는 무슨 일이 있어도 한 번은 답을 준다.** 아이폰에서는 녹음기가 저 혼자 멈추고도
  멈췄다고 알려 주지 않는 일이 있다. 알림만 기다리면 답이 영영 안 와서 단추가 `Stop` 인 채로
  굳는다 — 실제로 겪었다. 알림 · 녹음기 상태 · 시계 세 가지로 받는다.
  화면 쪽도 답을 기다리지 않고 **단추를 먼저 되돌린다**
- MediaRecorder 오디오 포맷이 다를 수 있음 — 재생 호환 확인 필요

### 갤럭시 / PC Chrome
- 전 기능 동작 예상. 특이 대응 없음

## 10. Phase 구분

### Phase 1 (이번 구축 범위)
- 프로필 선택, 홈(갈래 고르기), 프로필별 영상 목록
- 연습 루프 5단계 전체 (녹음 포함, 음성인식은 기능 감지)
- 채점(관대/엄격), 힌트 단계, 단축키
- 문장카드 4모드. **자동 적재는 하지 않는다** — 담을 문장은 사람이 고른다 (운영자 결정)
- 오답 리포트(자주 틀리는 단어 목록), 기본 통계(연속 일수, 일별 학습량)
- 등록 스크립트(yt-dlp 파이프라인), 편집 화면
- 데이터 내보내기/불러오기 (백업용 — 파일 + 클립보드 복사 방식 둘 다)
- 하루 다섯 문장 (6-2) — 만드는 쪽은 GitHub Actions, 채점·녹음·카드는 기존 것 재사용

### Phase 2 후보 (지금 만들지 않음, 구조만 훼손하지 않게)
- 쉐도잉 모드 (영상 전체를 문장마다 멈추며 따라 말하기)
- 폰 → PC 등록 대기열
- 녹음 파일 선택 저장
- 문장카드 간격 반복(spaced repetition)
- 구두점 복원(자동생성 자막 품질 개선)
- **AI 대화 연습** (Langua 식 — 내 문장 교정 + 대화 + 대화 요약) — ROADMAP 2단계.
  **"비용 0원"만 예외로 깬다. "서버 없음"은 그대로 지킨다.** 폰이 회사에 직접 말을 건다
  (확인함 — ROADMAP 의 "브라우저에서 직접 부를 수 있나" 표).
  화면 흐름, 대화 요약 설계, 열쇠 다루기, 비용 산식, 넘어가는 판정 기준이 전부
  `ROADMAP.md` 2단계 절에 있다. **거기를 먼저 읽는다.**
  **주고받는 것은 글뿐이다 — 회사 한 곳, 열쇠 하나, 연 2.5만 원.**
  받아적기는 폰 내장(또는 브라우저 안에서 도는 것)을 쓴다 — AI 로 하려면 서버가 필요해서 뺐다.
  **AI 목소리는 안 산다** — 폰 내장을 쓴다 (2026-08-23 운영자 결정, 근거는 ROADMAP)

## 10-b. 대화 연습 — 열쇠 넣기 (ROADMAP 2단계, 만드는 중)

2단계는 **유료 API 를 쓴다.** SPEC 2 의 "비용 0원"만 예외로 깨고 **"서버 없음"은 그대로 지킨다** —
폰이 회사에 직접 말을 건다. 근거와 값은 `ROADMAP.md` 2단계 절에 있다.

**만드는 것은 0원이다. 돈은 열쇠를 넣는 순간부터 나간다** (2026-08-23 운영자 결정).
그래서 1단계 판정을 기다리지 않고 만들어 두고, 열쇠는 판정 뒤에 넣는다.

### 열쇠는 기기에 하나

- 홈 → `Settings` → `AI key`
- **IndexedDB `settings` 저장소. 여기만 프로필 ID 를 안 넣는다** —
  같은 기기를 가족이 나눠 쓰는 것이 이 앱의 전제인데(SPEC 3), 열쇠를 사람마다 두면
  각자 발급받아야 해서 감당이 안 된다. **기기에 하나를 넣고 같이 쓰고, 그 사실을 화면에 적는다**
- **백업(SPEC 10)에 넣지 않는다.** 백업 파일은 사람이 손으로 주고받는 물건이다
- 저장소에 올리지 않는다

### 넣을 때 실제로 한 번 불러 본다

저장하기 전에 제일 짧은 요청을 한 번 보낸다. **안 되면 저장하지 않는다** —
안 되는 열쇠를 넣어 두면 대화 도중에야 알게 되고, 그때는 원인을 갈라내기가 더 어렵다.

실패는 갈래를 나눠 사람 말로 알린다 (SPEC 9 "조용히 실패하지 않는다").

| 언제 | 화면에 |
|---|---|
| 열쇠가 틀림 (401·403) | 열쇠가 거부됐다. 다 복사했는지 보라 |
| 한도·잔액 (429, 결제 관련 400) | 열쇠는 되는데 계정에 한도가 걸렸다 |
| 인터넷 (연결 실패) | 회사에 닿지 못했다. 연결을 보고 다시 |
| 답이 없음 (30초) | 답이 안 온다. 다시 |
| 회사 쪽 문제 (5xx) | 회사가 지금 문제다. 나중에 |

### 회사마다 어댑터를 따로 쓴다

`talk.js` 에 회사별로 주소·헤더·부르는 모양을 둔다.
**설정 한 줄로 갈아 끼우려 하면 안 된다** — 답의 모양을 강제하는 방식도,
브라우저에서 직접 부를 수 있는지도 회사마다 다르다 (ROADMAP "모델 선택 기준").

지금은 **Anthropic 하나**다. 브라우저에서 직접 부를 수 있는 것이 확인됐고
(`anthropic-dangerous-direct-browser-access` 헤더 — 없으면 CORS 에서 막힌다),
답의 모양 강제와 캐싱이 같이 되는 것도 확인된 곳이라서다.

**`fetch` 와 `Promise` 를 쓰지 않는다.** 구형 iOS 사파리에서 그대로 돌아야 한다.

## 10-c. 대화 연습 — 화면과 흐름 (ROADMAP 2단계)

```
홈 → Talk → 주제 고르기 → 대화 → End → 대화 요약
```

화면은 `talkui.js` 가 그린다. **`app.js` 에 넣지 않았다** — 통째로 새 기능이고
`app.js` 는 이미 크다. `app.js` 쪽은 다리(화면 옮기기·폰 목소리)만 놓는다.

### 주제 고르기

- **Free talk** — 상황을 하나 뽑는다. **하루 문장이 쓰는 `config.situations` 를 그대로 쓴다** —
  목록을 따로 두면 둘이 어긋난다
- **From today's sentences** — 최근 묶음의 문장을 이어서
- **From a show** — 등록한 영상의 대사를 이어서

**없는 갈래는 줄을 아예 안 그린다.** 자료가 없는데 단추만 있으면 눌러 보고 실망한다.

### 말하면 알아서 돈다 — 단추로 굴리지 않는다 (2026-08-26 운영자 지적)

처음에는 `Say` 를 눌러 말하고 `Send` 를 눌러 보냈다. **그러면 대화가 아니라 서식 채우기가 된다** —
운영자가 써 보고 "챗이랑 뭐가 다르냐" 고 했다. 맞는 말이었다.

```
듣는다 → 말이 멎으면 저절로 보낸다 → 답을 소리로 읽는다 → 다시 듣는다
```

- **말하는 동안 받아적히는 것이 화면에 흐른다.** 다 끝나야 보이면 말하기가 불안하다
- **조용해지면 보낸다. 다만 얼마나 기다릴지는 하나로 정해 두지 않는다** (2026-08-26 운영자 지적).
  1.5초로 뒀더니 **생각하는 사이에 잘라 보냈다** — 영어를 짜맞추는 사람은 문장 한가운데서 멈춘다.
  원어민 기준으로 재면 안 된다
  - 말이 끝난 것 같으면 **2.5초**
  - **아직 이어질 것 같으면 5초.** 마지막 낱말이 관사·전치사·접속사·조동사·대명사이거나
    쉼표로 끝나면 뒤에 말이 더 있는 것이다. 실제로 `It's kind of a` 에서 잘린 적이 있다
  - **어느 쪽으로 틀릴지도 정해 두었다** — 오래 기다리는 것은 조금 답답할 뿐이지만
    **잘라 버리면 대화가 깨진다.** 그래서 헷갈리면 기다리는 쪽으로 기운다
- **기다리는 동안 바가 차오른다.** 아무 예고 없이 사라지면 놀란다.
  차오르는 동안 말을 이으면 처음으로 돌아간다
- **다 말했으면 그 칸을 눌러 바로 보낸다.** 기다릴 이유가 없을 때가 있다
- **멈춰도 하던 말은 안 없어진다** (2026-08-26 운영자 지적). 멈추면 사라져서
  처음부터 다시 말해야 했다. 이제 멈춘 뒤에도 화면에 남고,
  **이어서 말하면 앞말에 붙고, 그대로 눌러 보낼 수도 있다.**
  받아적힌 것을 지우는 자리는 **보내고 난 뒤 한 곳뿐**이다 — 여러 군데서 지우면
  어느 하나가 하던 말을 삼킨다
- 셈은 `node tools/test_talkwait.js`
- 답이 오면 읽어 주고, **다 읽으면 마이크가 저절로 다시 열린다**
- 사람이 누를 것은 **멈추기와 끝내기 둘뿐**이다

지켜야 할 것 넷:

- **녹음기(`MediaRecorder`)를 켜지 않는다.** 대화는 소리를 저장하지 않으므로 음성인식만 있으면 되고,
  그래야 **아이폰에서 녹음기와 음성인식이 마이크를 다투는 일이 아예 없다**
  (`record.js` 에 적어 둔 그 문제다). 그래서 여기서는 `Recorder` 를 안 쓴다
- **읽는 동안에는 마이크를 닫는다.** 안 그러면 제 목소리를 받아적는다
- **다 읽었다는 알림이 안 오는 기기가 있다.** 글자 길이로 어림잡은 시계로 한 번 더 받는다 —
  이걸 빼면 답을 읽고 나서 마이크가 안 열려 대화가 거기서 멈춘다
- **사파리는 `continuous` 를 흘려버리고 저 혼자 끝낸다.** 끝났다는 알림이 오면
  아직 들을 차례인지 보고 다시 켠다. **다만 끝없이 다시 켜지는 않는다** — 아래 참고

### 마이크·스피커·마이크 소리 — 우리가 할 수 있는 것과 없는 것 (2026-08-26 확인)

운영자가 셋을 물었다. **둘은 브라우저에서 안 된다.** 안 되는 것을 되는 척하지 않는다.

| 물어본 것 | 되나 | 왜 |
|---|---|---|
| 마이크 고르기 | **안 된다** | 음성인식(Web Speech)에는 어느 마이크를 쓸지 고르는 길이 아예 없다. `getUserMedia` 로는 고를 수 있지만 그건 녹음용이고, 음성인식은 그 갈래를 안 받는다 |
| 스피커 고르기 | **안 된다** | 소리를 어느 스피커로 낼지 고르는 것(`setSinkId`)이 **아이폰 사파리에 없다.** 게다가 `speechSynthesis` 는 그 갈래 자체가 없다 |
| 마이크 켜고 끌 때 나는 소리 | **못 없앤다, 못 바꾼다** | 그건 **아이폰이 내는 소리**다. 웹페이지에는 끄거나 다른 소리로 바꿀 길이 없다. 우리가 소리를 하나 더 얹을 수는 있어도 그건 더 시끄럽다 |
| 답을 읽어 줄 **목소리** 고르기 | **된다** | 폰에 든 영어 목소리를 목록으로 준다 — 남자·여자, 미국·영국·호주. SPEC 10-d |

**소리에서 우리가 줄일 수 있는 것은 횟수뿐이다.** 사파리는 조용하면 저 혼자 듣기를
끝내고, 아직 들을 차례면 우리가 다시 켠다 — 그때마다 소리가 또 난다. 생각하느라
오래 말이 없으면 그 소리가 **끝없이 되풀이된다.** 자리를 비우면 영영 그런다.

그래서 **마지막 말로부터 1분 조용하면 다시 켜지 않는다.** 단추는 도로 `Talk` 이 되고,
다시 누르면 그때부터 듣는다. **짧게 잡으면 생각하는 사이에 마이크가 꺼지므로
여기서도 헷갈리면 기다리는 쪽이다.** 셈은 `node tools/test_talkwait.js`.

**그리고 이미 저 혼자 끝난 것을 또 끄지 않는다.** 끄는 동작 자체가 소리를 내므로,
끝났다는 알림을 받고 나서 한 번 더 끄면 소리만 한 번 더 난다.
`stopHearing(true)` 가 그 자리다 — 끄는 동작은 건너뛰고 뒷정리만 한다.

> **켤 때와 끌 때 중 어느 쪽이 들리는지는 우리가 정하지 못한다.** 운영자는 아이폰에서
> **끌 때만** 들린다고 했다. 왜 한쪽만 들리는지는 모른다 — 폰이 정하는 것이고,
> 우리에게는 둘 다 손댈 방법이 없다. **모르는 것을 아는 척해서 원인을 지어내지 않는다**
> (`Fixed` 를 고칠 때 배운 것과 같다). 우리가 할 수 있는 것은 **끄는 횟수를 줄이는 것뿐**이다.

소리가 아예 싫은 날에는 **`Type instead` 로 바꾸면 마이크를 안 쓴다** — 소리도 안 난다.

**마이크가 없거나 막힌 기기에서는 타이핑 칸이 대신 나온다.** 오류 화면을 띄우지 않는다 (SPEC 9).
되는 기기에서도 `Type instead` 로 언제든 바꿀 수 있다 — 조용한 데서 할 때가 있다.

### 받아적힌 글을 그 자리에서 바로 고친다 (2026-08-26 운영자 지적)

운영자가 말했다 — **"가끔 낱말 하나를 잘못 알아듣는데, 그걸 바꾸지 않고 보내면 좀 이상해진다."**
맞다. 한 글자 고치자고 문장 전체를 다시 말할 수는 없다.

처음에는 `Fix a word` 단추를 두고, 누르면 그 글이 담긴 **딴 칸**이 열리게 했다.
운영자가 되물었다 — **"그냥 바로 수정하면 안 되고 그 과정을 겪어야 되냐."**
맞는 말이었다. **보이는 글과 고치는 글이 같은 자리여야 한다** —
한 낱말 고치자고 단추를 거치게 하면 그건 고치는 게 아니라 옮겨 쓰는 것이다.

**받아적힌 상자 자체가 칠 수 있는 칸이다.** 낱말을 누르면 거기 커서가 간다.
고치고 `Send` 를 누르면 고친 쪽이 나간다. **거치는 단계가 없다.**

**그리고 고치는 동안에도 마이크는 켜 둔다** (2026-08-26 운영자 지적).
처음에는 손대는 순간 마이크를 껐다 — 새 말이 들어오면 커서가 튀기 때문이다.
운영자가 말했다 — **"마이크는 계속 켜 두고 수정하고 얘기하고 수정하고 얘기하고 그렇게 못 하나."**
된다. 커서가 튀는 것은 마이크가 아니라 **그리는 쪽**에서 막을 일이었다.

- **받아적히는 중인 조각은 손대는 동안 안 넣는다.** 그건 한 마디 안에서도 계속 바뀌어서,
  넣었다 뺐다 하면 글이 요동친다. **그 마디가 끝나면 그때 한 번에 뒤에 붙는다**
- **커서 자리를 그대로 되돌려 놓는다.** 새 말은 글 끝에 붙으므로 고치던 자리는 그 앞에 그대로 있다
- **고치는 중에는 저절로 안 보낸다.** 치는 사이에 나가 버리면 고칠 틈이 없다.
  손댈 때 돌던 시계를 멈추고, 손을 떼면 다시 잰다 — **말이 들어왔을 때만 재면
  이미 돌고 있던 시계가 고치는 사이에 터진다** (실제로 그랬다)
- **잠깐 닫고 싶으면 작은 글씨의 `Pause the mic`.** 조용한 데서 하거나 옆에서 떠들 때가 있다
- **손댈 때·뗄 때 화면을 통째로 다시 그리면 안 된다.** 큰 단추를 누르면 칸에서 손이 떨어지는데,
  그때 다시 그리면 **누르려던 그 단추가 사라져 누름이 허공에 떨어진다** —
  고쳐 놓고 `Send` 를 눌러도 안 나갔다. 실제로 그랬다. 글 있는 칸만 다시 그린다
- **친 것은 곧바로 담아 두는 자리(`saidFinal`)로 간다.** 화면은 아무 때나 다시 그려지므로
  칸 안에만 두면 고친 것이 사라진다. **담아 두는 자리는 하나여야 한다** —
  멈춰도 말이 안 없어지는 것과 같은 규칙이다
- **칸이 하나뿐이다.** 마이크가 없는 기기도 같은 칸에 친다 — 딴 칸을 만들면
  어느 쪽이 나갈지 알 수 없다. 안내 글귀만 바뀐다
- **손대면 테두리가 또렷해진다** — 고칠 수 있는 자리라는 걸 그때 알게 된다
- 글자는 어느 단계에서도 **16px 아래로 내려가지 않는다** (가장 작은 단계도 17px).
  안 그러면 칸을 누를 때 iOS 가 화면을 확대한다
- **이미 보낸 것은 못 고친다.** 나가는 동안에는 칸이 잠긴다

### 되풀이된 낱말을 저절로 지우지 않는다 (2026-08-26 운영자 물음, 안 하기로 결정)

운영자가 물었다 — **"말을 더듬어서 같은 낱말이 두 번 들어가면 자동으로 없앨까,
아니면 듣는 쪽이 알아서 알아들을까?"**

**저절로 지우지 않는다.** 기계는 더듬은 것과 제대로 말한 것을 **구별하지 못한다.**

| 두 번 나온 것 | 실제로는 |
|---|---|
| `I had had enough` | **맞는 영어다** (과거완료) |
| `the thing that that guy said` | **맞는 영어다** |
| `no no no`, `very very good`, `bye bye` | **맞는 영어다** |
| `I I went there` | 더듬은 것 |

규칙으로 가르려면 **낱말 목록**을 적어야 하는데, 그건 이 저장소가 이미 두 번 실패한 길이다
(하루 문장의 `and that ~`, 여기의 `wanna`/`gonna`). 게다가 **지우는 것은 조용한 실패**다 —
맞는 영어가 사라져도 아무도 모른다.

대신 **이미 있는 두 가지로 받는다.**

1. **AI 쪽은 이미 안다.** 지시문에 *"받아적힌 글이라 더듬고 되풀이하는 것이 섞여 있다"* 가
   들어 있고, 판단 기준은 하나다 — **사람 입에서 그렇게 나오면 이상한가.**
   더듬은 것은 그 질문에서 저절로 걸러진다
2. **거슬리면 손으로 지운다.** 바로 위의 `Fix a word` 가 그 자리다.
   **기계가 조용히 지우는 것보다 사람이 보고 지우는 것이 낫다**

### 한 턴의 생김새 — 내 말 → 답 → 고친 것 (2026-08-26 운영자 결정)

```
┌───────────────────────┐
│ 내가 한 말             │   ← 상자로 둘러 내 말임을 표시한다
└───────────────────────┘
AI 대답                      ← 바로 밑에. 폰 목소리로 읽어 준다
│ ✓ nothing to fix          ← 고칠 것이 없으면 한 줄. 누르면 펴진다
│ ─────────────────────
│ [Fixed]      고친 문장     ← 고칠 것이 있을 때만 파란 상자
│ [More natural]
│ [why]        배울 표현은 ** 로 강조 (Daily 의 note 와 같은 규칙)
│ [Save card]
```

**처음에는 내 말과 답 사이에 교정을 끼워 놓았다.** 운영자가 "내 문장을 보낸 칸과
답변한 칸이 잘 안 된다" 고 했다 — 맞는 말이었다. **내가 한 말과 그에 대한 답이
서로 멀어져서 어느 것이 짝인지 알기 어려웠다.** 교정을 사이에 두면
대화가 한 번에 한 줄씩 끊긴다.

그래서 **말과 답을 붙이고, 교정은 그 밑으로 내렸다.** 교정은 대화가 아니라
**나중에 볼 공부거리**다. 그 자리를 왼쪽 세로줄로 표시해 대화와 갈라 놓는다.

- **고칠 것이 없으면 크게 그리지 않는다.** 할 말이 없는데 큰 상자가 떠 있으면
  기계처럼 느껴진다 (운영자 지적). **파란 상자가 뜨는 것 자체가 신호**가 되게 한다
- 그래도 **칸은 비지 않는다** — 접힌 줄을 누르면 그 안에 다 들어 있다
- 화면 위에 **그 대화에서 고친 횟수**를 적는다. 챗과 다른 점이 눈에 보이는 자리다
- **`Save card` 는 "고친 문장" 을 담는다** — 내가 한 말이 아니라 고쳐진 쪽이다
- **`End` 를 눌러야 끝난다.** 자동으로 안 끝낸다 — 하다 말면 요약도 반쪽이다

### 이건 말하기지 글쓰기가 아니다 (2026-08-26 운영자 지적)

운영자가 `Fixed` 와 `More natural` 이 **이상하게 고쳐진다**고 했다. 화면을 보니 원인이 둘이었다.

**1. 받아적힌 글이 이미 깨져 있는데 그걸 사람 실수로 봤다.**
`they're they the reason`, `or sarcastic` 같은 것은 **폰이 잘못 들은 것**이지 학습자가
틀린 것이 아니다. 그런데 낱말을 하나씩 기워서 **말이 안 되는 문장**을 내놓았다.

**2. 글쓰기 기준으로 고쳤다.** `wanna` 를 "격식 있는 글에서는 want to" 라고 지적했다.
**말하기 연습에서 `wanna` 는 맞는 말이다.** 대소문자와 문장부호를 지적한 것도 같은 잘못이다 —
**타이핑한 것이 아니다.**

#### 고치는 법 — 낱말 목록이 아니라 상황으로 (2026-08-26 운영자 결정)

처음에는 지시문에 **낱말 목록**을 적었다 — "`wanna` `gonna` `kinda` 는 고치지 마라",
"되풀이된 낱말은 **보통 폰이 잘못 들은 것**이다". 운영자가 그것을 되물었다.

- **"내가 진짜 잘못 말한 걸 수도 있다. 내 영어가 완벽하지 않으니까."**
- **"말을 더듬는 과정에서 두 번 들어간 걸 수도 있고."**
- **"예외를 계속 넣는 규칙을 만들다 보면 말이 이상해질 거 아니야."**

맞는 말이고, **이 저장소가 하루 문장에서 이미 겪은 것**이다 (CLAUDE.md "규칙은 울타리로만 쓴다").
게다가 더 나쁜 잘못이 하나 있었다 — **원인을 단정한 것.** 되풀이된 낱말이 잘못 들은 것인지
더듬은 것인지 진짜 실수인지 **알 수가 없는데 안다고 써 놓았다.**

그래서 **원인을 추측하게 하지 않고 결과를 판단하게 한다.**

지시문은 이제 **상황을 설명한다** — 폰에 대고 실시간으로, 아직 배우는 중인 말로,
문장을 짜면서 말하고 있다. 그래서 더듬고 되풀이하고, 문장부호는 본인 것이 아니고,
이상한 낱말 중 어떤 것은 폰이 잘못 들은 것이고 어떤 것은 본인 실수인데 **가릴 수가 없다.**

**가릴 필요도 없다. 판단 기준은 하나다:**

> **사람 입에서 그렇게 나오면 이상하게 들리나?**
> 이상하면 고치고, 그냥 지나갈 만하면 둔다.

이 질문 하나로 `wanna` 도, 더듬은 것도, 잘못 들은 것도 다 처리된다. **목록이 필요 없다.**

나머지 셋은 그대로 둔다.

- `corrected` 는 **그 자체로 말이 되는 문장**이어야 한다. 낱말만 기워 놓고 엉킨 채로 두지 않는다
- `natural` 은 **같은 뜻을 다시 말하는 것**이다. 평서문을 의문문으로 바꾸지 않고,
  **없던 뜻을 지어내지 않는다**
- **모르겠으면** 제일 그럴듯한 하나를 `corrected` 에 넣고, `why` 에 모르겠다고 적고,
  `reply` 에서 되묻는다. **지어내서 아는 척하지 않는다**

> **다만 틀은 규칙으로 남는다.** 칸 넷을 반드시 채우는 것은 상황에 맡기지 않는다 —
> 그것이 이 단계의 존재 이유이고, 판단이 아니라 틀이기 때문이다.
> **선은 여기 있다: 틀은 규칙으로, 무엇이 좋은 영어인가는 상황으로.**
> `node tools/test_talk.js` 가 양쪽을 다 지킨다 — 칸이 필수인지, 그리고
> **지시문이 다시 낱말 목록으로 돌아가지 않았는지.**

### AI 가 매 턴 되묻지 않게 한다

처음 지시문에 "되물어서 대화를 이어가라" 고 써 놓았더니 **매 턴 질문이 돌아와
대화가 아니라 인터뷰가 됐다** (운영자 지적). 지금은 이렇게 시킨다 —
가끔은 자기 얘기를 하거나 그냥 맞장구치고, **정말 궁금할 때만 되묻는다.**
답 길이도 25~45 단어로 줄였다. **소리로 읽히는 글이라 길면 듣기 지친다.**

### 답의 모양을 강제한다 — 이 단계의 존재 이유

칸 넷(`corrected` `natural` `why` `reply`)을 미리 정하고 **넷 다 필수**로 둔다.
비우면 형식에 안 맞아 거부되므로 **까먹는 것이 구조적으로 불가능**해진다.
`additionalProperties: false` 로 딴 칸을 채워 넣는 것도 막는다.

보장되는 것은 **"칸이 채워진다"** 이지 "교정이 항상 훌륭하다" 가 아니다.

**모양이 틀리면 조용히 한 번 다시 청한다. 두 번 틀리면 그 턴만 건너뛰고 화면에 남긴다.**

검사는 `node tools/test_talk.js`.

### 지시문은 대화 밖에 둔다

매 요청마다 맨 앞에 다시 붙는 자리(`system`)에 둔다 — 대화가 100턴이 돼도 묻힐 자리가 없다.
여기에 **자주 틀리는 낱말(오답 기록)과 최근 담은 문장카드**를 같이 실어 보낸다.

- **같은 재료면 글자까지 같아야 한다.** 조금이라도 바뀌면 캐싱이 처음부터 다시 걸린다
- **재료가 없으면 그 줄을 아예 안 넣는다**
- **길수록 싸다.** Haiku 4.5 는 앞부분이 4,096 토큰을 넘어야 캐싱이 걸리므로,
  이걸 싣는 것이 덤이 아니라 **비용을 맞추는 장치**다 (ROADMAP 비용 절)

### 얼마 들었는지 보여 준다 (2026-08-26 운영자 요청)

운영자가 물었다 — "지금까지 API 비용은 얼마 썼어?" 그때는 **앱이 기록을 안 해서 답할 수 없었다.**
막연한 돈 걱정은 숫자로 바꾸는 것이 낫다.

답에 사용량(`usage`)이 같이 온다. 그걸로 그 자리에서 값을 낸다.

- **대화 요약 화면**에 이번 대화 값
- **리포트 화면**에 이번 달 / 전부 / 대화당 평균

지켜야 할 것 넷:

- **달러로 적는다** (운영자 결정). 콘솔(`platform.claude.com` → Usage)이 달러로 보여 주므로
  **같은 단위여야 대조가 된다.** 원화로 바꾸면 환율까지 얽혀 맞춰 볼 수가 없다
- **`input_tokens` 는 캐시에 안 걸린 나머지다.** 캐시에 넣어 둔 것과 다시 읽은 것은 따로 온다.
  셋을 각각 다른 배수로 세야 하고, **겹쳐 세면 안 된다**
- **아주 작은 값이 0 으로 보이면 안 된다.** 센트 아래는 셋째 자리까지 적고,
  그보다 작으면 `<$0.001` 로 적는다
- **이건 우리가 셈한 값이지 청구서가 아니다.** 화면에 그렇게 적어 둔다 —
  안 그러면 이 숫자를 청구서로 오해한다

단가는 `talk.js` 의 회사표에 있다. **값이 바뀌면 거기만 고친다.**
셈은 `node tools/test_talkcost.js` — **실제 청구서와 맞춰 본 것도 검사에 넣어 두었다.**

### 대화 요약

`End` 를 누르면 대화 전체를 한 번 더 보내 다섯 칸을 받는다 —
`summary` / `did_well` / `to_fix` / `culture` / `words`.
`words` 는 **한 번에 문장카드로 담는 단추**를 둔다.

### 조용히 실패하지 않는다 (SPEC 9)

| 언제 | 화면에 | 쓴 글은 |
|---|---|---|
| 인터넷 끊김 | 연결이 끊겼다. 다시 누르면 이어진다 | **안 지운다** |
| 열쇠가 틀림 | 설정에서 다시 넣어라 | 안 지운다 |
| 한도 초과 | 지금은 여기까지 쓸 수 있다 | 안 지운다 |
| 답이 없음(30초) | 다시 보내라 | 안 지운다 |
| 답의 모양이 틀림 | 한 번 다시 청하고, 두 번 틀리면 그 턴만 건너뛴다 | 안 지운다 |

### 대화 기록과 문장카드

- **기기 안(IndexedDB `talks`)에만 둔다.** 저장소에 안 올린다 — 개인 대화다
- **백업(SPEC 10)에는 요약만 들어간다.** 대화 원문은 안 들어간다 —
  백업 파일은 사람이 손으로 주고받는 물건이고, 다시 읽을 값어치는 요약 쪽에 있다
- **대화 번호는 절대 바뀌면 안 된다** — 문장카드가 그 번호를 가리킨다 (Daily 묶음과 같은 규칙)
- 카드의 `videoId` 는 `talk-{번호}`(턴)와 `talk-{번호}-w`(요약의 배울 표현) 두 가지다
- **카드 화면은 이 둘을 기기에서 찾는다.** 저장소에 파일이 없기 때문이다 —
  이걸 빼먹으면 카드는 담기는데 열면 빈 화면이 나온다
- **한국어(`ko`)를 넣지 않는다.** 대화에는 한국어가 없다.
  그래서 한→영 모드에는 안 나오고 듣고 말하기·받아쓰기 재시험에만 나온다

## 10-d. 사람마다 고르는 것 (2026-08-26 운영자 요청)

운영자가 말했다 — **"글자가 잘 안 보인다"**, **"지금 너무 어렵다"**,
그리고 **"이 모든 걸 프로필마다 설정해서 바꿀 수 있게 해 줘. 마이크 시간 포함."**

`Settings` 화면 맨 위에 다섯을 둔다. **프로필별이다** — 같은 기기를 가족이 나눠 쓰고 취향이 다르다.
(AI 열쇠만 기기별이다. SPEC 10-b. **둘을 헷갈리면 안 된다.**)

| 고르는 것 | 값 | 기본 |
|---|---|---|
| 색 | 어둡게 / 밝게 | 어둡게 |
| 글자 크기 | 보통 / 크게 / 제일 크게 | 보통 |
| 답을 읽어 줄 목소리 | 폰에 든 영어 목소리 | 폰이 고른 대로 |
| 대화에서 차례 넘기기 | **눌러서 보낸다** / 저절로 보낸다 | 눌러서 |
| 보내기 전에 기다리는 시간 | 1.5 ~ 8초 | 2.5초 |

지켜야 할 것 여섯:

- **기본값은 `prefs.js` 한 곳에만 둔다.** 화면마다 적어 두면 어느 하나가 어긋난다.
  고를 수 있는 목록도 거기 있고, 설정 화면은 그것을 보고 그린다
- **고르는 즉시 저장되고 화면에 먹는다. 저장 단추를 두지 않는다** —
  눌러야 되는 줄 모르고 나가면 안 바뀐 채로 남는다
- **색은 반드시 `var(--…)` 로만 쓴다.** 화면마다 색을 직접 적으면
  테마를 바꿀 때 거기만 안 바뀐다
- **글자 크기도 `var(--f-…)` 로만 쓴다.** 고정 px 을 쓰면 그 칸만 안 커진다.
  입력칸은 어느 단계에서도 16px 아래로 내려가면 안 된다 — iOS 가 화면을 확대한다
- **모르는 값이 들어와도 기본값으로 되돌린다.** 옛 백업을 불러올 수 있다
- **목소리만은 고를 목록을 미리 못 적는다.** 폰마다 들어 있는 것이 달라서,
  그때그때 폰에 물어본다. 없는 기기에서는 **그 줄을 아예 안 그린다** (SPEC 9)

셈은 `node tools/test_prefs.js`.

### 목소리 — 고를 수 있는 것은 이것뿐이다

운영자가 **마이크와 스피커도 고르게 해 달라**고 했다. **브라우저에서는 안 된다** —
왜인지는 SPEC 10-c 의 표에 적어 두었다. 대신 **답을 읽어 줄 목소리**는 고를 수 있다.

- 폰에 든 목소리 중 **영어인 것만** 보여 준다. 한국어 목소리로 영어를 읽으면 알아들을 수 없다
- **고르면 바로 한 문장 읽어 준다.** 이름만 보고는 어떤 목소리인지 알 수 없다.
  `Hear it` 로 다시 들을 수도 있다
- **목록은 브라우저가 늦게 채운다.** 채워지면 한 번 다시 그린다 —
  안 그러면 설정을 열 때마다 목소리 줄이 비어 있다
- 고른 목소리가 **없어졌으면 폰이 고른 대로 둔다.** 폰을 바꾸거나 목소리를 지울 수 있다
- 그 옆에 **마이크와 스피커는 폰에서 바꾸라고 한 줄 적는다.** 없는 기능을 찾아 헤매지 않게

### 차례 넘기기 — 두 갈래를 다 둔다

처음에는 **저절로 보내는 쪽만** 만들었다. 운영자가 "단추 없이 말하면 되게 해 달라"고 해서다.
그런데 써 보니 **생각하는 사이에 잘리고, 답이 끝나자마자 마이크가 열려 쫓기는 느낌**이라
**눌러서 보내는 쪽으로 바꿔 달라**고 했다.

**한쪽을 지우지 않는다.** 지우면 다른 쪽이 맞는 사람이 못 쓴다. 골라 쓰게 두고,
**기본은 눌러서 보내는 쪽**이다.

- 눌러서 보내는 쪽: 아무것도 저절로 나가지 않는다. 답을 읽은 뒤에도 **눌러야 마이크가 열린다**
- 저절로 보내는 쪽: 조용해지면 나가고, 답을 읽은 뒤 저절로 다시 듣는다.
  기다리는 시간은 위 표의 값을 쓴다 (말끝이 이어질 것 같으면 거기에 더 기다린다 — SPEC 10-c)

### 누를 것은 하나여야 한다

전에는 **크기가 같은 단추 셋**이 나란히 있었다. 무엇을 눌러야 할지 알 수 없었다.

이제 **큰 것 하나 + 아래 작은 글씨 둘**이다. 큰 것은 그때 할 일 하나만 말한다 —
`Start talking` → `Listening` → **`Send`** → `Thinking…` → `Speaking`.
`Type instead` 와 `End and get a report` 는 밑줄만 있는 작은 글씨다.

**말이 잡히는 순간 큰 단추가 `Send` 로 바뀌어야 한다.** 안 그러면 눌러도 멈추기만 하고
한 번 더 눌러야 나간다 — 실제로 그랬다. 다만 **글자가 바뀔 때만 다시 그린다**(매번 그리면 깜빡인다).

## 11. 검증 체크리스트 (구현 후 사용자가 직접 확인할 항목)

각 항목은 사용자가 코드를 읽지 않고 확인 가능해야 한다.

1. PC에서 등록 명령 1회 → 몇 분 내 사이트에 영상 표시
2. 문장 구간 재생이 문장 첫 단어를 자르지 않음
3. 타이핑 채점 — 틀린 단어만 빨갛게, 관대 모드에서 대소문자/마침표 무시 확인
4. 녹음 → 재생 → 재녹음, 다음 문장으로 넘어가면 녹음 사라짐
5. PC에서 음성인식 채점 동작
6. 갤럭시: 전 기능 / 아이폰: 홈 화면 추가 후 데이터 유지, 음성인식 버튼 유무와 무관하게 녹음 동작
7. 프로필 2개 만들어 같은 기기에서 진도가 섞이지 않음 확인
8. 브라우저 껐다 켜도 진도 유지
9. 내보내기 → 다른 브라우저에서 불러오기 → 진도 복원
10. 홈 → `Daily` → 오늘 문장 5개가 보임. 한국어를 보고 말한 뒤 `Show answer`
11. 문장이 실제로 말하는 것 같은 일상 대화체인지 (교과서 문장 같으면 실패)
12. `Save card` → 카드 화면 한→영 모드에 그 문장이 있음
13. 며칠 뒤에도 새 문장이 들어옴 (안 들어오면 화면 위에 경고가 뜸)
14. 리포트 화면에 **주간 판정표**가 보임. 최근 네 주의 연 날 / 건너뛴 비율 / 담은 카드
15. 홈 → `Settings` → 아무 글자나 넣고 `Check and save` → **저장되지 않고** 이유가 뜸.
    제대로 된 열쇠를 넣으면 저장됨. `Remove key` 로 지워짐
16. 열쇠를 넣은 뒤 백업을 받아 열어 봐도 **열쇠가 그 안에 없음**
17. 홈 → `Talk` → 주제를 고르고 한 마디 → **고친 문장이 별도 박스로 뜸**.
    10턴을 해도 그 박스가 매번 채워짐 (이게 이 기능의 전부다)
18. 비행기 모드로 바꾸고 `Send` → 이유가 뜨고 **쓴 글이 안 지워짐**
19. `End` → 잘한 것·고칠 것·문화·배울 표현이 나옴
20. `Save card` → 카드 화면에서 그 문장이 실제로 열림 (빈 화면이면 실패)
21. 백업을 받아 열어 봐도 **대화 원문은 없고 요약만 있음**
22. `Settings` → 색을 밝게 → **화면 전체가 바로 밝아짐** (설정 화면만이 아니라)
23. 글자를 제일 크게 → **모든 화면의 글자가 커짐**. 입력칸을 눌러도 화면이 확대되지 않음
24. 프로필을 바꾸면 **그 사람이 고른 대로 돌아옴**
25. `Talk` 에서 말한 뒤 **가만히 있어도 안 나감.** `Send` 를 눌러야 나감.
    답을 읽은 뒤에도 **눌러야 마이크가 열림**
26. 설정에서 "저절로 보낸다" 로 바꾸면 **가만히 있어도 나감**
27. `Talk` 의 한 턴이 **내가 한 말 → AI 답 → 고친 문장** 차례로 보임.
    내 말과 답이 **붙어 있어야** 한다 (사이에 교정이 끼면 실패)
28. `Settings` → 목소리를 바꾸면 **그 자리에서 바뀐 목소리로 한 문장 읽어 줌**.
    `Talk` 으로 가서 답을 들으면 **같은 목소리로 읽음**
29. `Talk` 에서 마이크를 켜 놓고 **1분 동안 아무 말도 안 하면 마이크가 저절로 꺼짐**
    (단추가 도로 `Talk` 이 됨). 그때까지 **띠롱 소리가 되풀이되지 않아야** 한다
30. `Talk` 에서 말한 뒤 **받아적힌 글을 바로 눌러** 낱말 하나를 고치고 `Send` →
    **고친 쪽이 나감** (화면의 내 말도 고친 것으로 보임). 누를 때 **화면이 확대되지 않음**
31. 고치는 동안에도 **마이크가 안 꺼짐**. 고치다 말고 그냥 이어서 말하면
    **고친 것 뒤에 붙고, 고치던 자리(커서)를 안 잃음**
31-b. `Shows` 의 2번에 **한국어가 안 보이고** `Show the Korean` 이 있음. 누르면 나오고,
    다음 문장으로 가면 **도로 감김**. `Daily` 쪽 한국어는 **그대로 보임**
32. 프로필을 고르면 **영상 목록이 아니라 고르는 화면**이 나옴. `Dictation` 을 눌러야 영상 목록이 나옴.
    각 칸에 오늘 한 개수가 보이고, 연습을 한 뒤 홈으로 돌아오면 그 숫자가 올라가 있음
