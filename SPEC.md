# 가족용 영어 받아쓰기 연습 앱 — 개발 스펙 v1.0

> 이 문서는 Claude Code에 그대로 전달하기 위한 스펙이다.
> 사용자(운영자)는 코드를 읽거나 쓰지 않는다. 모든 안내는 "설치 → 실행 → 확인" 수준으로 제공할 것.
> 이미 동작하는 코드를 수정할 때는 한 번에 한 가지만 바꾸고 확인 후 다음으로 넘어갈 것.

---

## 1. 목표

dictation 방식의 문장 단위 받아쓰기 연습을, 사용자가 고른 아무 유튜브 영상으로 할 수 있는 개인·가족용 무료 웹앱.

- 비용 0원 (유료 API, 유료 서버 없음)
- 가족 구성원 여러 명이 각자 프로필로 사용
- PC(Windows, Chrome), 갤럭시(Chrome), 아이폰(Safari/chrome 엔진)에서 동작

## 2. 확정된 아키텍처 결정사항

| 항목 | 결정 | 이유 |
|---|---|---|
| 호스팅 | GitHub Pages (정적 사이트) | 무료, https(마이크 필요), 딜레이 없음 |
| 백엔드 서버 | **없음** | 필요 없음. Apps Script는 콜드스타트 딜레이로 기각됨 |
| 문장 데이터 보관 | 저장소 내 JSON 파일 (앱과 같은 repo) | CORS 원천 차단 불가, 반영 즉시 |
| 개인 진도/기록 | 브라우저 IndexedDB (프로필별 키 분리) | 기기별 독립, 서버 불필요. localStorage 아님 — 용량 문제 |
| 영상 등록 | **PC 로컬 스크립트** (yt-dlp) | 유튜브가 클라우드 서버 IP를 차단하므로 서버 방식은 불안정. 집 IP는 안정적 |
| 영상 재생 | YouTube IFrame Player API | 무료, 키 불필요. 영상 데이터는 일절 보관하지 않음 (구간 재생 명령만) |
| 인증/로그인 | **없음** | 프로필 선택만. 비밀번호 없음 |
| 녹음 | 메모리에만 유지, 저장 안 함 | 문장 넘어가면 폐기 |

## 3. 사용자 모델

- 프로필: 이름 + 색상/이모지 정도의 단순 구조. 앱 첫 화면에서 선택 (넷플릭스 방식)
- **영상 라이브러리는 프로필별로 분리** — 각자 자기 영상만 보임 (전체 보기 옵션은 있어도 됨)
- 문장카드(복습 저장), 오답 기록, 진도, 통계 모두 프로필별 분리
- 같은 기기를 두 사람이 쓸 수 있음 → IndexedDB 키에 프로필 ID 포함

## 4. 데이터 구조

### 4-1. 저장소 내 정적 데이터 (전 기기 공유, 등록 스크립트가 생성)

```
/data/
  profiles.json          # [{id, name, color}]
  videos/
    {profileId}/
      index.json         # 그 사람의 영상 목록 [{videoId, title, addedAt, sentenceCount, hasKorean, source}]
      {videoId}.json     # 문장 데이터
```

`{videoId}.json` 형태:

```json
{
  "videoId": "abc123",
  "title": "...",
  "source": "manual_captions | auto_captions",
  "sentences": [
    {
      "i": 0,
      "start": 12.4,
      "end": 17.8,
      "text": "영어 문장.",
      "ko": "한국어 번역 (있을 때만)",
      "recording": null
    }
  ]
}
```

- `recording` 필드는 향후 확장용 자리만 확보. Phase 1에서는 항상 null.
- `index.json` 의 `source` 는 8번의 "경계가 부정확할 수 있음" 뱃지를 라이브러리에서 띄우기 위한 것이다. 없으면 영상 파일을 전부 열어봐야 한다.
- 구간 재생 시 시작점에 lead-in 여유를 둔다 (유튜브 탐색이 정밀하지 않고, 자동자막은 문장 시작을 늦게 잡는다). 듣기 화면 슬라이더로 0~2.0초를 0.1초 단위로 맞춘다. 기본값 0.6초. 문장 목록 위에 둔다.
- 끝점에도 `end + 0.3초` 여유를 둔다. 자동자막의 단어 시각이 정밀하지 않아 마지막 단어가 잘린다. 이 값도 설정으로 조절 가능하게.
- 유튜브 플레이어의 눈금과 시간 표시는 감춘다(`controls: 0`). 재생 위치가 보이면 듣기에 방해가 되고, 조작은 앱 버튼으로만 한다.
- 유튜브 자막(CC)은 꺼야 한다. 답이 화면에 그대로 보이기 때문이다. `cc_load_policy: 0` 만으로는 확실하지 않아 재생기가 준비될 때·재생이 시작될 때·영상을 갈아 낄 때마다 `unloadModule` 로 끈다. `setOption` 으로 끄려 하면 오히려 켜진다. 조작 막대를 감췄으므로 사용자가 손으로 끌 방법이 없다 — 앱이 책임진다.
- **화면을 덮고 소리만 듣는 선택지를 둔다.** 어떤 영상은 번역 자막이 그림 자체에 새겨져 있어 어떤 설정으로도 못 가린다. 받아쓰기에는 소리만 있으면 된다. 주 기능이 아니므로 설정 줄에 작은 토글로 둔다.
- **이전/다음 문장 버튼은 화면 아래에 고정한다.** 연습 중 가장 자주 누르는 버튼인데 화면을 움직일 때마다 찾아 헤매게 된다.

### 4-2. IndexedDB (기기별·프로필별)

- 진도: 영상별 마지막 문장 위치, 문장별 시도/정답 여부
- 오답 기록: 틀린 단어 목록 (단어, 문장 참조, 날짜) → 오답 리포트 재료
- 문장카드: 저장한 문장 참조 + 저장 사유(수동/자동)
- 일별 학습량: 하루에 채점한 문장 수. 연속 학습 일수는 여기서 센다

### 4-3. 오답 리포트 / 통계 화면

라이브러리에서 들어간다. 한 화면에 셋을 담는다.

- **연속 학습 일수** — 오늘부터 거꾸로 이어지는 날 수. 오늘 아직 안 했으면 어제까지 인정한다
- **최근 14일 학습량** — 하루에 채점한 문장 수를 막대로
- **자주 틀리는 낱말** — 많이 틀린 순. 낱말을 누르면 그 낱말을 마지막으로 틀린 문장으로 바로 간다

목록이 길어지면 위 50개만 보여 준다. 자주 틀리는 것을 보자는 화면이므로 꼬리는 도움이 안 된다.

## 5. 화면 흐름

```
프로필 선택 → 내 라이브러리(영상 목록) → 연습 화면
                                   → 문장카드 복습
                                   → 통계/오답 리포트
설정(채점 관대함, 재생 오프셋 등)
편집 화면(문장 경계 보정용 — 평소엔 안 씀)
```

## 6. 연습 화면 — 핵심 루프 (문장당 5단계)

1. **리스닝** — 해당 구간만 재생. 반복 재생, 0.75배속 지원
2. **타이핑** — 들은 것을 입력
3. **정답 확인** — 단어 단위 비교, 틀린 단어만 표시. 오답 단어는 자동으로 오답 기록에 적재. 틀린 문장은 자동으로 문장카드에 적재
4. **따라 말하기** — 마이크 녹음 (메모리에만). 지원 기기에서는 음성인식(Web Speech API)으로 받아적어 정답과 비교 표시
5. **녹음 듣기** — 원본 ↔ 내 녹음 번갈아 듣기, 재녹음 가능 → 다음 문장

### 채점 규칙
- 기본: 관대 모드 — 대소문자, 문장부호, 축약형(it's/it is), 붙임표(short-term/short term) 차이는 정답 처리
- 설정에서 엄격 모드 전환 가능 (설정 화면이 생기기 전까지는 연습 화면에 토글을 둔다)
- 축약형은 **애매하지 않은 것만** 같게 본다. `Bill's` 같은 소유격은 `Bill is` 로 풀지 않는다
- 낱말 비교는 자리를 하나씩 맞추지 않고 **가장 길게 겹치도록 맞춰 본다.** 낱말 하나를 빠뜨렸을 때 그 뒤가 전부 오답이 되면 안 된다
- 문장 글자는 **채점하거나 정답을 보기 전까지 감춘다.** 보이면 받아쓰기가 아니다

### 힌트 단계 (정답 보기 전)
0.75배속 재듣기 → 각 단어 첫 글자 → 각 단어 앞 절반 → 정답 공개

단어 개수만 알려 주는 단계는 두지 않는다. 첫 글자를 보여 주면 개수도 함께 보인다.

```
T–––– y–– s– m––– f–– j–––––– u– o– t––– S–––––.
Tha–– yo– s– mu–– fo– join––– u– o– th–– Sun–––.
Thank you so much for joining us on this Sunday.
```

### 단축키 (PC)
입력칸에 글을 치는 중에도 눌리는 조합으로 정했다.

| 키 | 하는 일 |
|---|---|
| `Enter` | 채점. 채점한 뒤 다시 누르면 다음 문장 |
| `Shift+Enter` | 다시 듣기 |
| `Alt+H` | 힌트 한 단계 |
| `Alt+S` | 느리게(0.75배속) 듣기 |

넓은 화면에서는 이 목록을 화면에도 적어 둔다.

## 7. 문장카드 복습 모드

- 카드 적재: **수동 저장**(연습 화면의 `Save card`) + 오답 자동 적재
- 복습 모드 4종:
  1. **한→영**: 한국어 보고 영어로 말하기 (`ko` 있는 문장만)
  2. **듣고 말하기**: 소리만 듣고 따라 말하기 (텍스트 숨김)
  3. **빈칸 채우기**: 틀렸던 단어만 가리고 채우기
  4. **받아쓰기 재시험**: 다시 타이핑
- 녹음/음성인식은 연습 화면과 동일하게 동작

## 8. 영상 등록 파이프라인 (PC 전용)

사용자 조작: **명령 하나** (예: `add "유튜브URL" --profile 이름`) 또는 동등하게 단순한 방식.

스크립트 동작 순서:
1. yt-dlp로 자막 다운로드 — 사람이 단 자막 우선, 없으면 자동생성 자막. 한국어 자막이 있으면 함께 수신하여 `ko` 필드에 매칭
2. 문장 분할 (아래 규칙)
3. `/data/videos/{profileId}/` 에 JSON 생성, index.json 갱신
4. git add / commit / push 까지 자동 → GitHub Pages 반영

### 문장 분할 규칙
- 줄 끝 `.` `?` `!` → 문장 종료
- 다음 줄 대문자 시작 → 경계
- 줄 간 무음 0.8초 이상 → 경계
- 한 문장이 15초 또는 25단어 초과 → 강제 분할
- 시작 시간 = 첫 조각의 start, 끝 시간 = 마지막 조각의 end
- **자동생성 자막**: JSON에 `source: "auto_captions"` 표시. 앱 라이브러리에서 "경계가 부정확할 수 있음" 뱃지 표시
  - 요즘 유튜브 자동자막은 구두점과 대문자를 넣어 준다. 자막에 문장부호가 실제로 있으면 구두점 규칙을 쓴다 — 안 쓰면 무음 간격에만 기대게 되어 문장이 25단어마다 잘린다
  - 문장부호가 없으면 무음 간격 + 강제 분할만으로 처리한다
  - 대문자 규칙은 자동자막에 쓰지 않는다. 반복되는 줄 때문에 단어 단위로 다루는데, 거기에 대문자 규칙을 쓰면 사람 이름마다 문장이 끊긴다
  - 자막 한 줄의 앞부분 단어에는 시각 표시가 없다. 여기에 자막이 화면에 뜬 시각을 주면 안 된다 — 한 줄은 다 말해진 뒤에 뜨므로 그 단어들은 그보다 **먼저** 말해진 것이다. 큐 시작에서 단어당 0.28초씩 거슬러 올라가 놓는다. 이걸 어기면 문장 앞부분이 통째로 잘려 들린다
- `[applause]` `[Music]` 같은 소리 표시는 **문장 경계로 쓰고** 글자에서만 뺀다. 그냥 지우면 박수 구간이 사라져 그 앞뒤 문장이 맞붙는다. `>>` (말하는 사람 바뀜)도 같게 다룬다
- 자막 조각을 끝내는 것은 **완전히 빈 줄**뿐이다. 공백 한 칸짜리 줄을 끝으로 보면 그 뒤 내용을 통째로 잃고, 그 말은 다음 조각에서 주워지면서 문장 시작이 몇 초씩 늦어진다. 유튜브 자동자막은 실제로 그런 줄을 넣는다

### 편집 화면 (보정용)
- 자동 분할 결과를 목록으로 표시, 각 경계에 "합치기 / 여기서 자르기" 버튼
- **자르기 시각은 추정값이다.** 자막은 줄 단위로만 시각을 주므로 앞뒤 글자 길이 비율로 나눈다. 화면에 그 사실을 적고 들어서 확인하라고 안내한다
- 수정 결과를 JSON 파일로 내려받기 → 저장소의 기존 파일 교체 (Claude Code로 반영)
- 자막 직접 붙여넣기 입력도 지원 (유튜브 스크립트 패널 / SRT / VTT / Language Reactor 붙여넣기 — 포맷 자동 인식). 등록 스크립트가 실패하는 예외 상황의 우회로

### 폰에서 발견한 영상
Phase 1에서는 도구 없이 운영으로 해결: 링크를 메신저 "나에게 보내기" 등으로 모아뒀다가 PC에서 일괄 등록. (앱 내 대기열 기능은 Phase 2 후보)

## 9. 기기별 대응

### 공통
- 반응형 — 폰 세로 화면 기준으로 연습 화면 최적화
- 마이크 권한은 최초 1회 요청

### 앱 화면 언어

**앱 UI 문구는 영어로 쓴다.** 영어 학습 앱이므로 화면 자체가 영어인 편이 자연스럽다.
PC 등록 스크립트가 뿌리는 말과 설치 안내(`tools/README.md`)는 **한국어로 둔다** — 그건 운영자용이다.

### 아이폰 (크롬 포함 — 엔진은 Safari)
- **7일 미접속 시 사이트 데이터 삭제 규칙** 존재 → 설치 안내에 "홈 화면에 추가" 필수 단계로 포함. 홈 화면 추가 시 이 규칙에서 제외됨
- Web Speech API(음성인식 채점) 동작 불확실 → **기능 감지(feature detection)로 처리**: 지원되면 버튼 표시, 미지원이면 해당 버튼만 숨기고 녹음 듣기는 유지. 오류 화면 금지
- MediaRecorder 오디오 포맷이 다를 수 있음 — 재생 호환 확인 필요

### 갤럭시 / PC Chrome
- 전 기능 동작 예상. 특이 대응 없음

## 10. Phase 구분

### Phase 1 (이번 구축 범위)
- 프로필 선택, 프로필별 라이브러리
- 연습 루프 5단계 전체 (녹음 포함, 음성인식은 기능 감지)
- 채점(관대/엄격), 힌트 단계, 단축키
- 문장카드 4모드, 오답 자동 적재
- 오답 리포트(자주 틀리는 단어 목록), 기본 통계(연속 일수, 일별 학습량)
- 등록 스크립트(yt-dlp 파이프라인), 편집 화면
- 데이터 내보내기/불러오기 (백업용 — 파일 + 클립보드 복사 방식 둘 다)

### Phase 2 후보 (지금 만들지 않음, 구조만 훼손하지 않게)
- 쉐도잉 모드 (영상 전체를 문장마다 멈추며 따라 말하기)
- 폰 → PC 등록 대기열
- 녹음 파일 선택 저장
- 문장카드 간격 반복(spaced repetition)
- 구두점 복원(자동생성 자막 품질 개선)

## 11. 검증 체크리스트 (구현 후 사용자가 직접 확인할 항목)

각 항목은 사용자가 코드를 읽지 않고 확인 가능해야 한다.

1. PC에서 등록 명령 1회 → 몇 분 내 사이트에 영상 표시
2. 문장 구간 재생이 문장 첫 단어를 자르지 않음
3. 타이핑 채점 — 틀린 단어만 빨갛게, 관대 모드에서 대소문자/마침표 무시 확인
4. 녹음 → 재생 → 재녹음, 다음 문장으로 넘어가면 녹음 사라짐
5. PC에서 음성인식 채점 동작
6. 갤럭시: 전 기능 / 아이폰: 홈 화면 추가 후 데이터 유지, 음성인식 버튼 유무와 무관하게 녹음 동작
7. 프로필 2개 만들어 같은 기기에서 진도가 섞이지 않음 확인
8. 브라우저 껐다 켜도 진도 유지
9. 내보내기 → 다른 브라우저에서 불러오기 → 진도 복원
